% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diversity.R
\name{pop_diversity}
\alias{pop_diversity}
\title{Within-population genetic diversity}
\usage{
pop_diversity(
  x,
  group = NULL,
  by = c("genome", "gene", "window"),
  genes = NULL,
  window = 10000,
  step = NULL,
  accessible = NULL,
  het = c("missing", "dosage"),
  min_snps = DIVERSITY_MIN_SNPS,
  max_missing = 0.1,
  min_samples = 4,
  genotype = NULL,
  meta = NULL
)
}
\arguments{
\item{x}{A \link{PopStructure}, or a genotype matrix (samples x SNPs, alt dosage 0/1/2,
\code{NA} for missing) with \code{chr:pos} column names.}

\item{group}{Metadata column naming the grouping (for a \code{PopStructure}) or a vector
aligned to the matrix rows. \code{NULL} treats every sample as one population.}

\item{by}{\code{"genome"} (default), \code{"gene"}, or \code{"window"}.}

\item{genes}{Gene table for \code{by = "gene"} (columns \code{name}, \code{chr}, \code{start}, \code{end});
defaults to \link{PF3D7_GENES}. Coordinates are 0-based half-open CDS spans.}

\item{window, step}{Window size and step in bp for \code{by = "window"}. \code{step} defaults to
\code{window}, giving abutting windows; set it smaller for a \strong{sliding} scan --
\verb{window = 5000, step = 2500} steps a 5 kb window along in 2.5 kb hops, so consecutive
windows share half their span. Sliding is the usual way to scan Tajima's D: a fixed
grid can split a signal across two windows and dilute it in both, and overlapping
windows also make the track read more smoothly. The cost is that neighbouring windows
are no longer independent -- do not count them as separate findings (see
\code{\link[=selection_peaks]{selection_peaks()}}).}

\item{accessible}{Callable regions as a data frame with \code{chrom}, \code{start}, \code{end} --
e.g. \link{PF3D7_CORE_REGIONS}. Sets the denominator of \code{pi} and \code{theta_w}.}

\item{het}{How to read a heterozygous call: \code{"missing"} or \code{"dosage"}.}

\item{min_snps}{Fewest SNPs for Tajima's D (default 3).}

\item{max_missing}{Per-SNP missingness allowed into the Tajima's D block.}

\item{min_samples}{Skip a group with fewer samples than this.}

\item{genotype}{Genotype matrix to use instead of a \code{PopStructure}'s stored one --
pass the \strong{full, unpruned} set, since LD-pruning removes the very sites diversity
is measured over.}

\item{meta}{When \code{x} is a matrix, a data frame with \code{sample} plus \code{group}.}
}
\value{
A tibble with one row per group x unit: \code{group}, the unit's identity, then
\code{n_samples}, \code{n_snps}, \code{n_sites}, \code{seg_sites}, \code{he}, \code{pi}, \code{theta_w}, \code{tajima_d},
\code{n_hap}, \code{hap_div}, \code{shannon_h}, \code{simpson_lambda}, \code{evenness}. \code{tajima_p} is the
two-sided test against the standard neutral model and \code{tajima_percentile} places each
unit's D within its own group's distribution -- read \code{\link[=tajima_d_pvalue]{tajima_d_pvalue()}} before using
the former, which is conservative under recombination.
}
\description{
Nucleotide diversity, expected heterozygosity, Watterson's theta, Tajima's D and
haplotype diversity, computed for each metadata group and reported genome-wide, per
gene, or in sliding windows.
}
\details{
\strong{\code{pi} is per accessible base pair} -- the sum of per-site heterozygosity over the
number of callable sites (\code{n_sites}), so windows of different SNP density stay
comparable. Pass \code{accessible} to say which bases were callable; without it every base
of the unit is assumed callable, which inflates nothing but does assume your VCF was
called across the whole span. \code{he} is the same per-site quantity averaged over the
\strong{SNPs} instead, which is what papers usually label expected heterozygosity (Hs/He);
it is not nucleotide diversity and the two are never comparable.

The parasite is haploid: \code{het = "missing"} (the default) treats a heterozygous call as
a mixed infection and drops it at that site, so an allele count is a count of samples.
\code{het = "dosage"} instead splits the call evenly between the two alleles.

Tajima's D needs a single sample count, so it is computed on a complete-case block --
SNPs missing in more than \code{max_missing} of the group are dropped, then any sample
still missing a call. \code{n_taj_snps} / \code{n_taj_samples} report what survived, and the
statistic is \code{NA} below \code{min_snps} SNPs or four samples.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
pop_diversity(ps, group = "country")
}
\references{
Nei, M. (1987) \emph{Molecular Evolutionary Genetics}. Columbia University Press.

Watterson, G. A. (1975) On the number of segregating sites in genetical models without
recombination. \emph{Theoretical Population Biology} 7, 256-276.
\doi{10.1016/0040-5809(75)90020-9}

Korunes, K. L. & Samuk, K. (2021) pixy: Unbiased estimation of nucleotide diversity and
divergence in the presence of missing data. \emph{Molecular Ecology Resources} 21,
1359-1368. \doi{10.1111/1755-0998.13326}
}
\seealso{
\code{\link[=pop_diff]{pop_diff()}} for between-group differentiation, \code{\link[=ld_index]{ld_index()}} and
\code{\link[=read_ld_decay]{read_ld_decay()}} for linkage disequilibrium, \code{\link[=run_ihs]{run_ihs()}} for selection.
}
