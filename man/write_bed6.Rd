% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bed_subtract.R
\name{write_bed6}
\alias{write_bed6}
\title{Write an interval table as a six-column BED, with optional metadata}
\usage{
write_bed6(
  x,
  file,
  name = NULL,
  score = NULL,
  strand = NULL,
  meta_cols = NULL,
  semicolon = ":",
  chrom = NULL,
  sort = TRUE,
  name_is_coords = FALSE,
  make_names_unique = FALSE
)
}
\arguments{
\item{x, file, chrom, sort}{As for \code{\link[=write_bed]{write_bed()}}.}

\item{name, score, strand}{Columns to write in those fields, or \code{NULL} for the defaults
above.}

\item{meta_cols}{Columns to carry in the seventh column, as a character vector of names,
or \code{TRUE} for every column not already written. \code{NULL} (the default) writes six columns.}

\item{semicolon}{What replaces a \verb{;} inside a metadata field or value.}

\item{name_is_coords}{Write \code{chrom-start-end} (\code{Pf3D7_07_v3-403221-403363}) as the name
field instead of a column (default \code{FALSE}).}

\item{make_names_unique}{Make the name field unique within the file (default \code{FALSE}):
a name that occurs more than once gets \verb{_1}, \verb{_2}, ... in file order, zero-padded to the
width of that name's count (\verb{_01} to \verb{_12} for twelve). Combines with \code{name_is_coords},
so identical coordinates written twice get distinct names. With no name column and
\code{name_is_coords = FALSE}, the coordinates are used as the names to make unique.}
}
\value{
\code{file}, invisibly.
}
\description{
The BED6 layout: \code{chrom}, \code{start}, \code{end}, \code{name}, \code{score}, \code{strand}, tab separated, no
header, \code{start} 0-based half-open. An optional seventh column carries any other columns
of the table as \verb{[field=value;field=value;]}, the form \code{elucidator} reads metadata in
(\code{meta_cols}; the name \code{meta} is kept for sample-metadata tables throughout the package).
}
\details{
Defaults follow the table: \code{name} is its \code{name} column, else \code{chrom-start-end}; \code{score}
is its \code{score} column, else the interval's width in bp; \code{strand} is its \code{strand} column,
else \code{+}. The chromosome column is chosen as \code{\link[=write_bed]{write_bed()}} does, preferring the
reference's own spelling. Missing values in \code{name}, \code{score} and \code{strand} are written as
\code{.}.

Metadata fields and values are written as-is, whitespace included, except that \verb{;} --
the separator -- is replaced by \code{semicolon} (default \code{:}) so a value can never split a
field. Numbers are written in full, never in scientific notation.
}
\examples{
targets <- bed_subtract(PF3D7_GENES[PF3D7_GENES$name == "pfcrt", ],
                        tandem_repeats_to_avoid(pf3d7_tandem_repeats()), pad = 10)
f <- file.path(tempdir(), "pfcrt_pieces.bed")
write_bed6(targets, f, meta_cols = c("gene_id", "piece"))
readLines(f)[1:2]

# every piece is called "pfcrt": number them, or name them by their coordinates
write_bed6(targets, f, make_names_unique = TRUE)
readLines(f)[1:2]
write_bed6(targets, f, name_is_coords = TRUE)
readLines(f)[1:2]
}
\seealso{
\code{\link[=write_bed]{write_bed()}} for the three- or four-column form.
}
