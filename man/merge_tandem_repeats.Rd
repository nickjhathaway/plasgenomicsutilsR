% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tandem_repeats.R
\name{merge_tandem_repeats}
\alias{merge_tandem_repeats}
\title{Merge tandem repeats that run into one another}
\usage{
merge_tandem_repeats(x, gap = 0, dedupe = FALSE)
}
\arguments{
\item{x}{A \code{\link[=tandem_repeats]{tandem_repeats()}} table, or anything \code{\link[=tandem_repeats]{tandem_repeats()}} accepts. An \code{avoid}
column (\code{\link[=flag_tandem_repeats]{flag_tandem_repeats()}}) is carried through as \emph{any member flagged}.}

\item{gap}{Records separated by at most this many bases are merged too (default \code{0}:
only overlapping or abutting ones).}

\item{dedupe}{Drop a repeat that lies entirely inside another on the same chromosome
whose unit is the same up to rotation or reverse complement -- \code{TAT} within a longer
\code{ATA} run, \code{AT} within \code{ATAT} -- before merging (default \code{FALSE}). The two finders often report one run twice
that way. Such a record adds nothing: the container has the same period and at least
the width, so the blocks and every flag come out the same; only \code{n_repeats} and the
\code{repeat_units} list, which no longer names both spellings, change.}
}
\value{
A tibble of blocks in chromosome / start order: \code{chrom}, \code{chr}, \code{start}, \code{end},
\code{width}, \code{name} (\code{chrom-start-end}), \code{n_repeats} (records merged), \code{repeat_units}
(the distinct units, comma-separated, left to right along the block), \code{periods} (the distinct
periods, ascending), \code{min_period}, and \code{avoid} when \code{x} had it.
}
\description{
Repeat finders report a stretch like \code{AAAAAAAAAAATATATATAT} as two records, an \code{A}
homopolymer and an \code{AT} run, that overlap or abut; one trinucleotide often flows into
another the same way. For avoiding slippage the whole stretch is one region, so this
joins records that overlap, abut, or lie within \code{gap} bases, and keeps a record of what
went into each block.
}
\examples{
bed <- data.frame(chrom = "Pf3D7_07_v3", start = c(100, 115, 300),
                  end = c(116, 130, 330),
                  name = c("Pf3D7_07_v3-100-116__A_x16",
                           "Pf3D7_07_v3-115-130__AT_x7.5",
                           "Pf3D7_07_v3-300-330__AAT_x10"))
merge_tandem_repeats(bed)[, c("start", "end", "n_repeats", "repeat_units", "periods")]

# the same run reported as `A` and `AA`: dedupe keeps one spelling
twice <- rbind(bed, data.frame(chrom = "Pf3D7_07_v3", start = 100, end = 116,
                               name = "Pf3D7_07_v3-100-116__AA_x8"))
merge_tandem_repeats(twice)$repeat_units[1]
merge_tandem_repeats(twice, dedupe = TRUE)$repeat_units[1]
}
\seealso{
\code{\link[=tandem_repeats_to_avoid]{tandem_repeats_to_avoid()}}, \code{\link[=bed_merge]{bed_merge()}} for plain intervals
}
