% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tandem_repeats.R
\name{flag_tandem_repeats}
\alias{flag_tandem_repeats}
\title{Flag the tandem repeats a polymerase is likely to slip on}
\usage{
flag_tandem_repeats(
  x,
  min_width_by_period = c(`1` = 11, `2` = 12, `3` = 21),
  min_width = 50
)
}
\arguments{
\item{x}{A \code{\link[=tandem_repeats]{tandem_repeats()}} table, or anything \code{\link[=tandem_repeats]{tandem_repeats()}} accepts.}

\item{min_width_by_period}{Minimum total width (bp) to flag, per period. A named
numeric vector whose names are periods, or an unnamed one read as periods 1, 2, 3, ...
in order. \code{NULL} for no per-period rule.}

\item{min_width}{Minimum total width (bp) to flag a repeat of any period. Also the length
from which a \emph{merged} run is avoided in \code{\link[=tandem_repeats_to_avoid]{tandem_repeats_to_avoid()}}.}
}
\value{
\code{x} as a tibble with a logical \code{avoid} column.
}
\description{
Adds an \code{avoid} column to a \code{\link[=tandem_repeats]{tandem_repeats()}} table. A repeat is flagged when it is at
least \code{min_width} bases long whatever its unit, or when its \strong{period} has its own,
shorter, threshold and the run reaches that. Short units slip at shorter lengths, so
the defaults descend with the period:
}
\details{
\tabular{lll}{
   period \tab flagged from \tab i.e. \cr
   1 (homopolymer) \tab 11 bp \tab \code{AAAAAAAAAAA} \cr
   2 (dinucleotide) \tab 12 bp \tab six copies of \code{AT} \cr
   3 (trinucleotide) \tab 21 bp \tab seven copies of \code{AAT} \cr
   any \tab 50 bp \tab  \cr
}


These are the thresholds the HEOME Pf3D7 design used. Tighten or loosen them per
period, add periods (\code{c("1" = 11, "2" = 12, "3" = 21, "4" = 30)}), or drop the period
rules altogether (\code{min_width_by_period = NULL}) to flag on total length only.

Flags are per record. Two repeats that run into each other -- an \code{A} homopolymer into
an \code{AT} run, one trinucleotide into another -- are each judged on their own here;
\code{\link[=merge_tandem_repeats]{merge_tandem_repeats()}} joins them, and \code{\link[=tandem_repeats_to_avoid]{tandem_repeats_to_avoid()}} is the two steps
together, with a merged run flagged when any member is or when the run as a whole
reaches \code{min_width}.
}
\examples{
bed <- data.frame(chrom = "Pf3D7_07_v3", start = c(100, 200, 300, 400),
                  end = c(110, 212, 320, 460),
                  name = c("Pf3D7_07_v3-100-110__A_x10",      # 10 bp homopolymer: kept
                           "Pf3D7_07_v3-200-212__AT_x6",      # 12 bp dinucleotide: flagged
                           "Pf3D7_07_v3-300-320__AAT_x6.67",  # 20 bp trinucleotide: kept
                           "Pf3D7_07_v3-400-460__TATTG_x12")) # 60 bp: flagged
flag_tandem_repeats(bed)[, c("repeat_unit", "period", "width", "avoid")]
}
\seealso{
\code{\link[=tandem_repeats_to_avoid]{tandem_repeats_to_avoid()}}, \code{\link[=merge_tandem_repeats]{merge_tandem_repeats()}}
}
