% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pop_structure.R
\name{plot_admixture}
\alias{plot_admixture}
\title{Admixture (STRUCTURE) bar plot from a Q matrix}
\usage{
plot_admixture(
  q,
  samples = NULL,
  meta = NULL,
  group = NULL,
  order_within = TRUE,
  sample_order = NULL,
  colours = NULL,
  group_bar = FALSE,
  group_colours = NULL,
  border = TRUE,
  border_colour = "black",
  border_linewidth = 0.15,
  legend_position = c("right", "bottom", "top", "left", "none"),
  legend_rows = NULL
)
}
\arguments{
\item{q}{A samples-by-K ancestry matrix (e.g. from \code{\link[=snmf_q]{snmf_q()}}).}

\item{samples}{Sample ids (defaults to \code{rownames(q)}).}

\item{meta}{Optional metadata data frame with a \code{sample} column.}

\item{group}{Optional metadata column to facet by (e.g. \code{"region"}).}

\item{order_within}{Order samples within each group by clustering (ignored when
\code{sample_order} is supplied).}

\item{sample_order}{Optional explicit sample order (see \code{\link[=admixture_order]{admixture_order()}}); keeps
bars in the same place across different K.}

\item{colours}{Optional fill colours for the K clusters.}

\item{group_bar}{Draw a coloured strip above the bars keyed by \code{group}.}

\item{group_colours}{Named \code{level -> colour} vector for the group strip (see
\code{\link[=meta_colors]{meta_colors()}}); pass the same mapping you use for the UMAP to match colours.}

\item{border}{Outline each sample's bar (default \code{TRUE}, matching
\code{\link[=plot_structure_figure]{plot_structure_figure()}}) so neighbours with nearly identical ancestry stay distinct;
\code{FALSE} for borderless bars. With very many samples in a narrow render the outlines
can swamp the fills -- either render wider (\code{\link[=save_plot]{save_plot()}} uses the attached width) or
set \code{border = FALSE} / a thinner \code{border_linewidth}.}

\item{border_colour, border_linewidth}{Colour and width of the per-sample outline.}

\item{legend_position}{Where the legends go: \code{"right"} (default), \code{"bottom"}, \code{"top"},
\code{"left"}, or \code{"none"}. A large K makes for a tall legend stack, so \code{"bottom"} often
fits better on a wide, short admixture panel.}

\item{legend_rows}{Keys per legend column (or per row when the legend is horizontal)
before wrapping into another column/row. \code{NULL} (default) wraps a side legend every
10 keys and splits a horizontal one over two
rows, which keeps \code{K} = 15 plus a group strip on the page. The suggested output height
accounts for whatever this works out to.}
}
\value{
A ggplot object.
}
\description{
Stacked ancestry-proportion bars, one per sample, optionally faceted by a grouping
column, with an optional group colour strip whose colours can match another plot
(e.g. UMAP points).
}
