% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bed_subtract.R
\name{bed_subtract}
\alias{bed_subtract}
\title{Subtract one set of genomic intervals from another}
\usage{
bed_subtract(
  locs1,
  locs2,
  chrom1 = "chr",
  start1 = "start",
  end1 = "end",
  chrom2 = "chr",
  start2 = "start",
  end2 = "end",
  min_width = 1
)
}
\arguments{
\item{locs1}{Interval table to subtract from (a data frame / tibble).}

\item{locs2}{What to remove. An interval table, a character vector of \code{chr:pos} SNP ids,
a genotype matrix or \code{\link[=load_genotypes]{load_genotypes()}} list (its column names are the ids), or a
\link{PopStructure} (its genotype panel).}

\item{chrom1, start1, end1}{Column names in \code{locs1} (defaults \code{"chr"}, \code{"start"}, \code{"end"};
\code{"chrom"} is accepted as a chromosome alias).}

\item{chrom2, start2, end2}{As above for \code{locs2} when it is a table.}

\item{min_width}{Drop leftover pieces narrower than this (default \code{1}, i.e. keep every
base). Raise it to ignore slivers.}
}
\value{
A tibble of the uncovered pieces: every \code{locs1} column, with \code{start}/\code{end}
replaced by the piece's own bounds, plus \code{piece} (which piece of that row this is) and
\code{width}. Rows of \code{locs1} covered completely are absent. Input row order is kept.
}
\description{
The complement of \code{\link[=bed_intersect]{bed_intersect()}}, and a dependency-free \verb{bedtools subtract}: what is
left of \code{locs1} once everything in \code{locs2} is removed, cut at \strong{base} resolution. An
interval partly covered comes back as the pieces that are not, one row each, carrying its
original columns; one covered end to end disappears.
}
\details{
The use it was written for is filling gaps in a callset. \code{\link[=aa_intervals]{aa_intervals()}} gives the
genomic span of each codon you care about; subtracting the SNPs a panel already has leaves
the bases that were never called, which is what to hand \verb{bcftools mpileup -R}. Because the
cut is per base, a codon with one of its three bases in the panel still returns the other
two -- which is the point, since a residue cannot be read from one base.
}
\section{Coordinates}{
 Intervals are \strong{0-based half-open} \verb{[start, end)}, as in BED and
throughout this package, so an interval abutting another (\code{end1 == start2}) loses
nothing. See \link{plasgenomicsutilsR-coordinates}.
}

\examples{
cds <- read_gff_cds(system.file("extdata", "pf3d7_drug_gene_cds.gff",
                                package = "plasgenomicsutilsR"))
want <- aa_intervals(data.frame(transcript_id = c("pfcrt", "pfcrt", "pfkelch13"),
                                aa_position = c(72, 76, 580)), cds)

# a panel that happens to carry only the middle base of the K76T codon
have <- paste0("Pf3D7_07_v3:", want$start[want$aa_position == 76] + 1)
gaps <- bed_subtract(want, have)
gaps[, c("name", "aa_position", "start", "end", "width")]
}
\seealso{
\code{\link[=bed_intersect]{bed_intersect()}} for the overlap, \code{\link[=write_bed]{write_bed()}} to write the result out.
}
