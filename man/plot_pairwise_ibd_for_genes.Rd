% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize.R
\name{plot_pairwise_ibd_for_genes}
\alias{plot_pairwise_ibd_for_genes}
\title{IBD "triangle" panels for genes or specific SNPs}
\usage{
plot_pairwise_ibd_for_genes(
  x,
  genes = NULL,
  snps = NULL,
  group = NULL,
  within = 0,
  agg = c("mean", "median", "max"),
  individual = FALSE,
  label = TRUE,
  digits = 2,
  ncol = NULL,
  trans = "identity",
  colors = NULL,
  limits = NULL,
  fill_scale = NULL
)
}
\arguments{
\item{x}{An \link{IbdResults} object. Gene triangles use its IBD \code{blocks} / \code{gene_overlap}
table if present, else the \code{pairwise_group} per-SNP table.}

\item{genes}{Gene names to include from the track (default: all; case-insensitive).
Ignored if only \code{snps} is given.}

\item{snps}{Specific SNPs to draw, as \code{"chr:pos"} ids (e.g. \code{"Pf3D7_07_v3:403222"})
or a data frame with \code{chr}, \code{pos} (and optional \code{name}). Each is one facet.}

\item{group}{For block-based overlap, the metadata column defining the groups
(default: the first non-\code{sample} column of the object's \code{meta}).}

\item{within}{For block-based overlap, pad each gene interval by this many bp on both
sides (default \code{0}).}

\item{agg}{For the SNP fallback, how a gene's value aggregates its in-gene SNPs:
\code{"mean"} (default), \code{"median"}, or \code{"max"}.}

\item{individual}{If \code{TRUE}, return a \strong{named list} of one-triangle plots (one per
feature, e.g. to write a multi-page PDF) with the legend tucked into the empty
upper triangle; if \code{FALSE} (default), one faceted grid plot.}

\item{label}{Draw the value in each tile.}

\item{digits}{Decimal places for the tile labels.}

\item{ncol}{Facet columns for the grid (default: ggplot2 chooses).}

\item{trans}{Fill-scale transform, e.g. \code{"identity"} (default), \code{"log2"}, \code{"sqrt"}.}

\item{colors}{Optional colour ramp for the fill (default: the pairwise-sharing ramp).}

\item{limits}{Fill limits. \code{NULL} (default) lets each plot scale to its own values;
\code{"shared"} pins every feature to the range across all of them, which makes the pages
from \code{individual = TRUE} colour-comparable; or give \code{c(lo, hi)} explicitly (values
outside are squished). A faceted grid already shares one scale across its panels.}

\item{fill_scale}{Optional ggplot2 fill scale that fully overrides the above.}
}
\value{
A ggplot object (grid), or a named list of ggplot objects when \code{individual}.
}
\description{
Draws a group-by-group IBD-sharing triangle per feature: a \strong{gene} (per-SNP
pairwise IBD for SNPs falling strictly inside the gene interval, aggregated) or a
\strong{specific SNP} (the sharing at that single position). One facet per feature -- use
it to ask whether a gene or locus is itself shared between groups.
}
\details{
Genes come from the \code{genes} track on the \link{IbdResults} object and membership is
strict: a SNP belongs to a gene only when its position is inside the interval (no
flanking). Target individual loci with \code{snps} when a specific variant matters more
than a whole gene.

\strong{How a gene's sharing is measured.} When the object carries IBD blocks
(\code{ibd_results(blocks=, meta=)}) or a precomputed overlap table (\verb{gene_overlap=}, from
\verb{plasgenomicsutils ibd_gene_overlap}), a gene's cell is the fraction of pairs whose IBD
\strong{block overlaps the gene interval} -- so a pair counts when it shares a segment
spanning the gene even with no genotyped SNP inside it (see \code{\link[=gene_ibd_overlap]{gene_ibd_overlap()}}).
Without blocks it falls back to aggregating the pairwise IBD of SNPs strictly inside the
gene. \verb{snps=} always uses the per-SNP path (a single locus is a point, not an interval).
}
