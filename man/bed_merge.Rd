% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tandem_repeats.R
\name{bed_merge}
\alias{bed_merge}
\title{Merge overlapping or nearby intervals}
\usage{
bed_merge(x, gap = 0, chrom = "chr", start = "start", end = "end")
}
\arguments{
\item{x}{An interval table.}

\item{gap}{Intervals separated by at most this many bases are merged too (default \code{0}:
only overlapping or abutting ones).}

\item{chrom, start, end}{Column names in \code{x} (\code{"chrom"} is accepted as an alias of the
default \code{"chr"}).}
}
\value{
A tibble of merged blocks in chromosome / start order: \code{chrom} (as \code{x} spells
it, taken from the block's first interval), \code{chr} (normalised), \code{start}, \code{end},
\code{width}, and \code{n}, how many input intervals the block absorbed.
}
\description{
A dependency-free \verb{bedtools merge}: intervals on one chromosome that overlap, abut, or
lie within \code{gap} bases of each other become one. The complement of what \code{\link[=bed_subtract]{bed_subtract()}}
does to a mask internally, exposed for when the merged blocks are what you want to look
at or write out.
}
\section{Coordinates}{
 0-based half-open \verb{[start, end)}; abutting intervals
(\code{end1 == start2}) are merged, as \verb{bedtools merge} does by default. See
\link{plasgenomicsutilsR-coordinates}.
}

\examples{
iv <- data.frame(chr = "7", start = c(100, 150, 300, 320), end = c(200, 250, 310, 330))
bed_merge(iv)              # 100-250 and two singletons
bed_merge(iv, gap = 10)    # 300-330 as well
}
\seealso{
\code{\link[=merge_tandem_repeats]{merge_tandem_repeats()}}, which also summarises what was merged;
\code{\link[=bed_subtract]{bed_subtract()}}, \code{\link[=bed_intersect]{bed_intersect()}}
}
