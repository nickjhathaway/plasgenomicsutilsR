% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize.R
\name{plot_ibd_sharing_manhattan}
\alias{plot_ibd_sharing_manhattan}
\title{IBD Manhattan plot}
\usage{
plot_ibd_sharing_manhattan(
  x,
  groups = NULL,
  chroms = NULL,
  skip_chr = NULL,
  highlight_genes = NULL,
  label_genes = NULL,
  point_size = 0.5,
  point_alpha = 0.6,
  colours = NULL
)
}
\arguments{
\item{x}{An \link{IbdResults} object.}

\item{groups}{Optional character vector to keep (needs a \code{group} column).}

\item{chroms}{Optional chromosomes to keep (any spelling); others are dropped
and the remaining ones re-laid-out contiguously.}

\item{skip_chr}{Optional chromosomes to drop (complement of \code{chroms}).}

\item{highlight_genes}{Optional gene names (from the object's \code{genes} track) to
draw as reference lines; default all genes in the track. Requesting a name not in
the track is an error (rather than silently drawing nothing).}

\item{label_genes}{Label the genes with their names. \code{NULL} (default) labels them
only when \code{highlight_genes} is given; \code{TRUE}/\code{FALSE} forces it.}

\item{point_size, point_alpha}{Point aesthetics.}

\item{colours}{Optional length-2 colour vector for the alternating chromosome bands.}
}
\value{
A ggplot object.
}
\description{
Per-SNP fraction of pairs IBD along the genome. If the table carries a
\code{group} column the plot is faceted one row per group.
}
