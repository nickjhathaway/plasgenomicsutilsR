% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_snps.R
\name{annotate_snps}
\alias{annotate_snps}
\title{Which intervals each SNP falls in}
\usage{
annotate_snps(
  scan,
  intervals,
  within = 0,
  keep = c("all", "hits"),
  collapse = FALSE,
  prefix = "",
  one_based_snps = FALSE
)
}
\arguments{
\item{scan}{A data frame with \code{snp_id} (\code{"chr:pos0"}) or with \code{chr} and \code{pos} columns.}

\item{intervals}{A data frame with \code{name}, \code{chr} (or \code{chrom}), \code{start} and \code{end} --
\link{PF3D7_GENES}, \link{PF_EXAMPLE_DRUG_GENES}, \link{PF3D7_CORE_REGIONS} or your own BED.}

\item{within}{Widen every interval by this many bp on both sides before testing, for
catching a hit just outside a short gene (default \code{0}).}

\item{keep}{\code{"all"} (default) keeps SNPs matching nothing, with \code{NA} annotations;
\code{"hits"} keeps only SNPs inside an interval.}

\item{collapse}{Return one row per SNP, with multiple hits pasted into \code{name} and counted
in \code{n_intervals}, instead of one row per SNP x interval (default \code{FALSE}).}

\item{prefix}{Prepend this to the added column names, to keep two annotations side by
side (e.g. \code{prefix = "core_"}).}

\item{one_based_snps}{The positions in \code{scan} are 1-based (VCF \code{POS}), so shift them before
testing. Genotype-matrix column names from \code{\link[=load_genotypes]{load_genotypes()}} are 1-based, and joining
those against these 0-based intervals without saying so moves every SNP one base and can
turn a near-miss into a hit. Default \code{FALSE}, the package convention.}
}
\value{
\code{scan} with \code{name}, \code{interval_start}, \code{interval_end}, \code{distance_to_midpoint} and
any \code{gene_id} carried through -- plus \code{n_intervals} when \code{collapse = TRUE}. The
original columns and their order are preserved.
}
\description{
Takes any per-SNP table -- \code{\link[=run_ihs]{run_ihs()}}, \code{\link[=run_rsb]{run_rsb()}}, \code{\link[=run_xpehh]{run_xpehh()}}, \code{\link[=beta_score]{beta_score()}},
\code{pop_diff_snps()}, an \code{IbdResults} selection table, anything with a \code{snp_id} or a
\code{chr}/\code{pos} pair -- and reports the intervals covering each SNP. The usual use is "which
gene is this hit in", which otherwise means writing the overlap join by hand every time.
}
\details{
Positions are 0-based and intervals half-open \verb{[start, end)}, the package convention
(\code{?"plasgenomicsutilsR-coordinates"}), so a SNP at the interval's \code{end} is \emph{outside} it.
Chromosome names are normalised on both sides, so \code{Pf3D7_07_v3}, \code{chr7} and \code{7} match.

A SNP in no interval is kept with \code{NA} (or dropped by \code{keep = "hits"}), and a SNP in
several -- overlapping gene spans, or nested intervals -- yields one row per interval
rather than being silently collapsed. Use \code{collapse = TRUE} for one row per SNP with the
names pasted together instead.
}
\examples{
ihs <- data.frame(snp_id = c("Pf3D7_07_v3:403500", "Pf3D7_07_v3:1"),
                  ihs = c(4.2, 0.1))
annotate_snps(ihs, PF_EXAMPLE_DRUG_GENES)
annotate_snps(ihs, PF_EXAMPLE_DRUG_GENES, keep = "hits")
}
\seealso{
\code{\link[=bed_intersect]{bed_intersect()}} for interval-to-interval overlap; \code{\link[=selection_peaks]{selection_peaks()}} to merge
neighbouring significant SNPs into loci before annotating.
}
