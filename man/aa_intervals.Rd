% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aa_intervals.R
\name{aa_intervals}
\alias{aa_intervals}
\title{Genomic interval of an amino-acid position}
\usage{
aa_intervals(positions, gff, genes = PF3D7_GENES, one_based_output = FALSE)
}
\arguments{
\item{positions}{A data frame with \code{transcript_id} and \code{aa_position}. \code{aa_position} is
\strong{1-based}, counting the initiator methionine as 1, matching how residues are numbered in
the literature -- so \code{76} is the residue everyone calls 76. \code{transcript_id} may
be a transcript id (\code{"PF3D7_0709000.1"}), a gene id (\code{"PF3D7_0709000"} -- every
transcript of it is returned), or a gene symbol when \code{genes} is given (\code{"pfcrt"}).}

\item{gff}{A GFF path, or the result of \code{\link[=read_gff_cds]{read_gff_cds()}} (parse once, reuse).}

\item{genes}{Optional gene table with \code{name} and \code{gene_id} columns, so \code{transcript_id} can
be a symbol; defaults to \link{PF3D7_GENES}.}

\item{one_based_output}{Return 1-based inclusive coordinates instead of the package's
0-based half-open convention. \code{FALSE} (default) keeps \code{start} 0-based so the output can
be used as an interval table directly.}
}
\value{
A tibble with \code{chr} (normalised), \code{chrom} (as the GFF spells it), \code{start},
\code{end}, \code{name}, \code{transcript_id}, \code{gene_id}, \code{aa_position}, \code{strand}, \code{codon_positions}
(a comma-separated list of the three base positions, always 1-based as coordinates are
usually quoted), \code{n_exons} and \code{spans_intron}. Positions past the end of a protein, and
ids that match nothing, are dropped with a warning naming them.
}
\description{
Turns "codon 76 of \emph{pfcrt}" into a genomic interval, by walking the protein back through
the transcript's coding exons. Amino-acid positions are how resistance markers are named
and reported, while every plot and interval tool here works in genomic coordinates, and the
conversion is not something you can do by eye: it depends on the exon structure, the strand
and the CDS phase.
}
\details{
The result is shaped like the package's other interval tables -- \code{chr}, \code{start}, \code{end},
\code{name} -- so it drops straight into \verb{genes =}, \verb{mark_snps =}, \code{\link[=annotate_snps]{annotate_snps()}} or
\code{\link[=bed_intersect]{bed_intersect()}}. \code{name} is \verb{<transcript_id>-AA<aa_position>}. Those genomic bounds are
0-based half-open like every other interval here, while the amino-acid position itself is
1-based and \code{codon_positions} lists 1-based bases -- the deliberate exception described in
\code{?"plasgenomicsutilsR-coordinates"}.

A codon can straddle an intron, in which case its three bases are not contiguous: \code{start}
and \code{end} then span the intron as well, and \code{spans_intron} flags it so the width is not
mistaken for three bases. \code{codon_positions} always lists the three base positions
themselves.
}
\examples{
cds <- read_gff_cds(system.file("extdata", "pf3d7_drug_gene_cds.gff",
                                package = "plasgenomicsutilsR"))

# K76T and C580Y, at the positions they are reported at
aa <- data.frame(transcript_id = c("pfcrt", "pfkelch13"), aa_position = c(76, 580))
aa_intervals(aa, cds, one_based_output = TRUE)[, c("name", "chr", "start", "end", "strand")]

# pfcrt has 13 coding exons, so four of its codons straddle an intron
every_codon <- aa_intervals(data.frame(transcript_id = "pfcrt", aa_position = 1:424), cds)
subset(every_codon, spans_intron)[, c("name", "codon_positions")]
}
\seealso{
\code{\link[=read_gff_cds]{read_gff_cds()}}, \code{\link[=annotate_snps]{annotate_snps()}}
}
