% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pair_fraction_summary.R
\name{pair_fraction_summary}
\alias{pair_fraction_summary}
\title{Summarise genome-wide IBD sharing between metadata groups}
\usage{
pair_fraction_summary(
  x,
  group = NULL,
  meta = NULL,
  value = .PAIR_FRACTION_COL,
  probs = c(0.25, 0.75),
  within = TRUE,
  min_pairs = 1L
)
}
\arguments{
\item{x}{An \link{IbdResults} holding a pair table (\code{ibd_results(pair_fraction = )} or
\verb{$set_pair_fraction()}), or the pair table itself as a data frame or path.}

\item{group}{Metadata column defining the groups. Defaults to the object's declared
group column, then to the first non-\code{sample} column of \code{meta}.}

\item{meta}{Sample metadata (\code{sample} plus \code{group}). Taken from \code{x} when it is an
\link{IbdResults}.}

\item{value}{Column holding the sharing measure (default \code{"ibd_fraction_accessible"},
what \verb{plasgenomicsutils ibd_fraction_and_snp_density} writes). Pass
\code{"ibd_fraction_full_genome"} to summarise against the full genome instead.}

\item{probs}{Quantiles to report, as proportions (default the quartiles,
\code{c(0.25, 0.75)}). Each becomes a \verb{q<pct>} column.}

\item{within}{Include each group's row against itself (default \code{TRUE}).}

\item{min_pairs}{Groups contributing fewer than this many pairs are dropped, with a
note (default \code{1}, so only empty combinations go).}
}
\value{
A tibble, one row per group pair, ordered by the object's group order (or the
column's factor levels):
\describe{
\item{\code{group_a}, \code{group_b}}{the two groups, \code{group_a == group_b} on a within-group
row.}
\item{\code{n_samples_a}, \code{n_samples_b}}{samples of each group present in the pair
table.}
\item{\code{n_pairs}}{sample pairs the summary is over. On a complete table this is
\code{n_samples_a * n_samples_b} between groups and \code{choose(n_samples_a, 2)} within
one; short of that, the table is missing pairs.}
\item{\code{mean}, \code{median}, \code{sd}, \code{min}, \code{max}}{over those pairs.}
\item{\code{q25}, \code{q75}}{the requested quantiles (type 7, \code{stats::quantile()}'s
default).}
}
}
\description{
Reduces the per-pair IBD fraction table to one row per pair of \code{group} levels, over
every sample pair that spans them: two groups' row covers all \verb{n_a x n_b} cross-group
pairs, and a group's row against itself covers its \code{choose(n, 2)} within-group pairs.
Within-group rows are the ones that say whether a group is internally related at all,
so they are included by default and the comparison to the between-group rows is the
point.
}
\details{
The summary is over \emph{pairs}, not samples, so a highly related cluster inside one group
pulls its mean up through every pair it takes part in. The median and the quartiles are
there because relatedness is skewed: most pairs share almost nothing and a few share a
great deal, so a mean on its own reads as a much more related cohort than any pair
actually is.
}
\examples{
pairs <- data.frame(
  sample1 = c("s1", "s1", "s1", "s2", "s2", "s3"),
  sample2 = c("s2", "s3", "s4", "s3", "s4", "s4"),
  ibd_fraction_accessible = c(0.02, 0.31, 0.04, 0.28, 0.03, 0.05))
meta <- data.frame(sample = paste0("s", 1:4),
                   region = c("north", "north", "south", "south"))
pair_fraction_summary(pairs, group = "region", meta = meta)
}
\seealso{
\code{\link[=plot_ibd_pair_network]{plot_ibd_pair_network()}}, which draws the same table pair by pair.
}
