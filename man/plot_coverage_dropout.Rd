% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage.R
\name{plot_coverage_dropout}
\alias{plot_coverage_dropout}
\title{Coverage dropouts along the genome}
\usage{
plot_coverage_dropout(
  windows,
  min_depth = 5,
  min_frac_samples = 0.9,
  genes = NULL,
  highlight_genes = NULL,
  label_genes = NULL,
  chroms = NULL,
  skip_chr = NULL,
  reference = DEFAULT_REFERENCE
)
}
\arguments{
\item{windows}{Per-window table from \code{coverage_depth_stats --windows-output}, or the
already-merged regions from \code{coverage_dropout_regions}.}

\item{min_depth}{A sample counts as uncovered in a window below this mean depth
(ignored when \code{windows} is already merged).}

\item{min_frac_samples}{Draw the flag line at this fraction.}

\item{genes, highlight_genes, label_genes}{Optional gene track, as in \code{\link[=plot_ihs]{plot_ihs()}}.}

\item{chroms, skip_chr, reference}{As in \code{\link[=plot_ihs]{plot_ihs()}}.}
}
\value{
A ggplot object.
}
\description{
The fraction of samples below depth in each window, across the genome, with the merged
dropout regions marked. Regions where the line sits near 1 are amplified in almost
nobody -- they will read as invariant rather than as missing unless they are excluded.
}
