% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ibd_pair_clusters.R
\name{ibd_pair_links}
\alias{ibd_pair_links}
\title{The IBD pairs a network draws}
\usage{
ibd_pair_links(
  pairs,
  weight = NULL,
  min_ibd = 0.01,
  samples = NULL,
  add_meta_cols = NULL,
  meta = NULL
)
}
\arguments{
\item{pairs}{The per-pair table from \verb{plasgenomicsutils ibd_fraction_and_snp_density}
(\verb{*.pair_ibd_fraction.tsv.gz}): a path, a data frame, or an \link{IbdResults} carrying one.}

\item{weight}{Column holding the fraction. Defaults to \code{ibd_fraction_accessible}, the
callable-genome denominator; \code{ibd_fraction_full_genome} divides by the whole genome
instead, so it reads lower for the same pair.}

\item{min_ibd}{Count a pair as linked only above this fraction (default \code{0.01}). Pass what
you plotted with, or the clusters will not match the components.}

\item{samples}{Optional subset of samples to keep.}

\item{add_meta_cols}{Metadata columns to attach to both endpoints. Each \code{col} becomes
\code{sample1_col} and \code{sample2_col}, in the order asked for. A sample missing from \code{meta}
gets \code{NA}; factor columns keep their level order, so a region stays in map order rather
than turning alphabetical.}

\item{meta}{Sample metadata for \code{add_meta_cols}; taken from the \link{IbdResults} when \code{pairs}
is one, so it only needs giving alongside a plain table.}
}
\value{
A tibble of \code{sample1}, \code{sample2} and \code{ibd_fraction} (the \code{weight} column's value),
highest sharing first, plus a pair of columns for each of \code{add_meta_cols}.
}
\description{
The edge list behind \code{\link[=plot_ibd_pair_network]{plot_ibd_pair_network()}}: one row per sample pair sharing more than
\code{min_ibd} of the genome. \code{\link[=ibd_pair_clusters]{ibd_pair_clusters()}} answers which samples those edges connect;
this is the sharing itself.
}
\details{
\code{add_meta_cols} carries metadata onto both ends of each edge, which is what turns the list
into something to summarise over -- whether a pair is within a site or between two, say:

\if{html}{\out{<div class="sourceCode">}}\preformatted{e <- ibd_all$ibd_pair_links(min_ibd = 0.03, add_meta_cols = "region")
table(within_region = e$sample1_region == e$sample2_region)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
ibd_pair_links(ibd_all, min_ibd = 0.03)

# the same edges, labelled with where each end came from
ibd_all$ibd_pair_links(min_ibd = 0.03, add_meta_cols = c("region", "country"))
}
}
\seealso{
\code{\link[=ibd_pair_clusters]{ibd_pair_clusters()}}, \code{\link[=plot_ibd_pair_network]{plot_ibd_pair_network()}},
\code{\link[=pair_fraction_summary]{pair_fraction_summary()}} for the sharing already summarised by group.
}
