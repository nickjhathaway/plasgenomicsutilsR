% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_scans.R
\name{plot_ihs}
\alias{plot_ihs}
\title{Manhattan plot of a haplotype-homozygosity scan}
\usage{
plot_ihs(
  scan,
  metric = c("neg_log10_p", "ihs", "value"),
  threshold = NULL,
  genes = NULL,
  highlight_genes = NULL,
  label_genes = NULL,
  chroms = NULL,
  skip_chr = NULL,
  reference = DEFAULT_REFERENCE,
  point_size = 0.6,
  point_alpha = 0.7,
  colours = NULL
)
}
\arguments{
\item{scan}{The tibble from \code{\link[=run_ihs]{run_ihs()}}, \code{\link[=run_rsb]{run_rsb()}} or \code{\link[=run_xpehh]{run_xpehh()}}.}

\item{metric}{\code{"neg_log10_p"} (default) or the statistic itself (\code{"ihs"} / \code{"value"}).}

\item{threshold}{Draw a dashed significance line at this height. \code{NULL} uses the
\code{-log10(p)} of a 1\% two-sided tail when plotting \code{neg_log10_p}, the convention these
scans are usually read at; \code{NA} draws none.}

\item{genes}{Gene table to mark (e.g. \link{PF_EXAMPLE_DRUG_GENES}); \code{NULL} for none.}

\item{highlight_genes, label_genes}{Which genes to mark and whether to name them.}

\item{chroms, skip_chr}{Chromosomes to keep or drop.}

\item{reference}{Reference id for the chromosome layout.}

\item{point_size, point_alpha, colours}{Point and band aesthetics.}
}
\value{
A ggplot object.
}
\description{
Draws \code{neg_log10_p} (or the raw statistic) along the genome, one panel per group or
population pair, over the chromosome bands and optional gene markers used by the other
genome-wide plots in the package.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
hap <- parasite_haplotypes(ps, maf = 0.05)
plot_ihs(run_ihs(hap, group = "country"), genes = PF_EXAMPLE_DRUG_GENES)
}
