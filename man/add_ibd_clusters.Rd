% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_ibd_clusters.R
\name{add_ibd_clusters}
\alias{add_ibd_clusters}
\title{Add IBD cluster ids to the stored metadata}
\usage{
add_ibd_clusters(
  x,
  genes = NULL,
  locus = NULL,
  within = 0,
  sharing = c("overlap", "complete"),
  size = FALSE,
  prefix = ""
)
}
\arguments{
\item{x}{An \link{IbdResults} with \code{blocks} and \code{meta} loaded.}

\item{genes}{Gene names in the object's track, or \code{NULL} for every gene in it. Ignored
when \code{locus} is given.}

\item{locus}{A locus instead of a gene: \code{"chr:pos"}, \code{"chr:start-end"}, or a data frame
with \code{chr}/\code{start}/\code{end}. Its \code{name} (or the coordinate string) labels the column.}

\item{within}{Pad the interval by this many bp on both sides before deciding whether a
block overlaps it (default \code{0}).}

\item{sharing}{\code{"overlap"} (default) counts a pair when any IBD block touches the
interval; \code{"complete"} only when a block spans the whole of it.}

\item{size}{Also add \verb{<label>_cluster_size}, the number of samples in the cluster
(default \code{FALSE}).}

\item{prefix}{Optional string put before each column name, for keeping several settings
side by side (e.g. \code{prefix = "complete_"}).
Re-running replaces any column of the same name, so changing \code{within} or \code{sharing} and
calling again re-clusters rather than accumulating stale columns. Use \code{prefix} when you
want two settings side by side.}
}
\value{
Invisibly \code{x}, with the metadata extended. \code{x$get_meta()} shows the new columns.
}
\description{
For each gene or locus, works out the single-linkage clusters of samples sharing IBD over
it and writes them into the object's metadata as \verb{<label>_cluster_id} (and
\verb{<label>_cluster_size} when \code{size = TRUE}). Any plot that reads a metadata column can then
use them -- notably \code{\link[=plot_ibd_network]{plot_ibd_network()}}, where the clusters \emph{are} the connected components
being drawn:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{ibd$add_ibd_clusters(genes = "pfcrt")
plot_ibd_network(ibd, gene = "pfcrt", color_group = "pfcrt_cluster_id")
}\if{html}{\out{</div>}}

Single linkage means a sample joins a cluster if it shares with \strong{any} member, so a chain
of pairs is one cluster even where its ends never share directly. Ids run largest cluster
first and are numbered per interval, so cluster 1 at one gene is unrelated to cluster 1 at
another. A sample that shares with nobody over the interval gets \code{NA}.

\code{within} and \code{sharing} mean exactly what they do in \code{\link[=plot_ibd_network]{plot_ibd_network()}} and
\code{\link[=gene_ibd_pairs]{gene_ibd_pairs()}}, and they decide which edges exist -- so pass the same values you plot
with, or the colours will not match the components.
}
\examples{
\dontrun{
ibd <- ibd_results(blocks = "blocks.hmm.txt", meta = meta,
                   genes = PF_EXAMPLE_DRUG_GENES)
add_ibd_clusters(ibd)                       # one column per gene in the track
plot_ibd_network(ibd, gene = "pfcrt", color_group = "pfcrt_cluster_id")
}
}
