% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jost_d.R
\name{pop_diff_table}
\alias{pop_diff_table}
\title{Group-pair differentiation summary across statistics}
\usage{
pop_diff_table(
  x,
  group = NULL,
  statistics = c("jost_d", "gst", "gst_hedrick", "fst"),
  stats = c("mean", "top_mean", "max"),
  top = 0.05,
  meta = NULL,
  clamp = TRUE
)
}
\arguments{
\item{x}{A \link{PopStructure} (uses its full genotype matrix for the active samples) or a
genotype matrix (samples x SNPs, 0/1/2, \code{NA}).}

\item{group}{Metadata column (for a \code{PopStructure}) or a per-sample grouping vector
(for a matrix).}

\item{statistics}{Measures to include (any of \code{"jost_d"}, \code{"gst"}, \code{"gst_hedrick"},
\code{"fst"}; default all four).}

\item{stats}{Per-pair summaries to include (\code{"mean"}, \code{"median"}, \code{"top_mean"},
\code{"max"}; default mean + top_mean + max).}

\item{top}{Fraction of top SNPs for \code{"top_mean"} (default \code{0.05}).}

\item{meta}{When \code{x} is a matrix, an optional data frame with a \code{sample} column plus
\code{group}; otherwise \code{group} is a vector aligned to the matrix rows.}

\item{clamp}{Clamp small negative estimates to 0 (default \code{TRUE}).}
}
\value{
A data frame: \code{a}, \code{b}, \code{n_snps}, then one \code{statistic_stat} column each.
}
\description{
One tidy table of every pairwise comparison with, for each statistic and summary, its
value -- so Jost's D, Nei's Gst, Hedrick's G'st and Hudson's Fst can be read together.
}
\examples{
\dontrun{
ps <- example_pop_structure("africa")
pop_diff_table(ps, group = "site")
}
}
