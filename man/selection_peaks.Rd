% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selection_peaks.R
\name{selection_peaks}
\alias{selection_peaks}
\title{Merge a selection scan's significant SNPs into peaks}
\usage{
selection_peaks(
  x,
  criterion = c("bonferroni", "fdr", "permutation", "empirical", "top", "value"),
  metric = NULL,
  cutoff = NULL,
  top = 0.01,
  gap = PEAK_GAP_BP,
  min_snps = 1L,
  pad = 0,
  genes = NULL,
  thresholds = NULL
)
}
\arguments{
\item{x}{An \link{IbdResults}, or a scan data frame with \code{chr}, \code{pos} and a value column.}

\item{criterion}{How a SNP qualifies:
\itemize{
\item \code{"bonferroni"} (default) / \code{"fdr"} — the scan's own \code{significant} /
\code{significant_fdr} flag, or the matching stored threshold line.
\item \code{"permutation"} — the \code{significant_perm} flag from
\code{ibd_selection_statistic --permute}: family-wise control against a null drawn from
the data rather than a chi-square(1). Prefer it when \code{lambda_gc} is far from 1.
\item \code{"empirical"} — the \code{significant_fdr_perm} flag from the same run: Benjamini-Hochberg
over the permutation's own per-SNP p-values. The FDR counterpart of \code{"permutation"},
and the only FDR here resting on calibrated p-values.
\item \code{"top"} — the top \code{top} fraction within each group, the convention for scans whose
null is not trustworthy.
\item \code{"value"} — at or above \code{cutoff}.
}}

\item{metric}{Column to rank by; guessed from the scan when \code{NULL}
(\code{neg_log10_p}, then \code{beta}, \code{value}, \code{ihs}).}

\item{cutoff}{Value cutoff for \code{criterion = "value"}.}

\item{top}{Tail fraction for \code{criterion = "top"} (default 0.01).}

\item{gap}{Merge hits separated by at most this many bp (default 20000).}

\item{min_snps}{Drop peaks supported by fewer than this many significant SNPs. Raising
it to 2 or 3 is the cheapest way to suppress isolated single-SNP hits.}

\item{pad}{Widen each peak by this many bp on both sides before reporting and before
matching genes.}

\item{genes}{Optional gene table (\code{name}, \code{chr} or \code{chrom}, \code{start}, \code{end}) to annotate
each peak with the genes it overlaps; \link{PF3D7_GENES} is a sensible argument.}

\item{thresholds}{Threshold table, when \code{x} is a bare data frame carrying no flag column.}
}
\value{
A tibble with one row per peak: the grouping column, \code{chr}, \code{start}, \code{end},
\code{width}, \code{n_snps} (significant SNPs in the peak), \code{peak_pos} (the single
highest-scoring SNP), \code{peak_value}, \code{mean_value}, and -- when a gene table was given
-- five annotation columns. Sorted by \code{peak_value}.

Everything is anchored on \code{peak_pos}, not the interval's midpoint: the midpoint is an
artefact of where merging started and stopped, and can land in a gap between genes.
\itemize{
\item \code{peak_interval_genes} — a \strong{list-column} of every gene the interval spans, in
genomic order. A peak of a few hundred kb, as real IBD sharing regions are, can
cross dozens, so this is the set to take into a follow-up rather than something to
read off the screen. Unnest it for one row per peak x gene:
\code{tidyr::unnest(pk, peak_interval_genes)}; \code{lengths()} of it is \code{n_genes}.
\item \code{gene_at_peak} — the gene(s) whose span \strong{contains} the peak SNP, comma-separated.
Usually one, since gene spans rarely overlap, and \strong{empty when the peak SNP is
intergenic} — about a third of peaks on a real cohort.
\item \code{nearest_gene}, \code{distance_to_gene} — of the genes the peak \strong{covers}, the closest
one to the peak SNP and the gap to it in bp, \code{0} when the SNP is inside it. This is
what to read when \code{gene_at_peak} is empty; those intergenic peaks are typically
within a kb or two of a gene in the same peak. Candidates are restricted to the
interval, so a peak that covers no gene reports none rather than pointing at
something far outside it — which matters when \code{genes} is a short list, where most
peaks cover nothing from it.
\item \code{n_genes} — how many genes the interval spans, i.e. \code{lengths(peak_interval_genes)}.
}

They agree by construction: \code{n_genes == 0} implies an empty \code{peak_interval_genes} and
empty name columns, \code{n_genes == lengths(peak_interval_genes)}, and a non-empty
\code{gene_at_peak} implies \code{distance_to_gene == 0}.
}
\description{
Turns a per-SNP scan into one row per implicated locus. A sweep leaves a run of
neighbouring significant SNPs, so the SNP count answers "how dense is my panel here"
while the peak count answers "how many things did I find" -- and only the second belongs
in a result.
}
\details{
Works on any of the per-SNP scans in the package: an \link{IbdResults} (its selection table),
or the tibble from \code{\link[=run_ihs]{run_ihs()}}, \code{\link[=run_rsb]{run_rsb()}}, \code{\link[=run_xpehh]{run_xpehh()}} or \code{\link[=beta_score]{beta_score()}}.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
b <- beta_score(ps, group = "country", window = 300000, min_window_snps = 1)
selection_peaks(b, criterion = "top", top = 0.2, genes = PF_EXAMPLE_DRUG_GENES)
}
\seealso{
\code{\link[=ihs_genes]{ihs_genes()}} and \code{\link[=beta_genes]{beta_genes()}} for the per-gene view; this is the per-locus
one, which does not need a gene to exist where the signal is.
}
