% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_ibd_overlap.R
\name{gene_ibd_overlap}
\alias{gene_ibd_overlap}
\title{Per-gene IBD-block overlap between groups}
\usage{
gene_ibd_overlap(x, genes = NULL, group = NULL, within = 0)
}
\arguments{
\item{x}{An \link{IbdResults} with IBD \code{blocks} and \code{meta}.}

\item{genes}{Gene names from the object's track (default all), or a gene-interval data
frame (\code{name}, \code{chr}/\code{chrom}, \code{start}, \code{end}) to use instead of the track.}

\item{group}{Metadata column defining the groups (default: the first non-\code{sample}
column of \code{meta}).}

\item{within}{Pad each gene interval by this many bp on both sides (default \code{0}).}
}
\value{
A tibble, one row per gene x group-pair: \code{gene} (a unique display label -- the
\code{name}, disambiguated as \code{name (gene_id)} where GFF Names repeat across gene
families), \code{name}, \code{gene_id}, \code{chr}, \code{start}, \code{end}, \code{group_a}, \code{group_b},
\code{n_pairs_ibd}, \code{n_pairs_total}, \code{frac_pairs_ibd}.
}
\description{
For each gene and each pair of groups, the fraction of sample pairs that share an IBD
\strong{block overlapping the gene} -- a pair counts when any of its IBD segments overlaps the
(optionally padded) gene interval, so a segment spanning the gene counts even with no
genotyped SNP inside it. The denominator is \emph{all} pairs compared in that group-pair (from
the analyzed-sample set), so pairs that are never IBD still count against the fraction.
}
\details{
Needs an \link{IbdResults} built with \verb{blocks =} and \verb{meta =} (or use the Python
\code{ibd_gene_overlap} tool and pass its table as \verb{gene_overlap =}).
}
