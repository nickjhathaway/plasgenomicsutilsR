% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervals.R
\name{bed_intersect}
\alias{bed_intersect}
\title{Intersect two sets of genomic intervals}
\usage{
bed_intersect(
  locs1,
  locs2,
  chrom1 = "chr",
  start1 = "start",
  end1 = "end",
  chrom2 = "chr",
  start2 = "start",
  end2 = "end"
)
}
\arguments{
\item{locs1, locs2}{Interval tables (data frames / tibbles).}

\item{chrom1, start1, end1}{Column names for the chromosome / start / end in \code{locs1}
(defaults \code{"chr"}, \code{"start"}, \code{"end"}; \code{"chrom"} is accepted as a chromosome alias).}

\item{chrom2, start2, end2}{As above for \code{locs2}.}
}
\value{
A list of three tibbles:
\describe{
\item{\code{overlap}}{one row per overlapping \code{locs1}x\code{locs2} pair -- every \code{locs1}
column, then every \code{locs2} column suffixed \code{.2}, then \code{overlap_start},
\code{overlap_end}, \code{overlap_bp}.}
\item{\code{only1}}{\code{locs1} rows overlapping nothing in \code{locs2}.}
\item{\code{only2}}{\code{locs2} rows overlapping nothing in \code{locs1}.}
}
}
\description{
A lightweight, dependency-free "bedtools intersect"-style overlap between two interval
tables (e.g. genes vs. core regions, SNPs vs. paralog masks). Two intervals overlap when
they are on the same chromosome and share at least one base
(\code{start1 < end2 & end1 > start2}); chromosome names are matched via \code{\link[=normalise_chr]{normalise_chr()}}
so \code{"Pf3D7_07_v3"} and \code{"7"} agree.
}
\section{Coordinates}{
 Intervals are \strong{0-based half-open} \verb{[start, end)}, as in BED and
throughout this package -- so abutting intervals (\code{end1 == start2}) do not overlap.
See \link{plasgenomicsutilsR-coordinates}.
}

\examples{
genes <- data.frame(name = c("a", "b"), chr = c("7", "7"),
                    start = c(100, 5000), end = c(200, 5200))
core  <- data.frame(chr = "7", start = 50, end = 1000)
hit <- bed_intersect(genes, core)
hit$overlap$name   # "a"  (inside core)
hit$only1$name     # "b"  (subtelomeric)
}
