% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_scans.R
\name{plot_diversity}
\alias{plot_diversity}
\title{Windowed diversity along the genome}
\usage{
plot_diversity(
  div,
  metric = "pi",
  genes = NULL,
  highlight_genes = NULL,
  label_genes = NULL,
  chroms = NULL,
  skip_chr = NULL,
  reference = DEFAULT_REFERENCE,
  point_size = 0.6,
  point_alpha = 0.7,
  colours = NULL
)
}
\arguments{
\item{div}{A \code{\link[=pop_diversity]{pop_diversity()}} result computed with \code{by = "window"}.}

\item{metric}{Which column to draw: \code{"pi"} (default), \code{"he"}, \code{"theta_w"},
\code{"tajima_d"}, \code{"hap_div"}, \code{"seg_sites"}, ...}

\item{genes}{Gene table to mark (e.g. \link{PF_EXAMPLE_DRUG_GENES}); \code{NULL} for none.}

\item{highlight_genes, label_genes}{Which genes to mark and whether to name them.}

\item{chroms, skip_chr}{Chromosomes to keep or drop.}

\item{reference}{Reference id for the chromosome layout.}

\item{point_size, point_alpha, colours}{Point and band aesthetics.}
}
\value{
A ggplot object.
}
\description{
One panel per group, with the statistic plotted at the window mid-points.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
d <- pop_diversity(ps, group = "country", by = "window", window = 500000)
plot_diversity(d, metric = "he")
}
