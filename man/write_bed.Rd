% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bed_subtract.R
\name{write_bed}
\alias{write_bed}
\title{Write an interval table as a BED file}
\usage{
write_bed(
  x,
  file,
  name = NULL,
  sort = TRUE,
  chrom = NULL,
  name_is_coords = FALSE,
  make_names_unique = FALSE
)
}
\arguments{
\item{x}{An interval table (\code{chrom}/\code{chr}, \code{start}, \code{end}), e.g. from \code{\link[=bed_subtract]{bed_subtract()}}.}

\item{file}{Path to write.}

\item{name}{Column to use as the BED name field, or \code{NULL} for none. Defaults to \code{"name"}
when the table has it.}

\item{sort}{Sort by chromosome and start (default \code{TRUE}), which is what the tools want.
Numbering for uniqueness follows the written order.}

\item{chrom}{Column holding the chromosome name to write. Defaults to the first of a
\verb{<assembly>_chrom} column, \code{"chrom"}, and \code{"chr"} that the table has (see above).}

\item{name_is_coords}{Write \code{chrom-start-end} (\code{Pf3D7_07_v3-403221-403363}) as the name
field instead of a column (default \code{FALSE}).}

\item{make_names_unique}{Make the name field unique within the file (default \code{FALSE}):
a name that occurs more than once gets \verb{_1}, \verb{_2}, ... in file order, zero-padded to the
width of that name's count (\verb{_01} to \verb{_12} for twelve). Combines with \code{name_is_coords},
so identical coordinates written twice get distinct names. With no name column and
\code{name_is_coords = FALSE}, the coordinates are used as the names to make unique.}
}
\value{
\code{file}, invisibly.
}
\description{
Three columns, tab separated, no header, \code{start} 0-based half-open -- what \code{bedtools}
and \verb{bcftools mpileup -R} expect. A fourth \code{name} column is written when the table has
one, since a BED that says what each interval is survives being looked at later.
}
\details{
\strong{The reference's own spelling is preferred.} A BED is read by other tools against a
real reference, so it has to carry the name the FASTA and the BAMs use -- writing a
normalised one produces a file that matches nothing, silently. Tables in this package
carry two kinds of chromosome column, and the default picks the one with the full name:
a \verb{<assembly>_chrom} column first (\code{Pf3D7_chrom} in \link{PF3D7_GENES} and the other bundled
datasets, where \code{chrom} is the short \code{"7"}), then \code{chrom} (which \code{\link[=aa_intervals]{aa_intervals()}},
\code{\link[=tandem_repeats]{tandem_repeats()}} and friends fill with the source spelling, keeping \code{chr} for the
normalised \code{"7"}), then \code{chr}. Pieces from \code{\link[=bed_subtract]{bed_subtract()}} keep their \code{locs1} columns,
so a gene table cut by a mask still writes the right names.
}
\examples{
iv <- data.frame(chr = c("Pf3D7_07_v3", "Pf3D7_07_v3"), start = c(403623, 403700),
                 end = c(403626, 403703), name = c("pfcrt-76", "pfcrt-102"))
write_bed(iv, file.path(tempdir(), "targets.bed"))
}
\seealso{
\code{\link[=bed_subtract]{bed_subtract()}}
}
