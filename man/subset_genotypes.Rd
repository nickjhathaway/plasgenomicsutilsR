% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset_genotypes.R
\name{subset_genotypes}
\alias{subset_genotypes}
\title{Restrict a genotype panel to a set of samples}
\usage{
subset_genotypes(x, samples, strict = FALSE)
}
\arguments{
\item{x}{A \code{\link[=load_genotypes]{load_genotypes()}} list, a genotype matrix (samples x SNPs), or a
\link{PopStructure} (which is narrowed with its own \verb{$subset()}).}

\item{samples}{The samples to keep: a character vector of ids, a
\code{\link[=parasite_haplotypes]{parasite_haplotypes()}} object (its kept samples), a \code{PopStructure}, another
\code{load_genotypes()} list, or a metadata data frame with a \code{sample} column.}

\item{strict}{Error when \code{samples} names ids the panel does not hold (default \code{FALSE},
which warns and keeps the intersection).}
}
\value{
The same kind of object as \code{x}, holding only those samples, in the panel's own
order.
}
\description{
Narrows the rows and leaves everything else alone, returning the same kind of object it
was given -- a \code{\link[=load_genotypes]{load_genotypes()}} list comes back a list, with \code{sample.id} narrowed to
match and \code{snp.id} / \code{allele} / \code{pruned} / \code{positions} / \code{variants} carried through.
}
\details{
The use it was written for is running a diversity or differentiation analysis on the
monoclonals a haplotype set kept:

\if{html}{\out{<div class="sourceCode">}}\preformatted{mono <- parasite_haplotypes(geno$genotype, fws = fws, min_fws = 0.92)
pop_diversity(subset_genotypes(geno, mono)$genotype, group = "region", meta = meta)
}\if{html}{\out{</div>}}

Note it subsets \strong{samples}, not SNPs: the full SNP panel is kept, which is what a
diversity estimate wants. Handing \code{hap$hap} to such an analysis instead would silently
use the MAF-filtered, imputed 0/1 matrix the scans need, which is a different panel.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
hap <- parasite_haplotypes(ps, maf = 0.05)

# a bare matrix
dim(subset_genotypes(ps$genotype(), hap))

# a load_genotypes()-shaped list keeps its other slots
geno <- list(genotype = ps$genotype(), sample.id = ps$get_samples(),
             snp.id = colnames(ps$genotype()), pruned = TRUE, positions = "0-based")
str(subset_genotypes(geno, hap)[c("sample.id", "pruned", "positions")], max.level = 1)
}
\seealso{
\code{\link[=haplotype_samples]{haplotype_samples()}}, \link{PopStructure}'s \verb{$subset()}.
}
