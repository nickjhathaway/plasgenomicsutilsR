% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ibd_block_extension_scan.R
\name{ibd_block_extension_scan}
\alias{ibd_block_extension_scan}
\title{Genome-wide scan of IBD block extension, and loci placed against it}
\usage{
ibd_block_extension_scan(
  x,
  loci = NULL,
  regions = NULL,
  width = 500,
  step = 2000,
  group = NULL,
  within = 0,
  sharing = c("overlap", "complete"),
  min_ref_blocks = 1L,
  min_pairs = 5L,
  min_windows = 50L,
  meta = NULL
)
}
\arguments{
\item{x}{An \link{IbdResults} built with \verb{blocks =} and \verb{meta =}.}

\item{loci}{Optional loci to place against the scan: gene names from the object's track,
or an interval data frame as \code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}} takes. Each comes back with
its \code{percentile} in its group's window distribution. \code{NULL} (default) returns the
windows alone.}

\item{regions}{Regions to tile (\code{chr}/\code{chrom}, \code{start}, \code{end}). Defaults to
\link{PF3D7_CORE_REGIONS} for a \code{pf3d7} object, which keeps the hypervariable subtelomeres
out of the background; pass your own for any other reference.}

\item{width}{Window width in bp (default \code{500}).}

\item{step}{Distance between window starts in bp (default \code{2000}). See the resolution
note: finer buys nothing.}

\item{group, within, sharing, min_ref_blocks, min_pairs, meta}{Passed to
\code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}}.}

\item{min_windows}{Groups with fewer than this many tested windows get \code{NA} percentiles
and a warning, since a distribution over a handful of windows is not one (default \code{50}).}
}
\value{
A tibble with \code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}}'s columns plus:
\describe{
\item{\code{source}}{\code{"window"} for the background rows, \code{"locus"} for anything named in
\code{loci}.}
\item{\code{percentile}}{where this row's \code{paired_ratio} falls among the \strong{windows} of the
same group, as a percentage at or below it. The number to quote.}
\item{\code{n_windows}}{tested windows behind that group's distribution.}
}
Loci come first, then windows by descending \code{paired_ratio}. The per-pair ratios are not
retained; call \code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}} directly on a locus when you want them.
}
\description{
Runs \code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}} over fixed-width windows tiling the genome, then reports
where each window -- and each locus you name -- sits in its own group's distribution. This
is the interface to reach for first: a single locus's \code{paired_ratio} has no interpretable
scale on its own, because the statistic is inflated at \emph{every} locus by length-biased
sampling, and the scan is what measures that inflation for the cohort in hand.
}
\details{
Windows rather than genes as the background, for two reasons. They are all the same width,
which matters under \code{sharing = "complete"}, where a segment cannot be shorter than the
locus is wide and so wider loci score higher for no biological reason. And they owe
nothing to annotation, so a target between genes still shows up.
}
\section{What the resolution is, and is not}{

Do not read a window as localising selection to its own width. The value at a point is set
by the segments covering it, and those run tens of kb either side, so neighbouring windows
repeat each other almost exactly: on one real cohort the correlation of
\code{log2(paired_ratio)} between windows 250 bp apart was 0.997, still 0.973 at 2 kb, and only
fell to 0.65 at the median segment length of 33 kb. Every window inside \emph{pfcrt}, \emph{pfgch1}
and \emph{pfkelch13} returned an identical ratio. The resolution floor is the segment-length
scale, so a sub-genic target is out of reach here -- use \code{\link[=run_ihs]{run_ihs()}} and \code{\link[=plot_ehh]{plot_ehh()}},
which work at SNP resolution.

That is also why \code{step} defaults to 2 kb rather than something finer: a denser grid costs
time and memory and returns the same numbers.
}

\section{Why there is no q-value}{

Windows overlap the same segments, so they are not independent tests. The core genome over
the median segment length is on the order of a few hundred independent positions against
tens of thousands of windows, which makes a Benjamini-Hochberg q across windows both
invalid and wildly conservative. \code{percentile} is the quantity to quote; it does not care
how finely the genome was cut. Per-window p-values are still reported, unadjusted, so you
can see which windows are tested at all.
}

\examples{
\dontrun{
ibd <- ibd_results(blocks = "hmm.txt", meta = meta, group_col_in_meta = "region")
scan <- ibd_block_extension_scan(ibd, loci = c("pfcrt", "pfgch1"))
subset(scan, source == "locus", c(locus, group, paired_ratio, percentile))
}
}
\seealso{
\code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}} for one locus and what the columns mean,
\code{\link[=ibd_block_extension_by_allele]{ibd_block_extension_by_allele()}} for splitting the pairs by carriage of a variant.
}
