% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensembl.R
\name{ensembl_gff_url}
\alias{ensembl_gff_url}
\alias{ensembl_genome_url}
\title{Ensembl Protists download URLs}
\usage{
ensembl_gff_url(
  species,
  release = ENSEMBL_PROTISTS_RELEASE,
  assembly = NULL,
  collection = NULL
)

ensembl_genome_url(
  species,
  release = ENSEMBL_PROTISTS_RELEASE,
  masking = c("none", "soft", "hard"),
  assembly = NULL,
  collection = NULL
)
}
\arguments{
\item{species}{Species name; see details.}

\item{release}{Ensembl Protists release number. Defaults to \link{ENSEMBL_PROTISTS_RELEASE}.}

\item{assembly}{Assembly token to use, skipping the species lookup. Optional.}

\item{collection}{FTP collection directory, or \code{""} for none. Optional; only needed
alongside \code{assembly} for a genome outside the table.}

\item{masking}{Repeat masking of the genome FASTA: \code{"none"} (the default), \code{"soft"}
(repeats in lower case) or \code{"hard"} (repeats as \code{N}).}
}
\value{
One URL, as a string.
}
\description{
The GFF3 annotation and the genome FASTA of a \emph{Plasmodium} genome, so a released
annotation can be handed straight to \code{\link[=read_gff_cds]{read_gff_cds()}} or \code{\link[=snp_aa_positions]{snp_aa_positions()}} without
looking the path up by hand. Nothing is downloaded -- these return the URL.
}
\details{
\code{species} may be the Ensembl name (\code{"plasmodium_falciparum"}), the species alone
(\code{"falciparum"}), \code{"P. falciparum"}, or a two-letter short name (\code{"pf"}, \code{"pv"}, \code{"pk"},
\code{"pb"}, \code{"pc"}, \code{"py"}). The short forms name the genome Ensembl treats as that species'
reference -- the one Ensembl gives a top-level directory. Species with no such genome,
\emph{P. malariae} and \emph{P. ovale} among them, have several competing assemblies and so must be
named in full; the error lists them, and \code{\link[=ensembl_species]{ensembl_species()}} lists them all.

The assembly token in every Ensembl filename follows whatever assembly the release
carried, and does change (Pf3D7 is \code{ASM276v2} through release 60 and \code{GCA000002765v3}
from release 61), so it is looked up for the release asked for rather than assumed. Pass
\code{assembly} to skip that lookup, which also builds URLs for a genome outside the table.
}
\examples{
ensembl_gff_url("falciparum")
ensembl_genome_url("falciparum")

# any of these name the same genome
ensembl_gff_url("pf")
ensembl_gff_url("P. falciparum")

# other species, and the assembly of an earlier release
ensembl_gff_url("vivax")
ensembl_genome_url("plasmodium_malariae_gca_900090045", masking = "soft")

\dontrun{
# a whole released annotation, read straight from the web
cds <- read_gff_cds(ensembl_gff_url("falciparum"))
snp_aa_positions(snps, cds, fasta = ensembl_genome_url("falciparum"))
}
}
\seealso{
\code{\link[=ensembl_species]{ensembl_species()}}, \code{\link[=read_gff_cds]{read_gff_cds()}}, \code{\link[=snp_aa_positions]{snp_aa_positions()}}
}
