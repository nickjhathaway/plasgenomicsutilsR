% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/carrier_genome_wide_relatedness.R
\name{carrier_genome_wide_relatedness}
\alias{carrier_genome_wide_relatedness}
\title{Are a variant's carriers more related genome-wide to begin with?}
\usage{
carrier_genome_wide_relatedness(
  x,
  allele,
  focal_chr,
  carrier = NULL,
  reference = NULL,
  group = NULL,
  min_pairs = 5L,
  meta = NULL
)
}
\arguments{
\item{x}{An \link{IbdResults} built with \verb{blocks =} and \verb{meta =}.}

\item{allele}{The variant to split on: the name of a metadata column, or a named vector of
\code{sample -> state} (as \code{\link[=allele_states]{allele_states()}} returns). Samples with \code{NA} are left out.}

\item{focal_chr}{The chromosome to exclude from the background, as the locus sits on it.
Any spelling \code{normalise_chr()} accepts (\code{"Pf3D7_07_v3"} or \code{"7"}).}

\item{carrier, reference}{Which states count as carrying and as reference; each may be a
vector. With exactly two observed states either may be left out and the other is implied
(\code{reference} the first level), with a message. With three or more states both must be
named, since there is nothing to imply the second from.}

\item{group}{Metadata column defining the groups. Defaults to the object's declared group
column, then to the first non-\code{sample} column of \code{meta}.}

\item{min_pairs}{A stratum needs this many pairs before it is reported, and the
reference stratum needs it before a fold or a test is anchored on it (default \code{5}).}

\item{meta}{Sample metadata; taken from \code{x} when not given.}
}
\value{
A tibble, one row per group x class, ordered by group then class:
\describe{
\item{\code{group}, \code{class}}{the group and one of \code{carrier/carrier},
\code{reference/reference}, \code{discordant}.}
\item{\code{n_pairs}}{every within-group pair of the class, including the pairs that share
nothing -- taking the mean over only the sharing pairs would compare two different
denominators and hide the effect being tested.}
\item{\code{mean_ibd_mb}, \code{median_ibd_mb}}{megabases of IBD per pair, summed over that
pair's segments off the focal chromosome.}
\item{\code{mean_ibd_frac}}{the mean as a share of the callable genome outside the focal
chromosome (its core length), the scale the rest of an IBD document uses.}
\item{\code{fold_vs_ref}}{\code{mean_ibd_mb} over the group's \code{reference/reference} mean.}
\item{\code{p_vs_ref}}{two-sided rank-sum of the stratum against that same group's
\code{reference/reference} pairs; \code{NA} on the reference row itself and where either side
is under \code{min_pairs}.}
}
}
\description{
The genome-wide background control for \code{\link[=ibd_block_extension_by_allele]{ibd_block_extension_by_allele()}}. Schaffner et
al.'s caution about reading IBD inside a sweep is that isolates carrying the swept
haplotype are typically more related to each other across the \emph{rest} of the genome as
well, so a locus statistic normalised only at the population level counts that background
as locus signal. This measures exactly that background: within each group it splits the
pairs by their state at a core variant and reports each stratum's mean per-pair IBD off
the \strong{focal chromosome}, which is dropped in full so the sweep cannot contribute to its
own baseline.
}
\details{
Where \code{fold_vs_ref} is above 1 and \code{p_vs_ref} is small, the carriers are a more related
set genome-wide and a population-level normalisation at the locus would be reading part of
that as locus signal; \code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}}'s \code{paired_ratio} and everything
\code{\link[=ibd_block_extension_by_allele]{ibd_block_extension_by_allele()}} reports already divide each pair by its own baseline and
are immune to it, so the point of the table is to say how much that per-pair control is
removing, and where. Where the fold sits at 1, carriage says nothing about background
relatedness and the two normalisations agree.
}
\section{What counts as carrier and reference}{

Unlike \code{\link[=ibd_block_extension_by_allele]{ibd_block_extension_by_allele()}}, which contrasts exactly two named states,
\code{carrier} and \code{reference} are \strong{sets} here, matched with \code{\%in\%}: at \emph{pfkelch13} every
propeller allele can count as carrier against a single \code{"REF"} reference, and a mixed call
naming both a carrier and a reference allele lands in neither. A pair is \code{carrier/carrier}
when both ends are in \code{carrier}, \code{reference/reference} when both are in \code{reference}, and
\code{discordant} when one is a carrier and the other a reference. A pair with an end in neither
set (a third allele, or \code{NA}) is left out of every stratum.
}

\examples{
\dontrun{
ibd <- ibd_results(blocks = "hmm.txt", meta = meta, group_col_in_meta = "region")
carrier_genome_wide_relatedness(ibd, allele = "PIN_Haplotype", focal_chr = "Pf3D7_07_v3",
                                carrier = "Present", reference = "Absent")
}
}
\seealso{
\code{\link[=ibd_block_extension_by_allele]{ibd_block_extension_by_allele()}} for the locus contrast this backstops,
\code{\link[=pair_fraction_summary]{pair_fraction_summary()}} for the whole-genome per-pair sharing by group,
\code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}} for the per-pair normalisation that absorbs this background.
}
