% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_ibd_pairs.R
\name{gene_ibd_pairs}
\alias{gene_ibd_pairs}
\title{Sample pairs sharing IBD over each gene}
\usage{
gene_ibd_pairs(x, genes = NULL, within = 0)
}
\arguments{
\item{x}{An \link{IbdResults} built with \verb{blocks =}.}

\item{genes}{Gene names from the object's track (default all), or a gene-interval data
frame (\code{name}, \code{chr}/\code{chrom}, \code{start}, \code{end}). Several genes come back in one table.}

\item{within}{Pad each gene interval by this many bp on both sides when deciding
whether a block overlaps it (default \code{0}). Coverage is always measured against the
gene's own span, so a block that reaches only into the padding covers \code{0}.}
}
\value{
A tibble with one row per pair x block x gene:
\describe{
\item{\code{sample1}, \code{sample2}}{the IBD pair, ordered so \code{sample1 < sample2}.}
\item{\code{chr}}{chromosome of the block and gene.}
\item{\code{block_start}, \code{block_end}}{the IBD segment, 0-based half-open.}
\item{\code{gene}, \code{name}, \code{gene_id}}{gene labels (\code{gene} is unique, see
\code{\link[=gene_ibd_overlap]{gene_ibd_overlap()}}).}
\item{\code{gene_start}, \code{gene_end}}{the gene interval, 0-based half-open.}
\item{\code{coverage}}{\code{"complete"} when the block spans the whole gene, else
\code{"partial"}.}
\item{\code{covered_start}, \code{covered_end}}{the covered portion of the gene -- the gene's
own bounds when \code{coverage} is \code{"complete"}.}
\item{\code{covered_bp}, \code{percent_covered}}{width of that portion, and it as a percentage
of the gene's length.}
}
}
\description{
An adjacency list of the pairs that are IBD across one or more genes: one row per
sample pair x IBD block x gene, saying how much of the gene the block covers. Pairs
with no IBD over a gene are simply absent -- use \code{\link[=gene_ibd_overlap]{gene_ibd_overlap()}} when you need the
denominator (all pairs compared) rather than just the sharing ones.
}
\details{
A pair appears more than once for a gene only if it has several separate IBD segments
spanning it.
}
\examples{
\dontrun{
ibd <- ibd_results(blocks = "hmm.txt", genes = PF_EXAMPLE_DRUG_GENES)
pairs <- gene_ibd_pairs(ibd, genes = c("pfcrt", "pfdhps"))
subset(pairs, coverage == "complete")
}
}
\seealso{
\code{\link[=gene_ibd_overlap]{gene_ibd_overlap()}} for the per-group-pair fractions,
\link{plasgenomicsutilsR-coordinates} for the interval convention.
}
