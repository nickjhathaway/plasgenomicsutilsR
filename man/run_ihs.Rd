% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ihs.R
\name{run_ihs}
\alias{run_ihs}
\title{Integrated haplotype score (iHS)}
\usage{
run_ihs(
  hap,
  group = NULL,
  meta = NULL,
  polarized = FALSE,
  freqbin = NULL,
  min_maf = 0.05,
  min_samples = 4,
  threads = 1
)
}
\arguments{
\item{hap}{A \code{\link[=parasite_haplotypes]{parasite_haplotypes()}} object.}

\item{group}{Metadata column naming the grouping, a vector aligned to the haplotype
rows, or \code{NULL} to scan every sample as one population.}

\item{meta}{Metadata (defaults to the one carried by \code{hap}).}

\item{polarized}{Treat allele 1 as derived (needs a real ancestral state).}

\item{freqbin}{Width of the allele-frequency bins iHS is standardised within, or \code{NULL}
(default) to pick one from \code{polarized}: \strong{1} (a single bin) when unpolarized, \code{0.05}
when polarized. The binning exists to control for \emph{derived} allele frequency, and an
unpolarized scan has no ancestral state -- only \code{FREQ_MAJ}/\code{FREQ_MIN} -- so major/minor
is not derived/ancestral and binning by it controls nothing. rehh warns about this and
about the resulting sparse bins above 0.5; a single bin silences both because it is the
right answer, not because the warning was noise. Not cosmetic: on a real 249-sample
cohort the two settings share only 25 of their top 50 |iHS| hits, and 4-12\% of SNPs
change sign (most in the 0.05-0.1 and >0.3 MAF bands).}

\item{min_maf}{Minor-allele frequency floor applied at standardisation.}

\item{min_samples}{Skip groups smaller than this.}

\item{threads}{Threads for \pkg{rehh}.}
}
\value{
A tibble with \code{group}, \code{chr}, \code{pos}, \code{snp_id}, \code{freq_minor}, \code{ihs} and
\code{neg_log10_p}.
}
\description{
Scans each group for recent positive directional selection, standardising the
integrated EHH ratio within allele-frequency bins so scores are comparable along the
genome. Large \code{abs(ihs)} marks a SNP whose haplotype background is unusually long for
its frequency.
}
\details{
Without an outgroup there is no ancestral state to polarise by, so \code{polarized = FALSE}
by default and the comparison is major versus minor allele rather than ancestral
versus derived. That is the standard treatment for \emph{P. falciparum} and it means the
\emph{sign} of \code{ihs} should not be read as "selection on the derived allele" -- use
\code{abs(ihs)} and \code{neg_log10_p}.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
hap <- parasite_haplotypes(ps, maf = 0.05)
run_ihs(hap, group = "country")
}
\references{
Voight, B. F., Kudaravalli, S., Wen, X. & Pritchard, J. K. (2006) A map of recent
positive selection in the human genome. \emph{PLoS Biology} 4, e72.
\doi{10.1371/journal.pbio.0040072}

Gautier, M., Klassmann, A. & Vitalis, R. (2017) rehh 2.0: a reimplementation of the R
package rehh to detect positive selection from haplotype structure.
\emph{Molecular Ecology Resources} 17, 78-90. \doi{10.1111/1755-0998.12634}
}
\seealso{
\code{\link[=ihs_genes]{ihs_genes()}}, \code{\link[=plot_ihs]{plot_ihs()}}, \code{\link[=run_rsb]{run_rsb()}}, \code{\link[=beta_score]{beta_score()}}
}
