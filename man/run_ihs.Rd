% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ihs.R
\name{run_ihs}
\alias{run_ihs}
\title{Integrated haplotype score (iHS)}
\usage{
run_ihs(
  hap,
  group = NULL,
  meta = NULL,
  polarized = FALSE,
  freqbin = NULL,
  min_maf = 0.05,
  maf_bands = NULL,
  min_samples = 4,
  maxgap = NA,
  scalegap = NA,
  discard_at_border = NULL,
  threads = 1,
  contrast = c("ref", "pairwise", "none")
)
}
\arguments{
\item{hap}{A \code{\link[=parasite_haplotypes]{parasite_haplotypes()}} object.}

\item{group}{Metadata column naming the grouping, a vector aligned to the haplotype
rows, or \code{NULL} to scan every sample as one population.}

\item{meta}{Metadata (defaults to the one carried by \code{hap}).}

\item{polarized}{Treat allele 1 as derived (needs a real ancestral state).}

\item{freqbin}{Width of the allele-frequency bins iHS is standardised within, or \code{NULL}
(default) to pick one from \code{polarized}: \strong{1} (a single bin) when unpolarized, \code{0.05}
when polarized. The binning exists to control for \emph{derived} allele frequency, and an
unpolarized scan has no ancestral state -- only \code{FREQ_MAJ}/\code{FREQ_MIN} -- so major/minor
is not derived/ancestral and binning by it controls nothing. rehh warns about this and
about the resulting sparse bins above 0.5; a single bin silences both because it is the
right answer, not because the warning was noise. Not cosmetic: on a real 249-sample
cohort the two settings share only 25 of their top 50 |iHS| hits, and 4-12\% of SNPs
change sign (most in the 0.05-0.1 and >0.3 MAF bands).}

\item{min_maf}{Minor-allele frequency floor applied at standardisation.}

\item{maf_bands}{Standardise within this many minor-allele-frequency bands, cut at
quantiles of the observed frequencies so each band holds a similar number of markers.
\code{NULL} (default) leaves the standardisation to \pkg{rehh} and \code{freqbin}. The spread of
the log iHH ratio depends on how common the minor allele is -- a rarer allele is
carried by fewer haplotypes, so its integrals are noisier -- and a single bin cannot
remove that, which leaves rarer SNPs over-represented in the tail. Bands do remove it,
without running into the empty bins that make \pkg{rehh}'s own binning unusable on an
unpolarized scan. Around 10 is reasonable; it replaces \code{freqbin} rather than combining
with it, and it changes every score, so a result computed with it is not comparable
to one computed without.}

\item{min_samples}{Skip groups smaller than this.}

\item{maxgap}{Largest gap between consecutive SNPs, in base pairs, that the EHH
integration may cross; \code{NA} (the default, and \pkg{rehh}'s) lets it cross any gap.
This matters more than its default suggests. A region with no SNPs -- a centromere, a
masked hypervariable block -- has nothing to break the haplotype, so EHH runs flat
across it and the integral accumulates \verb{EHH x gap length}. The SNPs flanking such a
hole then score on the width of the hole rather than on their haplotypes, in either
direction: the ratio is diluted towards zero when both alleles carry EHH into the gap,
and inflated when only one does. Pick a value from the data's own spacing (several
dozen times the median gap leaves ordinary density untouched while stopping at a real
hole) rather than from a round number.}

\item{scalegap}{Gaps wider than this are counted as being exactly this wide, rather than
stopping the integration outright; \code{NA} (default) does not rescale. A softer form of
\code{maxgap} -- it caps a hole's contribution instead of refusing to cross it.}

\item{discard_at_border}{Return \code{NA} instead of a truncated integral when the
integration runs into the end of a chromosome or a gap wider than \code{maxgap}. \code{NULL}
(default) ties it to \code{maxgap}: off when no \code{maxgap} is set (so the markers nearest the
telomeres are still scored), on when one is. On sparse markers this can empty the scan
-- if EHH never decays before the data runs out, every marker is at a border -- so a
scan that comes back mostly \code{NA} says so.}

\item{threads}{Threads for \pkg{rehh}.}
}
\value{
A tibble with \code{group}, \code{chr}, \code{pos}, \code{snp_id}, \code{freq_minor}, \code{unihs}, \code{ihs} and
\code{neg_log10_p}.

\code{unihs} is the \strong{un}standardised statistic, \code{log(iHH_major / iHH_minor)} (ancestral
over derived when \code{polarized = TRUE}), so \code{exp(unihs)} is the integrated-EHH ratio
itself. \code{ihs} is that value z-scored within its frequency band, which is what makes
scores comparable along the genome but also throws the scale away -- the band's mean and
sd are not recoverable from \code{ihs} alone, so keep \code{unihs} if you ever want the ratio back.
Note that \code{ihs = 0} does \strong{not} mean a ratio of 1: it means average for that group, and
the average is below 1 wherever minor-allele haplotypes are systematically longer.

\code{ihs} and \code{neg_log10_p} are \code{NA} wherever the integral could not be formed for one of
the two alleles -- see the note on missing scores below.
}
\description{
Scans each group for recent positive directional selection, standardising the
integrated EHH ratio within allele-frequency bins so scores are comparable along the
genome. Large \code{abs(ihs)} marks a SNP whose haplotype background is unusually long for
its frequency.
}
\details{
Without an outgroup there is no ancestral state to polarise by, so \code{polarized = FALSE}
by default and the comparison is major versus minor allele rather than ancestral
versus derived. That is the standard treatment for \emph{P. falciparum} and it means the
\emph{sign} of \code{ihs} should not be read as "selection on the derived allele" -- use
\code{abs(ihs)} and \code{neg_log10_p}.
}
\section{Contrasts}{

\code{log(iHH_A / iHH_B)} is a ratio, so it needs exactly two terms and there is no k-allele
iHS. At a marker carrying three alleles the honest answer is \strong{k-1 contrasts}, each saying
which two it compared, and the \code{contrast} column names them as \code{"0>1"}, \code{"0>2"} and so on
in allele-index order (\verb{$sites$alt} on the panel says which base each index is).

\code{"ref"} compares each alternate against the reference only. That is what keeps independent
origins apart: at a codon where three changes arose separately, pooling them into one
"not reference" class merges exactly the distinction the scan exists to draw.

What makes this cheap is that \strong{per-allele iHH does not depend on which other alleles are
at the marker} -- EHH for an allele class only ever involves that class's haplotypes. So
every contrast at a marker comes out of one \code{\link[rehh:calc_ehh]{rehh::calc_ehh()}} call, and the numbers are
identical to what an explicitly subset and recoded panel would give (verified to nine
decimal places). Only multiallelic markers cost anything extra.

A biallelic marker has one contrast, so \code{"ref"} reproduces \code{"none"} exactly, value for
value. The standardisation is over every row at once, so a marker contributing two rows has
both judged against the same genome-wide distribution.

Downstream, \code{contrast} is a grouping key: \code{\link[=ihs_windows]{ihs_windows()}}, \code{\link[=ihs_genes]{ihs_genes()}},
\code{\link[=selection_peaks]{selection_peaks()}} and \code{\link[=plot_ihs]{plot_ihs()}} all split on it, because two contrasts at one position
are two measurements rather than two SNPs at one site.
}

\section{Why a SNP can appear for one group only, or score \code{NA}}{

The scan is per group, so a SNP is tested in a group only where it is polymorphic there
and clears \code{min_maf} there. A variant private to one region therefore has one row, not
one per region, and that is a statement about the cohort rather than a fault.

A row can be present with \code{ihs} and \code{neg_log10_p} both \code{NA}. That means the SNP passed the
frequency filter but \pkg{rehh} could not integrate EHH for at least one of its two
alleles, so the log ratio is undefined. The usual causes are too few haplotypes carrying
the minor allele for the decay to be estimated, and EHH that never falls below the cutoff
before the data runs out -- a chromosome end, or a gap wider than \code{maxgap}. Both get more
common in small groups and at low minor-allele counts, which is the same corner where a
score that \emph{is} returned deserves the least trust. Treat \code{NA} as "not measurable here",
not as "no selection".
}

\examples{
ps <- example_pop_structure(umap = FALSE)
hap <- parasite_haplotypes(ps, maf = 0.05)
run_ihs(hap, group = "country")
}
\references{
Voight, B. F., Kudaravalli, S., Wen, X. & Pritchard, J. K. (2006) A map of recent
positive selection in the human genome. \emph{PLoS Biology} 4, e72.
\doi{10.1371/journal.pbio.0040072}

Gautier, M., Klassmann, A. & Vitalis, R. (2017) rehh 2.0: a reimplementation of the R
package rehh to detect positive selection from haplotype structure.
\emph{Molecular Ecology Resources} 17, 78-90. \doi{10.1111/1755-0998.12634}
}
\seealso{
\code{\link[=ihs_windows]{ihs_windows()}}, \code{\link[=ihs_genes]{ihs_genes()}}, \code{\link[=plot_ihs]{plot_ihs()}}, \code{\link[=run_rsb]{run_rsb()}}, \code{\link[=beta_score]{beta_score()}}
}
