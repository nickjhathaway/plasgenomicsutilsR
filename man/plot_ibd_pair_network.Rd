% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ibd_pair_network.R
\name{plot_ibd_pair_network}
\alias{plot_ibd_pair_network}
\title{Genome-wide IBD relatedness network}
\usage{
plot_ibd_pair_network(
  pairs,
  meta = NULL,
  weight = NULL,
  min_ibd = 0.01,
  samples = NULL,
  color_group = NULL,
  colors = NULL,
  shape_group = NULL,
  shapes = NULL,
  na_shape = .NA_SHAPE,
  na_colour = "grey70",
  include_isolated = TRUE,
  layout = "fr",
  spread = 1.5,
  node_size = 3,
  node_alpha = 0.9,
  edge_colour = "grey65",
  edge_alpha = 0.6,
  weight_range = c(0.15, 2.6),
  weight_breaks = NULL,
  title = NULL,
  subtitle = TRUE,
  seed = 42,
  colour_group = NULL,
  colours = NULL,
  na_color = NULL,
  edge_color = NULL
)
}
\arguments{
\item{pairs}{The per-pair table from \verb{plasgenomicsutils ibd_fraction_and_snp_density}
(\verb{*.pair_ibd_fraction.tsv.gz}): a path, a data frame, or an \link{IbdResults} carrying one.
Needs \code{sample1}/\code{sample2}, which that command writes.}

\item{meta}{Sample metadata for \code{color_group} / \code{shape_group}; taken from the \link{IbdResults}
when \code{pairs} is one.}

\item{weight}{Column holding the fraction. Defaults to \code{ibd_fraction_accessible}, the
callable-genome denominator; \code{ibd_fraction_full_genome} divides by the whole genome
instead, so it reads lower for the same pair.}

\item{min_ibd}{Draw an edge only above this fraction (default \code{0.01}). Every pair is in the
table, most of them sharing essentially nothing, so without a cutoff the graph is complete.}

\item{samples}{Optional subset of samples to keep.}

\item{color_group, colour_group, shape_group}{Metadata columns for node colour and shape.}

\item{colors, colours, shapes}{Values for those scales, named or positional (see \code{\link[=plot_ibd_network]{plot_ibd_network()}}).}

\item{na_shape, na_colour, na_color}{What a sample with no value in those columns gets.

Legends stack in a fixed order -- colour, shape, then the IBD width -- so plots stay
comparable; override with \code{+ ggplot2::guides(linewidth = ggplot2::guide_legend(order = 1))}.}

\item{include_isolated}{Show samples with no edge (default \code{TRUE}).}

\item{layout, spread, seed}{Layout algorithm, clique spreading, and the seed that makes it
reproducible.}

\item{node_size, node_alpha, edge_colour, edge_color, edge_alpha}{Node and edge aesthetics.}

\item{weight_range}{Narrowest and widest edge, in \code{linewidth} units.}

\item{weight_breaks}{Legend breaks; defaults to powers of two spanning the data, since
sharing runs over orders of magnitude.}

\item{title}{Plot title; \code{NULL} (default) writes one, \code{FALSE} or \code{NA} drops it.}

\item{subtitle}{\code{TRUE} (default) counts samples, edges and unconnected samples; a string
replaces it, \code{FALSE} drops it.}
}
\value{
A ggplot object.
}
\description{
Nodes are samples; an edge joins a pair sharing more than \code{min_ibd} of the genome IBD, and
its width is how much. \code{\link[=plot_ibd_network]{plot_ibd_network()}} asks who shares one locus, where every edge means
the same thing; this asks who is related overall, so the amount is the point.
}
\details{
Samples with no edge above the cutoff are drawn as a grid underneath, separated by a dashed
rule and counted -- an unrelated sample is a result, and dropping it silently would overstate
how connected the cohort is.
}
\examples{
\dontrun{
plot_ibd_pair_network("ibd_fraction.pair_ibd_fraction.tsv.gz", meta = meta,
                      color_group = "region", min_ibd = 0.03)
}
}
\seealso{
\code{\link[=plot_ibd_network]{plot_ibd_network()}} for one gene or locus.
}
