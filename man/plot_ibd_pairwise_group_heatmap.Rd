% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize.R
\name{plot_ibd_pairwise_group_heatmap}
\alias{plot_ibd_pairwise_group_heatmap}
\title{Group-by-group IBD heatmap along the genome}
\usage{
plot_ibd_pairwise_group_heatmap(
  x,
  anchor = NULL,
  chroms = NULL,
  skip_chr = NULL,
  trans = "identity",
  colors = NULL,
  limits = NULL,
  fill_scale = NULL,
  highlight_genes = NULL,
  label_genes = NULL
)
}
\arguments{
\item{x}{An \link{IbdResults} object.}

\item{anchor}{Optional single group to show (one panel) instead of all.}

\item{chroms}{Optional chromosomes to keep (any spelling); others are dropped and
the remaining ones re-laid-out contiguously.}

\item{skip_chr}{Optional chromosomes to drop (complement of \code{chroms}).}

\item{trans}{Fill-scale transform, e.g. \code{"identity"} (default), \code{"log2"}, \code{"sqrt"}.}

\item{colors}{Optional colour ramp for the fill (defaults to a single-hue
light-to-dark sequential scale that stays readable when most values are near 0).}

\item{limits}{Optional \code{c(lo, hi)} fill limits; values outside are squished into
range, so a few extremes near 1 don't crush the rest of the scale.}

\item{fill_scale}{Optional ggplot2 fill scale that fully overrides the above.}

\item{highlight_genes}{Optional gene names from the \code{genes} track to mark with lines;
requesting a name not in the track is an error.}

\item{label_genes}{Label the genes above the top panel. \code{NULL} (default) labels them
only when \code{highlight_genes} is given; \code{TRUE}/\code{FALSE} forces it.}
}
\value{
A ggplot object.
}
\description{
Per-SNP IBD sharing between group pairs as tiles along the genome, one facet
per anchor group. The stored upper-triangle (\code{group_a <= group_b}) is
mirrored so every anchor shows all partner groups. Alternating grey chromosome
bands and thin boundary lines mark where each chromosome starts and ends, and the
x-axis spans the \strong{full chromosome lengths} so un-genotyped (empty) regions are
visible rather than collapsed out.
}
