% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_region_haplotypes.R
\name{plot_region_haplotypes}
\alias{plot_region_haplotypes}
\title{Genotypes over one region, clustered by sample}
\usage{
plot_region_haplotypes(
  x,
  region,
  split = NULL,
  annotations = NULL,
  genotypes = NULL,
  samples = NULL,
  spacing = c("even", "genomic"),
  cluster = TRUE,
  dendrogram = TRUE,
  dend_width = 0.15,
  border = TRUE,
  border_colour = "grey45",
  allele = NULL,
  mark_snps = NULL,
  mark_colour = "#B2182B",
  genes = NULL,
  gene_track = NULL,
  gene_label_angle = 0,
  pad = 0,
  min_span = 0,
  max_snps = 2000,
  snp_width = NULL,
  colours = NULL,
  na_colour = "grey85",
  show_sample_names = NULL,
  reference = DEFAULT_REFERENCE
)
}
\arguments{
\item{x}{A \link{PopStructure} object (its genotypes supply the calls, its metadata the split).}

\item{region}{The interval to draw: a gene name from \code{genes}, a range
(\code{"13:1,720,000-1,730,000"}), a whole chromosome, or a one-row data frame with
chr/start/end.}

\item{split}{Optional metadata column whose levels block the rows. Samples are clustered
inside each block, and the blocks keep the column's level order.}

\item{annotations}{Optional metadata columns to draw as coloured strips down the right,
one column each, sharing the object's colour maps (see \code{\link[=meta_colors]{meta_colors()}}) so a level keeps
the colour it has in the other plots. Each gets its own legend.}

\item{genotypes}{Optional alternative calls to draw: a genotype matrix (samples x SNPs
with \code{chr:pos} column names), a \code{\link[=load_genotypes]{load_genotypes()}} list, or another \link{PopStructure}. \code{NULL}
(default) uses \code{x}'s own matrix. Metadata, grouping and the active sample set always come
from \code{x}, so one pruned object can supply the annotations while the full panel supplies
the calls -- which is the combination this plot wants.}

\item{samples}{Optional sample ids to keep.}

\item{spacing}{\code{"even"} (default) gives every SNP equal width; \code{"genomic"} places each at
its real coordinate.}

\item{cluster}{Cluster the samples (default \code{TRUE}). \code{FALSE} keeps them in the order they
arrive, which is worth doing when the metadata order is the point.}

\item{dendrogram}{Draw the dendrogram beside the rows (needs \code{cluster}).}

\item{dend_width}{Width of the dendrogram panel, as a fraction of the heatmap's.}

\item{border}{Outline every call (default \code{TRUE}), which is what makes single SNPs
readable as cells rather than a wash of colour.}

\item{border_colour}{Colour of that outline.}

\item{allele}{Which allele the dosages count, \code{"alt"} or \code{"ref"}. \code{NULL} (default) asks
the object; if it does not record it, alt is assumed and a message says so. The two are
indistinguishable from the matrix, and getting it backwards mislabels every call.}

\item{mark_snps}{Optional SNPs to mark with a vertical line: \code{chr:pos} ids, bare
positions, or a gene name from \code{genes} (marking every SNP in it).}

\item{mark_colour}{Colour for those lines.}

\item{genes}{Gene table for the track and for resolving names (e.g. \link{PF3D7_GENES}).}

\item{gene_track}{Draw the gene track under the heatmap (default \code{TRUE} when \code{genes} is
given). Under \code{"even"} spacing the boxes are mapped onto the SNP columns they cover, so
the track still says which columns sit in which gene.}

\item{gene_label_angle}{Rotation for the gene names, in degrees; \code{45} or \code{90} keeps long
systematic ids from colliding.}

\item{pad, min_span}{Context around \code{region}, as in \code{\link[=plot_ibd_locus]{plot_ibd_locus()}}: one value pads both
sides, two the left and the right (named \code{left} / \code{right} if you like).}

\item{max_snps}{Refuse to draw more than this many columns (default 2000). A window with
thousands of SNPs is unreadable as tiles; narrow it rather than have it silently thinned.}

\item{snp_width}{Width of each mark under \code{"genomic"} spacing, in base pairs. \code{NULL}
(default) uses 0.5\% of the window, wide enough to see and narrow enough to leave the
gaps between SNPs visible.}

\item{colours}{Named fill colours for \code{reference} / \code{mixed} / \code{alternate}.}

\item{na_colour}{Fill for missing calls.}

\item{show_sample_names}{Label the rows. \code{NULL} (default) labels them when there are at
most 40 samples.}

\item{reference}{Reference id, used when \code{region} names a whole chromosome.}
}
\value{
A patchwork of the dendrogram, heatmap and gene track, or a plain ggplot when
neither of those two is drawn.
}
\description{
A genotype heatmap for a single interval: one row per sample, one column per SNP, with the
samples clustered and a dendrogram beside them, and a gene track underneath. Splitting the
rows by a metadata column fixes the blocks and clusters \emph{within} each one, so a haplotype
shared across a group shows up as a solid band rather than being scattered by a
genome-wide ordering.
}
\details{
The genotypes should be the \strong{full} panel, not an LD-pruned one. Pruning keeps one SNP out
of each correlated run, and a correlated run is what a shared haplotype is -- so a pruned
panel shows fewer SNPs, chosen to be as uncorrelated as possible, and the blocks come out
thinner than they are. Build the object with \code{load_genotypes(prune = FALSE)} for this plot
and keep the pruned one for PCA / UMAP / admixture, where pruning is what you want; the
plot says so when the object records that it was pruned.

\code{spacing} decides what the horizontal axis means, and the two answers show different
things. \code{"even"} gives every SNP the same width, which is how the haplotype structure is
easiest to read but says nothing about distance. \code{"genomic"} puts each SNP at its real
coordinate, so a dense cluster of SNPs looks dense -- correct about position, but sparse
stretches become wide empty bands.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
plot_region_haplotypes(ps, "pfcrt", split = "country", pad = 20000,
                       genes = PF_EXAMPLE_DRUG_GENES)
}
