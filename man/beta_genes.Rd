% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta_score.R
\name{beta_genes}
\alias{beta_genes}
\title{Summarise beta scores per gene}
\usage{
beta_genes(b, genes = NULL, min_snps = 3)
}
\arguments{
\item{b}{The tibble from \code{\link[=beta_score]{beta_score()}}.}

\item{genes}{Gene table (\code{name}, \code{chr} or \code{chrom}, \code{start}, \code{end}); defaults to
\link{PF3D7_GENES}. Coordinates are 0-based half-open.}

\item{min_snps}{Genes with fewer scored SNPs than this are dropped.}
}
\value{
A tibble with \code{group}, \code{gene}, \code{chr}, \code{start}, \code{end}, \code{n_snps}, \code{beta_mean},
\code{beta_max}, sorted by \code{beta_mean} within each group.
}
\description{
Mean and maximum beta over the SNPs inside each gene, giving the per-gene view used to
rank candidates for balancing selection.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
b <- beta_score(ps, group = "country", window = 200000, min_window_snps = 1)
beta_genes(b, genes = PF_EXAMPLE_DRUG_GENES, min_snps = 1)
}
