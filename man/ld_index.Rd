% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld.R
\name{ld_index}
\alias{ld_index}
\title{Multilocus linkage disequilibrium: the index of association}
\usage{
ld_index(
  x,
  group = NULL,
  maf = 0,
  max_snps = LD_MAX_SNPS,
  het = c("missing", "dosage"),
  min_samples = 4,
  meta = NULL,
  genotype = NULL
)
}
\arguments{
\item{x}{A \link{PopStructure} or a genotype matrix (samples x SNPs, alt dosage, \code{chr:pos}
column names).}

\item{group}{Metadata column name, or a vector aligned to the rows; \code{NULL} pools
every sample.}

\item{maf}{Skip loci below this minor-allele frequency within the group.}

\item{max_snps}{Loci sampled before the pairwise distances, evenly along the genome.}

\item{het}{How a heterozygous call is read; see \code{\link[=pop_diversity]{pop_diversity()}}.}

\item{min_samples}{Skip groups smaller than this.}

\item{meta, genotype}{As in \code{\link[=pop_diversity]{pop_diversity()}}.}
}
\value{
A tibble with \code{group}, \code{n_samples}, \code{n_loci}, \code{ia}, \code{rbar_d}.
}
\description{
\code{Ia} and its sample-size-standardised form \code{rbarD} measure whether alleles at
\emph{unlinked} loci travel together -- the genome-wide signature of clonal propagation.
Both are 0 under free recombination and rise as reproduction becomes more clonal.
\code{rbarD} is the one to compare between datasets, since \code{Ia} grows with the number of
loci. Both are also upward-biased in small samples, so a group of ten will score above
a group of a hundred drawn from the same population -- read them beside \code{n_samples}.
}
\details{
A multilocus genotype is only defined where every locus is called, so the loci
carrying a gap are dropped and every sample is kept -- over thousands of loci even
slight missingness would otherwise leave almost no complete sample, and it is the
samples the statistic is about. \code{n_loci} reports what survived. The estimator is the
pairwise-distance decomposition of Brown et al. (1980), standardised by Agapow & Burt
(2001).
}
\examples{
ps <- example_pop_structure(umap = FALSE)
ld_index(ps, group = "country")
}
\references{
Brown, A. H. D., Feldman, M. W. & Nevo, E. (1980) Multilocus structure of natural
populations of \emph{Hordeum spontaneum}. \emph{Genetics} 96, 523-536.
\doi{10.1093/genetics/96.2.523}

Agapow, P.-M. & Burt, A. (2001) Indices of multilocus linkage disequilibrium.
\emph{Molecular Ecology Notes} 1, 101-102. \doi{10.1046/j.1471-8278.2000.00014.x}
}
