% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pop_structure.R
\name{plot_umap}
\alias{plot_umap}
\title{UMAP scatter plot}
\usage{
plot_umap(
  x,
  colour = NULL,
  colors = NULL,
  point_size = 1.6,
  point_alpha = 0.8,
  legend_point_size = 3,
  color = NULL,
  colours = NULL
)
}
\arguments{
\item{x}{A \code{pop_structure} object (built with \code{umap = TRUE}) or a \link{PopStructure}.}

\item{colour, color}{Metadata column to colour points by (needs \code{meta}).}

\item{colors, colours}{Optional named \code{level -> colour} vector for the colour scale
(e.g. from \code{\link[=meta_colors]{meta_colors()}}); a \code{PopStructure} supplies its stored map.}

\item{point_size, point_alpha}{Point aesthetics.}

\item{legend_point_size}{Size of the coloured dots in the legend (default \code{3}, larger
than the plotted points so the key is easy to read); \code{NULL} leaves it as-is.}
}
\value{
A ggplot object.
}
\description{
UMAP scatter plot
}
\examples{
ps <- example_pop_structure(umap = FALSE)
ps$run_umap(pca_components = 5)
plot_umap(ps, colour = "country")
}
