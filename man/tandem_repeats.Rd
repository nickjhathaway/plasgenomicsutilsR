% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tandem_repeats.R
\name{tandem_repeats}
\alias{tandem_repeats}
\title{Read a table of short tandem repeats}
\usage{
tandem_repeats(x)
}
\arguments{
\item{x}{Path to a BED file (plain or gzipped), or a data frame holding the first four
BED fields -- by name (\code{chrom}/\code{chr}, \code{start}, \code{end}, \code{name}) or, failing that, by
position.}
}
\value{
A tibble with one row per repeat: \code{chrom}, \code{chr}, \code{start}, \code{end}, \code{width},
\code{name}, \code{repeat_unit}, \code{unit_size}, \code{period}, \code{copies}. Input order is kept.
}
\description{
Turns the BED a repeat finder writes into a table that says, for each repeat, what its
unit is, how long that unit really is (its \emph{period}), and how many bases the run covers
-- the three things slippage depends on. \code{\link[=flag_tandem_repeats]{flag_tandem_repeats()}} applies the thresholds,
\code{\link[=merge_tandem_repeats]{merge_tandem_repeats()}} joins runs that flow into one another, and
\code{\link[=tandem_repeats_to_avoid]{tandem_repeats_to_avoid()}} does both.
}
\details{
Only the first four BED fields are read. The name field must be \code{UNIT_xCOPIES}
(\code{AACCCTA_x50.5714}), optionally preceded by a \code{chrom-start-end__} location, which is
what \verb{elucidator TandemRepeatFinderOutputToBed} and \code{findSimpleTandemRepeatLocations}
write (\verb{Pf3D7_01_v3-2-356__AACCCTA_x50.5714}); the location only restates the
coordinates and is ignored. A name of any other form gives \code{NA} for the unit, period and
copies, and such a row can still be flagged on its total width alone.

\strong{Period, not unit size.} A finder spells the same run as \code{AT}, \code{ATAT} or \code{ATATAT}
depending on where its alignment started. \code{period} is the smallest number of bases the
unit is a repetition of (2 for all three spellings; 1 for \code{AAAA}; 4 for \code{TATT}), so a
dinucleotide threshold reaches every dinucleotide run however it was reported.
\code{unit_size} keeps the reported spelling's length. The same span is sometimes reported
twice with different units (the two finders disagree on the alignment); both rows are
kept, and merging collapses them.
}
\section{Coordinates}{
 0-based half-open \verb{[start, end)}, as in BED and throughout the
package; see \link{plasgenomicsutilsR-coordinates}. \code{chr} is the chromosome normalised for
matching (\code{\link[=normalise_chr]{normalise_chr()}}), \code{chrom} as the file spells it.
}

\examples{
bed <- data.frame(chrom = "Pf3D7_07_v3", start = c(100, 130, 500),
                  end = c(120, 160, 560),
                  name = c("Pf3D7_07_v3-100-120__A_x20",
                           "Pf3D7_07_v3-130-160__ATAT_x7.5",
                           "Pf3D7_07_v3-500-560__TATT_x15"))
tandem_repeats(bed)[, c("start", "end", "width", "repeat_unit", "period")]
}
\seealso{
\code{\link[=pf3d7_tandem_repeats]{pf3d7_tandem_repeats()}} for the bundled Pf3D7 table,
\code{\link[=tandem_repeats_to_avoid]{tandem_repeats_to_avoid()}} for the whole recipe.
}
