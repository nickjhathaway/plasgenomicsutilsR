% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gff_features.R
\name{read_gff_features}
\alias{read_gff_features}
\title{Exons, introns, CDS or spans of genes, from a GFF}
\usage{
read_gff_features(
  gff,
  feature = c("exon", "intron", "cds", "gene"),
  ids = NULL,
  per = c("gene", "transcript"),
  genes = PF3D7_GENES
)
}
\arguments{
\item{gff}{Path or URL to a GFF3 file, plain or gzipped, as for \code{\link[=read_gff_cds]{read_gff_cds()}}.}

\item{feature}{One of \code{"exon"}, \code{"intron"}, \code{"cds"}, \code{"gene"}.}

\item{ids}{Which genes: gene ids (\code{"PF3D7_0709000"}), the GFF's \code{Name} attribute
(\code{"CRT"}, case-insensitive), a \code{name} from \code{genes} (\code{"pfcrt"}), or transcript ids, which
restrict that gene to the transcript named. \code{NULL} (the default) returns every gene.}

\item{per}{\code{"gene"} merges isoforms; \code{"transcript"} keeps them apart.}

\item{genes}{A gene table with \code{name} and \code{gene_id} columns, so \code{ids} can use the
package's friendly names. Defaults to \link{PF3D7_GENES}; \code{NULL} to use the GFF's \code{Name}
only.}
}
\value{
A tibble with one row per interval: \code{gene_id}, \code{name} (the \code{genes} name, else
the GFF \code{Name}, else \code{NA}), \code{transcript_id} when \code{per = "transcript"}, \code{feature},
\code{index}, \code{chrom} (as the GFF spells it), \code{chr} (normalised), \code{start}, \code{end}, \code{width},
\code{strand}. Genes in GFF order, intervals by position.
}
\description{
The intervals a target design starts from. Most of a gene's troublesome tandem repeats
sit in its introns, so a design will often want the exons alone; the rest are in the
exons, so the flow is gene, then exons, then \code{\link[=bed_subtract]{bed_subtract()}} with the repeats from
\code{\link[=tandem_repeats_to_avoid]{tandem_repeats_to_avoid()}}. This reads a GFF3 and returns the pieces you ask for as
interval rows that drop straight into \code{\link[=bed_subtract]{bed_subtract()}}, \code{\link[=bed_intersect]{bed_intersect()}} and
\code{\link[=write_bed]{write_bed()}}.
}
\details{
\code{feature} is what to return:
\itemize{
\item \code{"exon"} -- the exons (the GFF's \code{exon} features; a GFF with none falls back to its
\code{CDS} features, with a message);
\item \code{"intron"} -- the gaps between consecutive exons;
\item \code{"cds"} -- the coding parts of the exons only;
\item \code{"gene"} -- each gene's whole span, one row per gene.
}

\code{per = "gene"} (the default) gives one set of intervals per gene, merging the exons of
every isoform, so an intron is a stretch no isoform transcribes; \code{per = "transcript"}
keeps each transcript's own exons and introns. \code{index} numbers them in transcript
orientation, so exon 1 of a minus-strand gene is the one with the highest coordinates.
}
\section{Coordinates}{
 \strong{0-based half-open}, like every interval table in the package
(see \link{plasgenomicsutilsR-coordinates}) -- converted from the GFF's 1-based inclusive
coordinates here, at the boundary. \code{\link[=read_gff_cds]{read_gff_cds()}} is the one reader that keeps the
GFF's numbering, because \code{\link[=aa_intervals]{aa_intervals()}} walks codons on it; do not mix the two.
}

\examples{
gff <- system.file("extdata", "pf3d7_drug_gene_cds.gff", package = "plasgenomicsutilsR")
read_gff_features(gff, "exon", ids = "pfcrt")
read_gff_features(gff, "intron", ids = "pfcrt")[, c("index", "start", "end", "width")]
read_gff_features(gff, "gene")

# the exons of two genes, minus the tandem repeats and 10 bp of clearance around each
exons <- read_gff_features(gff, "exon", ids = c("pfcrt", "pfkelch13"))
targets <- bed_subtract(exons, tandem_repeats_to_avoid(pf3d7_tandem_repeats()), pad = 10)
targets[, c("name", "index", "start", "end", "piece", "width")]
}
\seealso{
\code{\link[=tandem_repeats_to_avoid]{tandem_repeats_to_avoid()}} and \code{\link[=bed_subtract]{bed_subtract()}} for what comes next,
\code{\link[=read_gff_cds]{read_gff_cds()}} for the codon-walking reader.
}
