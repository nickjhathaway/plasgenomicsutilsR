% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld.R
\name{read_ld_decay}
\alias{read_ld_decay}
\title{Read an LD-decay table}
\usage{
read_ld_decay(path, half_decay = NULL, levels = NULL)
}
\arguments{
\item{path}{TSV(.gz) written by \verb{plasgenomicsutils ld_decay}.}

\item{half_decay}{Optional TSV from \code{--half-decay-output}; recomputed when absent.}

\item{levels}{Optional group order; defaults to a natural sort.}
}
\value{
A tibble of \code{group}, \code{bin_start}, \code{bin_end}, \code{bin_mid}, \code{n_pairs}, \code{mean_r2},
\code{median_r2}.
}
\description{
Reads what \verb{plasgenomicsutils ld_decay} writes, ready for \code{\link[=plot_ld_decay]{plot_ld_decay()}}. The
half-decay distance is attached as an \code{ld_half_decay} attribute -- recomputed from the
binned means if the separate table is not supplied -- and the scan's \code{max_dist},
\code{max_snps} and \code{maf} are attached from the file header, so the thinning behind a curve
travels with it.
}
\details{
The decay scan lives in the Python package because it is quadratic in the number of
SNPs inside each window; on a 249-sample, 28k-SNP callset it runs in about 7 seconds
there. \code{ld_index()} stays here, being linear and quick.
}
\seealso{
\code{\link[=ld_index]{ld_index()}}, \code{\link[=plot_ld_decay]{plot_ld_decay()}}
}
