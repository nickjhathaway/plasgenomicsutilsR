% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ihs.R
\name{run_rsb}
\alias{run_rsb}
\title{Cross-population extended haplotype homozygosity (Rsb)}
\usage{
run_rsb(
  hap,
  group,
  meta = NULL,
  pairs = NULL,
  polarized = FALSE,
  min_samples = 4,
  maxgap = NA,
  scalegap = NA,
  discard_at_border = NULL,
  threads = 1
)
}
\arguments{
\item{hap}{A \code{\link[=parasite_haplotypes]{parasite_haplotypes()}} object.}

\item{group}{Metadata column naming the grouping (required -- there must be at least
two groups to compare).}

\item{meta}{Metadata (defaults to the one carried by \code{hap}).}

\item{pairs}{Optional list of \code{c(group_a, group_b)} pairs; defaults to all pairs.}

\item{polarized}{Treat allele 1 as derived (needs a real ancestral state).}

\item{min_samples}{Skip groups smaller than this.}

\item{maxgap}{Largest gap between consecutive SNPs, in base pairs, that the EHH
integration may cross; \code{NA} (the default, and \pkg{rehh}'s) lets it cross any gap.
This matters more than its default suggests. A region with no SNPs -- a centromere, a
masked hypervariable block -- has nothing to break the haplotype, so EHH runs flat
across it and the integral accumulates \verb{EHH x gap length}. The SNPs flanking such a
hole then score on the width of the hole rather than on their haplotypes, in either
direction: the ratio is diluted towards zero when both alleles carry EHH into the gap,
and inflated when only one does. Pick a value from the data's own spacing (several
dozen times the median gap leaves ordinary density untouched while stopping at a real
hole) rather than from a round number.}

\item{scalegap}{Gaps wider than this are counted as being exactly this wide, rather than
stopping the integration outright; \code{NA} (default) does not rescale. A softer form of
\code{maxgap} -- it caps a hole's contribution instead of refusing to cross it.}

\item{discard_at_border}{Return \code{NA} instead of a truncated integral when the
integration runs into the end of a chromosome or a gap wider than \code{maxgap}. \code{NULL}
(default) ties it to \code{maxgap}: off when no \code{maxgap} is set (so the markers nearest the
telomeres are still scored), on when one is. On sparse markers this can empty the scan
-- if EHH never decays before the data runs out, every marker is at a border -- so a
scan that comes back mostly \code{NA} says so.}

\item{threads}{Threads for \pkg{rehh}.}
}
\value{
A tibble with \code{pair}, \code{pop1}, \code{pop2}, \code{chr}, \code{pos}, \code{snp_id}, \code{value} (Rsb)
and \code{neg_log10_p}.

\strong{Reading \code{value}.} Rsb is a log ratio of site-specific extended haplotype
homozygosity between the two populations, standardised to roughly a standard normal
under neutrality. So it is a z-score, and its \strong{sign says which population}:
positive means haplotype homozygosity extends further in \code{pop1} than \code{pop2}, i.e. the
sweep is in \code{pop1}; negative points at \code{pop2}. Swapping the pair flips the sign.

Magnitude reads like any z: |Rsb| above ~2 is unremarkable in a genome scan, above ~4
is worth a look, and real sweeps in \emph{P. falciparum} run higher still. Rather than
picking a cutoff by eye, use \code{neg_log10_p} -- the two-sided normal p-value rehh derives
from \code{value} -- and correct it: at ~20k SNPs a Bonferroni line sits near 5.6, which is
the convention in the literature. \code{\link[=selection_peaks]{selection_peaks()}} will merge the survivors into
loci, since one sweep spans many SNPs, and \code{\link[=annotate_snps]{annotate_snps()}} says which genes they are
in.

Two cautions. The standardisation assumes most of the genome is neutral, so a
genome-wide p-value is relative to \emph{this} comparison and not comparable across pairs
with different sample sizes. And Rsb contrasts two populations, so a high score means
they differ -- it cannot by itself tell a sweep in one from a loss of variation in the
other; that is what the EHH decay curves are for.
}
\description{
Compares the site-specific integrated EHH of two groups. Unlike \code{\link[=run_ihs]{run_ihs()}}, Rsb finds
sweeps that have gone to completion in one population, because the comparison is
against the other population rather than against the other allele.
}
\examples{
\dontrun{
# Rsb contrasts two groups' haplotype homozygosity at the same SNP
hap <- parasite_haplotypes(ps, fws = fws, min_fws = 0.95)
rsb <- run_rsb(hap, group = "region", pop1 = "north", pop2 = "south")
plot_ihs(rsb)
}
}
\references{
Tang, K., Thornton, K. R. & Stoneking, M. (2007) A new approach for using
genome scans to detect recent positive selection in the human genome.
\emph{PLoS Biology} 5, e171. \doi{10.1371/journal.pbio.0050171}
}
