% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ihs.R
\name{run_rsb}
\alias{run_rsb}
\title{Cross-population extended haplotype homozygosity (Rsb)}
\usage{
run_rsb(
  hap,
  group,
  meta = NULL,
  pairs = NULL,
  polarized = FALSE,
  min_samples = 4,
  threads = 1
)
}
\arguments{
\item{hap}{A \code{\link[=parasite_haplotypes]{parasite_haplotypes()}} object.}

\item{group}{Metadata column naming the grouping (required -- there must be at least
two groups to compare).}

\item{meta}{Metadata (defaults to the one carried by \code{hap}).}

\item{pairs}{Optional list of \code{c(group_a, group_b)} pairs; defaults to all pairs.}

\item{polarized}{Treat allele 1 as derived (needs a real ancestral state).}

\item{min_samples}{Skip groups smaller than this.}

\item{threads}{Threads for \pkg{rehh}.}
}
\value{
A tibble with \code{pair}, \code{pop1}, \code{pop2}, \code{chr}, \code{pos}, \code{snp_id}, \code{value} (Rsb)
and \code{neg_log10_p}.
}
\description{
Compares the site-specific integrated EHH of two groups. Unlike \code{\link[=run_ihs]{run_ihs()}}, Rsb finds
sweeps that have gone to completion in one population, because the comparison is
against the other population rather than against the other allele.
}
\references{
Tang, K., Thornton, K. R. & Stoneking, M. (2007) A new approach for using
genome scans to detect recent positive selection in the human genome.
\emph{PLoS Biology} 5, e171. \doi{10.1371/journal.pbio.0050171}
}
