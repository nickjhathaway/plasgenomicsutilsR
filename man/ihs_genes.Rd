% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ihs.R
\name{ihs_genes}
\alias{ihs_genes}
\title{Summarise a haplotype scan per gene}
\usage{
ihs_genes(scan, genes = NULL, within = 0, min_snps = 1)
}
\arguments{
\item{scan}{The tibble from \code{\link[=run_ihs]{run_ihs()}}, \code{\link[=run_rsb]{run_rsb()}} or \code{\link[=run_xpehh]{run_xpehh()}}.}

\item{genes}{Gene table (\code{name}, \code{chr} or \code{chrom}, \code{start}, \code{end}); defaults to
\link{PF3D7_GENES}. Coordinates are 0-based half-open.}

\item{within}{Widen each gene by this many bp on both sides before matching SNPs.
Note what this does to a table of "top genes": one strong SNP is then credited to
every gene within \code{within} bp of it, so a single sweep can fill several rows that
share a \code{peak_pos}. \code{peak_in_gene} says whether the peak is actually inside the
gene or was pulled in from the flank, and \code{n_snps} counts the widened window.}

\item{min_snps}{Genes with fewer scored SNPs than this are dropped.}
}
\value{
A tibble with the grouping column, \code{gene}, \code{chr}, \code{start}, \code{end}, \code{n_snps},
\code{max_neg_log10_p}, \code{max_abs_value}, \code{peak_pos} and \code{peak_in_gene}.
}
\description{
The strongest signal inside each gene, which is how selection scans are usually
reported: one row per gene rather than per SNP.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
hap <- parasite_haplotypes(ps, maf = 0.05)
ihs_genes(run_ihs(hap, group = "country"), genes = PF_EXAMPLE_DRUG_GENES, min_snps = 1)
}
