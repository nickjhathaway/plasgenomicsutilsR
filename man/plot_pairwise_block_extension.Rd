% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_block_extension.R
\name{plot_pairwise_block_extension}
\alias{plot_pairwise_block_extension}
\title{Block extension between every pair of groups, as a triangle per locus}
\usage{
plot_pairwise_block_extension(
  x,
  loci = NULL,
  min_pairs = 5L,
  group = NULL,
  within = 0,
  sharing = c("overlap", "complete"),
  min_ref_blocks = 1L,
  individual = FALSE,
  label = TRUE,
  digits = 2,
  ncol = NULL,
  limits = "shared",
  colors = NULL,
  fill_scale = NULL,
  meta = NULL,
  colours = NULL
)
}
\arguments{
\item{x}{An \link{IbdResults} built with \verb{blocks =} and \verb{meta =}, or a data frame already
returned by \code{ibd_block_extension_test(..., pairs = "all")}.}

\item{loci}{Loci to draw, as \code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}} takes them. Ignored when \code{x} is
already a result table.}

\item{min_pairs}{Group pairs with fewer sharing pairs are left grey (default \code{5}).}

\item{group, within, sharing, min_ref_blocks, meta}{Passed to \code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}}.}

\item{individual}{Return a named list of one plot per locus instead of a faceted grid.}

\item{label}{Draw the ratio and pair count on each tile (default \code{TRUE}).}

\item{digits}{Decimal places for the ratio (default \code{2}).}

\item{ncol}{Columns in the facet grid.}

\item{limits}{Fill limits, or \code{"shared"} (default) to pin every locus to one symmetric
scale so the pages are comparable.}

\item{colors, colours}{Fill ramp, low to high through the midpoint.}

\item{fill_scale}{A complete \code{ggplot2} fill scale, replacing the built-in one.}
}
\value{
A \code{ggplot}, or a named list of them when \code{individual = TRUE}.
}
\description{
The length counterpart of \code{\link[=plot_pairwise_ibd_for_genes]{plot_pairwise_ibd_for_genes()}}. That plot fills each group-pair
cell with the \emph{fraction of pairs sharing} a gene; this one fills it with how much \strong{longer}
those shared segments are than the sharing pairs' own genome-wide background, the
\code{paired_ratio} of \code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}}.
}
\details{
The diagonal is the within-group result. The off-diagonal is what the diagonal cannot say:
whether the long segments continue across a group boundary, which is the difference between
a haplotype that is spreading between regions and one that is expanding inside each.
}
\section{Reading the tiles}{

The fill diverges around \strong{1} on a log2 scale, so 2 and 0.5 sit the same distance from the
middle. Note that 1 is \emph{not} the null -- every locus is inflated by length-biased sampling,
so a whole triangle sitting above 1 is the expected picture, not a finding. Use
\code{\link[=ibd_block_extension_scan]{ibd_block_extension_scan()}} to say what a given ratio is worth in a given group.

Cells are grey where the group pair had fewer than \code{min_pairs} sharing pairs, which is
common and is not the same as a ratio near 1. Every drawn cell carries its pair count under
the ratio, because a cell resting on five pairs and one resting on five hundred look
identical otherwise and routinely differ by more than the colour does.
}

\examples{
\dontrun{
ibd <- ibd_results(blocks = "hmm.txt", meta = meta, group_col_in_meta = "region")
plot_pairwise_block_extension(ibd, c("pfcrt", "pfgch1"))
}
}
\seealso{
\code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}}, \code{\link[=ibd_block_extension_scan]{ibd_block_extension_scan()}},
\code{\link[=plot_pairwise_ibd_for_genes]{plot_pairwise_ibd_for_genes()}} for the sharing-fraction counterpart.
}
