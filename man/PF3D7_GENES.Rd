% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{PF3D7_GENES}
\alias{PF3D7_GENES}
\title{Pf3D7 gene coordinates}
\format{
A data frame with 5318 rows and columns:
\describe{
\item{Pf3D7_chrom}{sequence id, e.g. \code{Pf3D7_01_v3}}
\item{start, end}{1-based transcript-span bounds}
\item{chrom}{short chromosome (\code{"1"}..\code{"14"}, or \code{"API"} / \code{"MIT"})}
\item{gene_id}{PlasmoDB gene id, e.g. \code{PF3D7_0709000}}
\item{name}{friendly display name (see above)}
}
}
\source{
VEuPathDB / PlasmoDB Pf3D7 GFF, release version 2020-09-01.
}
\usage{
PF3D7_GENES
}
\description{
Every protein-coding gene in the \emph{Plasmodium falciparum} 3D7 reference, for use as
the \code{genes} track of an \link{IbdResults} object (gene reference lines on Manhattan /
tug-of-war plots, the pairwise-IBD triangles, and \code{\link[=pos_selection_genes]{pos_selection_genes()}}).
}
\details{
Coordinates are the \strong{transcript (mRNA) span} -- the minimum transcript start to the
maximum transcript end across a gene's isoforms -- rather than the wider \code{gene}
feature, so they exclude the untranslated flanks recorded on the gene record.

\code{name} is a friendly \code{"pf"}-prefixed lower-casing of the GFF \code{Name} attribute
(e.g. \code{CRT} -> \code{pfcrt}, \code{MSP1} -> \code{pfmsp1}); genes with no \code{Name} fall back to their
\code{gene_id} (so \code{name} equals \code{gene_id} for those). The two folate genes, whose GFF
Names are long compounds, are surgically set to \code{pfdhfr} (\code{PF3D7_0417200}) and
\code{pfdhps} (\code{PF3D7_0810800}). Family names (\code{pfvar}, \code{pfrif}, ...) repeat across the
family's members, so \code{name} is not unique; \code{gene_id} is.
}
\seealso{
\link{PF_EXAMPLE_DRUG_GENES}, \code{\link[=pos_selection_genes]{pos_selection_genes()}}
}
\keyword{datasets}
