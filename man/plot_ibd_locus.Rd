% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ibd_locus.R
\name{plot_ibd_locus}
\alias{plot_ibd_locus}
\title{One locus in detail: IBD sharing against a selection scan}
\usage{
plot_ibd_locus(
  x,
  locus,
  scan = NULL,
  metric = "neg_log10_p",
  groups = NULL,
  pad = 0.1,
  min_span = 50000,
  threshold = NULL,
  scan_abs = NULL,
  size_by = NULL,
  shape_by = NULL,
  point_size = 1.4,
  ibd_colour = "#2166ac",
  scan_colour = "#d95f02",
  gene_track = TRUE,
  genes_for_track = NULL,
  gene_label_angle = 0
)
}
\arguments{
\item{x}{An \link{IbdResults} object (needs a per_snp_group table).}

\item{locus}{The interval to draw: a gene name from the object's track, a range
(\code{"7:380,000-430,000"}), a whole chromosome (\code{"7"}), or a one-row data frame with
chr/start/end.}

\item{scan}{Optional per-SNP scan for the right axis: a \code{\link[=run_ihs]{run_ihs()}} result, a
\code{\link[=beta_score]{beta_score()}} table, or any table with chr, pos and \code{metric}. \code{NULL} (default) uses
the object's own IBD selection statistic.}

\item{metric}{Column of \code{scan} to draw (default \code{"neg_log10_p"}).}

\item{groups}{Optional groups to keep; \code{NULL} draws every group in the object.}

\item{pad}{Context to add around \code{locus}, clamped to the chromosome. One value pads both
sides (default 10\%); two pad the left and the right, either in that order or named --
\code{c(left = 5000, right = 40000)}, and naming only one side pads only that side. Each side
is read on its own: below 1 it is a fraction of the interval's span, at or above 1 it is
base pairs, so \code{c(0.1, 20000)} is legal.}

\item{min_span}{Widen the window to at least this many base pairs (default 50 kb). A
single gene padded by a fraction of its own length is usually too narrow to contain
more than a handful of SNPs.}

\item{threshold}{Line(s) to draw on the scan axis: a height, or the name of a threshold
the selection run wrote -- \code{"bonferroni"}, \code{"fdr"}, \code{"permutation"}, \code{"empirical"},
\code{"both"} or \code{"all"}, the same vocabulary and colours \code{\link[=plot_selection_manhattan]{plot_selection_manhattan()}} uses,
and per group where the run wrote one per group. \code{NULL} (default) draws the object's
Bonferroni line, or the 1\% tail when the right axis is an external \code{scan}; \code{NA} draws
none. Naming a kind the run did not write is an error, and naming one at all needs the
object's own statistic, since the threshold table says nothing about an external scan.}

\item{scan_abs}{Plot the magnitude of a signed statistic. \code{NULL} (default) takes the
absolute value when the metric actually has negative values -- iHS, Rsb, a z-score and
beta are all read by distance from zero, and both tails mean selection -- and labels the
axis \verb{|metric|}. \code{FALSE} keeps the sign and extends the panel below zero instead, which
is readable but means a point level with the IBD curve's zero is a value of zero rather
than the floor of the axis.}

\item{size_by}{Optional scan column to map to point size (e.g. a per-SNP allele
frequency difference). \code{NULL} (default) draws every point the same size.}

\item{shape_by}{Optional scan column to map to point shape. \code{NULL} (default) uses a
mutation-class column (\code{mutation_type}, \code{mutation_class}, \code{effect}, \code{consequence}) if
the scan has one, and plain points otherwise; \code{NA} never maps shape.}

\item{point_size}{Size of the scan points when \code{size_by} is not used.}

\item{ibd_colour, scan_colour}{Colours for the IBD curve and the scan points.}

\item{gene_track}{Draw the gene track under the panel (default \code{TRUE}).}

\item{genes_for_track}{Optional gene table for the track drawn under the panel
(e.g. \link{PF3D7_GENES}), so every gene in the window is shown and named while the object's
own track still supplies the marked positions inside the panel. Without it the
track and the marks come from the same genes, which means marking a whole annotation
just to see the neighbours.}

\item{gene_label_angle}{Rotation for the gene names in that track, in degrees. \code{0}
(default) centres each name under its gene; \code{45} or \code{90} runs it down to the left,
which is what keeps long systematic ids from colliding over a dense annotation.}
}
\value{
A ggplot object, or a patchwork of the panel over the gene track when
\code{gene_track = TRUE} and the window contains genes.
}
\description{
A zoomed panel over a single interval with two vertical axes: the fraction of pairs
sharing each SNP by IBD as a step curve on the left, and a per-SNP selection scan as
points on the right, with a gene track underneath. Reading both signals against one
another over a few hundred kilobases is what separates a shared haplotype that is also
under selection from one that is merely common.
}
\details{
The two tracks keep their own units. The scan is drawn on the secondary axis, so a point
and a curve at the same height are not the same number: compare shapes and positions,
not heights.
}
\examples{
x <- example_ibd_results()
plot_ibd_locus(x, "pfcrt", pad = 50000)
}
