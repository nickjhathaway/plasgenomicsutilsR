% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pop_structure.R
\name{snmf_cross_entropy}
\alias{snmf_cross_entropy}
\title{Cross-entropy of every sNMF replicate, summarised per K}
\usage{
snmf_cross_entropy(x, K = NULL)
}
\arguments{
\item{x}{An \code{\link[=run_snmf]{run_snmf()}} result (or a raw LEA project, with \code{K} given).}

\item{K}{Candidate K values (defaults to the fitted range).}
}
\value{
A tibble, one row per K: \code{K}, \code{n_runs}, \code{min}, \code{mean}, \code{max}, and \code{best_run}
(the replicate index attaining \code{min}, i.e. the one \code{\link[=snmf_q]{snmf_q()}} returns).
}
\description{
sNMF fits \code{rep} independent replicates at each K and scores each by cross-entropy
(lower is better). \code{\link[=snmf_q]{snmf_q()}} and \code{\link[=plot_admixture]{plot_admixture()}} use the \strong{minimum}-cross-entropy
replicate, so the \code{min} column is the one that describes the ancestry actually plotted;
\code{mean} and \code{max} show how much the replicates disagreed, which is worth a look before
trusting a K.
}
\details{
\code{min} is also what K is chosen on, by \code{\link[=snmf_best_k]{snmf_best_k()}} and by the
\code{\link[=plot_snmf_cross_entropy]{plot_snmf_cross_entropy()}} elbow, so the K you settle on and the ancestry you draw at it
are scored by the same number. A curve that keeps falling or is flat means the data do not
support a well-defined K, whatever \code{\link[=snmf_best_k]{snmf_best_k()}} returns; \code{n_runs} is worth a glance
first, since a K whose replicates mostly failed has its minimum taken over fewer of them.
}
\examples{
\dontrun{
snmf_cross_entropy(fit)           # one row per K: min, mean, and n_runs
}
}
\seealso{
\code{\link[=plot_snmf_cross_entropy]{plot_snmf_cross_entropy()}}, \code{\link[=snmf_best_k]{snmf_best_k()}}, \code{\link[=snmf_q]{snmf_q()}}
}
