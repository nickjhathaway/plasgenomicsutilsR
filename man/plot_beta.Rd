% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_scans.R
\name{plot_beta}
\alias{plot_beta}
\title{Manhattan plot of beta scores}
\usage{
plot_beta(
  b,
  threshold = NULL,
  genes = NULL,
  highlight_genes = NULL,
  label_genes = NULL,
  chroms = NULL,
  skip_chr = NULL,
  reference = DEFAULT_REFERENCE,
  point_size = 0.6,
  point_alpha = 0.7,
  colours = NULL
)
}
\arguments{
\item{b}{The tibble from \code{\link[=beta_score]{beta_score()}}.}

\item{threshold}{Draw a dashed line at this beta; \code{NULL} uses the 99th percentile of
the scores actually plotted, the empirical tail these scans are read at.}

\item{genes}{Gene table to mark (e.g. \link{PF_EXAMPLE_DRUG_GENES}); \code{NULL} for none.}

\item{highlight_genes, label_genes}{Which genes to mark and whether to name them.}

\item{chroms, skip_chr}{Chromosomes to keep or drop.}

\item{reference}{Reference id for the chromosome layout.}

\item{point_size, point_alpha, colours}{Point and band aesthetics.}
}
\value{
A ggplot object.
}
\description{
Manhattan plot of beta scores
}
\examples{
ps <- example_pop_structure(umap = FALSE)
b <- beta_score(ps, group = "country", window = 300000, min_window_snps = 1)
plot_beta(b)
}
