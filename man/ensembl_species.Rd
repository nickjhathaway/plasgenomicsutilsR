% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensembl.R
\name{ensembl_species}
\alias{ensembl_species}
\title{Plasmodium genomes in an Ensembl Protists release}
\usage{
ensembl_species(release = ENSEMBL_PROTISTS_RELEASE, pattern = NULL)
}
\arguments{
\item{release}{Ensembl Protists release number. Defaults to \link{ENSEMBL_PROTISTS_RELEASE}.}

\item{pattern}{Optional regular expression, matched against the species name -- e.g.
\code{"vivax"} for the \emph{P. vivax} genomes.}
}
\value{
A tibble of \code{species} (the name to pass on), \code{assembly} (the token that appears in
the filenames) and \code{collection} (the FTP collection directory the genome sits in, \code{""}
for the genomes Ensembl gives a top-level directory).
}
\description{
The names \code{\link[=ensembl_gff_url]{ensembl_gff_url()}} and \code{\link[=ensembl_genome_url]{ensembl_genome_url()}} accept, with the assembly each
carries in that release. The default release's table ships with the package; any other
release is read from the Ensembl FTP site once per session.
}
\examples{
ensembl_species()

# the several assemblies of one species
ensembl_species(pattern = "ovale")
}
\seealso{
\code{\link[=ensembl_gff_url]{ensembl_gff_url()}}, \code{\link[=ensembl_genome_url]{ensembl_genome_url()}}
}
