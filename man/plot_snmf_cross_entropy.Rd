% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pop_structure.R
\name{plot_snmf_cross_entropy}
\alias{plot_snmf_cross_entropy}
\title{Cross-entropy elbow plot for choosing K}
\usage{
plot_snmf_cross_entropy(
  x,
  K = NULL,
  stat = c("min", "mean"),
  show_range = TRUE,
  best_k = NULL,
  point_size = 2.4,
  line_width = 0.6
)
}
\arguments{
\item{x}{An \code{\link[=run_snmf]{run_snmf()}} result, or a table from \code{\link[=snmf_cross_entropy]{snmf_cross_entropy()}}.}

\item{K}{Candidate K values (defaults to the fitted range).}

\item{stat}{Which summary the line follows, and which the red marker minimises:
\code{"min"} (default, as in \code{\link[=snmf_best_k]{snmf_best_k()}}) or \code{"mean"}. See \code{\link[=snmf_best_k]{snmf_best_k()}} for why
\code{"min"} is the default and when \code{"mean"} is worth asking for.}

\item{show_range}{Draw the replicate min-max band (default \code{TRUE}).}

\item{best_k}{K to mark in red; \code{NULL} (default) marks the K minimising \code{stat}, \code{NA}
marks none.}

\item{point_size, line_width}{Point and line sizes.}
}
\value{
A ggplot object.
}
\description{
Cross-entropy against K, so the elbow (or the absence of one) is visible. The line
follows \code{stat}, and \code{show_range = TRUE} adds the replicate min-max band -- a wide band
means the replicates disagreed and that K is not reproducible.
}
\examples{
\dontrun{
fit <- run_snmf(geno, K = 1:10, rep = 10)
snmf_cross_entropy(fit)          # the numbers
plot_snmf_cross_entropy(fit)     # the elbow
}
}
