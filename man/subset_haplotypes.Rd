% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ihs.R
\name{subset_haplotypes}
\alias{subset_haplotypes}
\title{Keep only some of the haplotypes}
\usage{
subset_haplotypes(x, samples = NULL, meta = NULL, ...)
}
\arguments{
\item{x}{A \code{\link[=parasite_haplotypes]{parasite_haplotypes()}} object.}

\item{samples}{Optional sample ids to keep.}

\item{meta}{Metadata to match \code{...} against; defaults to the object's own.}

\item{...}{Metadata filters as \code{column = values}, e.g. \code{region = "Southwest"}. A value that
no sample has is an error rather than a silently empty result.}
}
\value{
\code{x} with fewer haplotypes; \code{print()} reports the restriction.
}
\description{
A \code{\link[=parasite_haplotypes]{parasite_haplotypes()}} object restricted to certain samples or metadata groups, for when a
scan or an EHH curve is only worth reading within one part of the cohort -- a mutation that
segregates in a single region, say, where pooling everything buries it.
}
\details{
Metadata columns are matched the way \link{PopStructure}'s \verb{$subset()} does, \code{column = values},
and a column takes several values:

\if{html}{\out{<div class="sourceCode">}}\preformatted{subset_haplotypes(hap, region = c("North", "Southwest"))
}\if{html}{\out{</div>}}

The \strong{SNP panel is left alone}: the same columns, the same MAF and missingness filtering that
built them. That is deliberate, so two subsets stay comparable to each other and to the whole
-- and it costs nothing for haplotype work, since a SNP that is monomorphic within the subset
is dropped when the scan or curve is computed anyway. Rebuild with \code{parasite_haplotypes()} on
a subset \link{PopStructure} instead when you want the filtering itself redone within the group.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
hap <- parasite_haplotypes(ps, maf = 0.05)
subset_haplotypes(hap, country = "Ghana")
}
\seealso{
\code{\link[=parasite_haplotypes]{parasite_haplotypes()}}, \code{\link[=plot_ehh]{plot_ehh()}}, \code{\link[=run_ihs]{run_ihs()}}
}
