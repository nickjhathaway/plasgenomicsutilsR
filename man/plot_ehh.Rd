% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ehh.R
\name{plot_ehh}
\alias{plot_ehh}
\title{EHH decay around one SNP}
\usage{
plot_ehh(
  x,
  focal,
  group = NULL,
  span = 50000,
  min_haplotypes = 10,
  polarized = FALSE,
  limehh = 0.05,
  genes = NULL,
  gene_track = NULL,
  gene_label_angle = 0,
  colours = NULL,
  show_freq = TRUE,
  freq_position = c("topleft", "topright", "bottomleft", "bottomright"),
  reference = DEFAULT_REFERENCE
)
}
\arguments{
\item{x}{A \code{\link[=parasite_haplotypes]{parasite_haplotypes()}} object, or a \link{PopStructure} (haplotypes are then built
with \code{parasite_haplotypes()} defaults, which is worth doing yourself when the Fws or MAF
cutoffs matter).}

\item{focal}{The SNP to measure from: a \code{chr:pos} id, a bare position, or a gene name from
\code{genes}. A gene holding several SNPs resolves to the one with the most balanced alleles,
reported in a message -- name a \code{chr:pos} to pick a particular mutation.}

\item{group}{Optional metadata column; one panel per level. \code{NULL} (default) pools every
haplotype.}

\item{span}{How far either side of the focal SNP to draw, in base pairs (default 50 kb).
One value is symmetric, two are the left and the right (named \code{left} / \code{right} if you
like), as elsewhere in the package.}

\item{min_haplotypes}{Skip a group with fewer haplotypes than this (default 10). EHH from a
handful of haplotypes is mostly noise.}

\item{polarized}{Treat the alleles as ancestral / derived (default \code{FALSE}, matching
\code{\link[=run_ihs]{run_ihs()}} on unpolarized calls, where they are simply the two states).}

\item{limehh}{Stop each curve once EHH falls below this (rehh's \code{limehh}, default 0.05).}

\item{genes}{Gene table for the track and for resolving \code{focal} (e.g. \link{PF3D7_GENES}).}

\item{gene_track}{Draw the gene track underneath (default \code{FALSE}). \code{genes} is usually
supplied only to resolve \code{focal}, and an EHH window is wide enough that a full annotation
would crowd a hundred names under it, so this is opt-in.}

\item{gene_label_angle}{Rotation for the gene names, in degrees.}

\item{colours}{Named colours for \code{reference} / \code{alternate}.}

\item{show_freq}{Note each panel's haplotype count and allele frequencies inside it
(default \code{TRUE}); \code{FALSE} leaves the panel clean.}

\item{freq_position}{Which corner that note sits in: \code{"topleft"} (default), \code{"topright"},
\code{"bottomleft"} or \code{"bottomright"}. The top corners are usually clear, since EHH is 1 at
the focal SNP and both curves have flattened along the bottom by the window's edges.}

\item{reference}{Reference id, used when \code{focal} names a whole chromosome.}
}
\value{
A patchwork of the curves over the gene track, or a plain ggplot without one.
}
\description{
Extended haplotype homozygosity either side of a focal SNP, one curve per allele: how far
the haplotype carrying each allele stays identical as you walk away from it. A sweep shows
as one allele holding EHH near 1 far past the point where the other has decayed -- the
picture behind a single point on an \code{\link[=run_ihs]{run_ihs()}} scan.
}
\details{
The alleles are the two states at the focal SNP itself, so this is the mutant-versus-
reference comparison without needing the SNPs annotated: \code{reference} is the allele coded 0
and \code{alternate} the one coded 1. \code{group} adds a panel per metadata group; without it every
haplotype is pooled, which is usually what you want first, since EHH knows nothing about
population structure and a group with few carriers gives a ragged curve.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
hap <- parasite_haplotypes(ps, maf = 0.05)
# the example panel is sparse, so measure from a SNP it actually has
plot_ehh(hap, hap$map$snp_id[10], span = 5e5)
}
