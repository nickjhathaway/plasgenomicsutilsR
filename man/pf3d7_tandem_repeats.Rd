% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tandem_repeats.R
\name{pf3d7_tandem_repeats}
\alias{pf3d7_tandem_repeats}
\title{Tandem repeats of the Pf3D7 reference genome}
\usage{
pf3d7_tandem_repeats()
}
\value{
The \code{\link[=tandem_repeats]{tandem_repeats()}} table for Pf3D7: about 272,000 rows.
}
\description{
Every short tandem repeat found in the \emph{P. falciparum} 3D7 reference, as a
\code{\link[=tandem_repeats]{tandem_repeats()}} table, so nobody has to run the finders again. Feed it to
\code{\link[=tandem_repeats_to_avoid]{tandem_repeats_to_avoid()}} and subtract the result from a set of targets with
\code{\link[=bed_subtract]{bed_subtract()}}.
}
\details{
Built once (April 2021) from \code{Pf3D7.fasta} (PlasmoDB, the assembly \link{PF3D7_GENES} is
annotated on) by the union of two finders:
\itemize{
\item Tandem Repeats Finder (Benson 1999), \verb{trf Pf3D7.fasta 2 7 7 80 10 50 1000 -f -d -m -h}
-- match 2, mismatch 7, indel 7, match / indel probabilities 80 / 10, minimum score 50,
maximum period 1000 -- converted to BED with
\verb{elucidator TandemRepeatFinderOutputToBed};
\item \verb{elucidator findSimpleTandemRepeatLocations --maxRepeatUnitSize 10}, an exhaustive
search for perfect repeats of units up to 10 bp, which \code{trf} is inconsistent about
around short simple repeats.
}

\code{copies} is the finder's own copy number, which for \code{trf} reflects an alignment with
indels and so is not always \code{width / unit_size}. All 16 sequences are covered, the
apicoplast and mitochondrion included.

The table is stored compactly under \code{inst/extdata} (about 1.1 MB) and expanded on first
use; later calls return the cached copy. \code{data-raw/PF3D7_TANDEM_REPEATS.R} rebuilds it.
}
\examples{
tr <- pf3d7_tandem_repeats()
table(pmin(tr$period, 5), useNA = "ifany")     # 1..4, and "5" for anything longer
}
\references{
Benson G (1999). Tandem repeats finder: a program to analyze DNA sequences.
\emph{Nucleic Acids Research} 27(2):573-580.
}
\seealso{
\code{\link[=tandem_repeats_to_avoid]{tandem_repeats_to_avoid()}}, \code{\link[=bed_subtract]{bed_subtract()}}
}
