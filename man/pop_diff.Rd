% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jost_d.R
\name{pop_diff}
\alias{pop_diff}
\title{Population differentiation between metadata groups, per SNP}
\usage{
pop_diff(
  x,
  group = NULL,
  statistic = c("jost_d", "gst_hedrick", "fst"),
  meta = NULL,
  clamp = TRUE,
  genotype = NULL
)
}
\arguments{
\item{x}{A \link{PopStructure} (uses its full genotype matrix for the active samples) or a
genotype matrix (samples x SNPs, 0/1/2, \code{NA}).}

\item{group}{Metadata column (for a \code{PopStructure}) or a per-sample grouping vector
(for a matrix).}

\item{statistic}{\code{"jost_d"} (Jost's D, default), \code{"gst_hedrick"} (Hedrick's
standardized G'st), or \code{"fst"} (Hudson's Fst); all in [0, 1]. Plain Nei's Gst is not
offered -- it is strongly deflated when within-group diversity is high (typical
genome-wide in \emph{P. falciparum}); its standardized form G'st is provided instead.}

\item{meta}{When \code{x} is a matrix, an optional data frame with a \code{sample} column plus
\code{group}; otherwise \code{group} is a vector aligned to the matrix rows.}

\item{clamp}{Clamp small negative estimates to 0 (default \code{TRUE}).}

\item{genotype}{Optional genotype matrix (samples x SNPs) or \code{\link[=load_genotypes]{load_genotypes()}} list to
use \strong{instead} of a \code{PopStructure}'s stored matrix -- pass the \strong{full, unpruned}
set here (e.g. \code{load_genotypes(vcf, prune = FALSE)}) so differentiation is measured on
every SNP while PCA/UMAP keep using the pruned matrix. Ignored when \code{x} is a matrix.}
}
\value{
A \code{pop_diff} object: a list with \code{D} (a SNP x pair matrix of the statistic),
\code{snp}, \code{groups}, \code{pairs}, \code{statistic}, and the group \code{freqs}.
}
\description{
Computes a per-SNP differentiation statistic for every pair of \code{group} levels, from
alt-allele dosages. Run it on the \strong{full, unpruned} genotypes -- LD-pruning removes
the differentiating SNPs you want here. Feed the result to \code{\link[=pop_diff_matrix]{pop_diff_matrix()}} /
\code{\link[=plot_diff_heatmap]{plot_diff_heatmap()}} for a group summary, or \code{\link[=top_differentiating_snps]{top_differentiating_snps()}} for markers.
}
\examples{
ps <- example_pop_structure("africa", umap = FALSE)
pd <- pop_diff(ps, group = "region")     # Jost's D by default
dim(pd$D)                                # SNPs x group pairs
}
\references{
Jost, L. (2008) G_ST and its relatives do not measure differentiation.
\emph{Molecular Ecology} 17, 4015-4026. \doi{10.1111/j.1365-294X.2008.03887.x}

Nei, M. & Chesser, R. K. (1983) Estimation of fixation indices and gene diversities.
\emph{Annals of Human Genetics} 47, 253-259. \doi{10.1111/j.1469-1809.1983.tb00993.x}

Hedrick, P. W. (2005) A standardized genetic differentiation measure.
\emph{Evolution} 59, 1633-1638. \doi{10.1111/j.0014-3820.2005.tb01814.x}

Hudson, R. R., Slatkin, M. & Maddison, W. P. (1992) Estimation of levels of gene flow
from DNA sequence data. \emph{Genetics} 132, 583-589. \doi{10.1093/genetics/132.2.583}

Bhatia, G., Patterson, N., Sankararaman, S. & Price, A. L. (2013) Estimating and
interpreting F_ST: the impact of rare variants. \emph{Genome Research} 23, 1514-1521.
\doi{10.1101/gr.154831.113}
}
