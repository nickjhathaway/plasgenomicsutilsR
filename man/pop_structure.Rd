% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pop_structure.R
\name{pop_structure}
\alias{pop_structure}
\title{Compute PCA and UMAP from a genotype matrix}
\usage{
pop_structure(
  geno,
  samples = NULL,
  meta = NULL,
  n_pcs = 50,
  umap = TRUE,
  umap_pca = 30,
  n_neighbors = 15,
  min_dist = 0.1,
  seed = 42
)
}
\arguments{
\item{geno}{A genotype matrix (samples x SNPs, 0/1/2, \code{NA} allowed) or the list
returned by \code{\link[=load_genotypes]{load_genotypes()}}.}

\item{samples}{Sample ids (defaults to the genotype list's \code{sample.id}, or matrix
row names).}

\item{meta}{Optional per-sample metadata: a data frame with a \code{sample} column
(plus e.g. \code{region}, \code{country}) used for colouring.}

\item{n_pcs}{Number of PCs to summarise (variance explained).}

\item{umap}{Compute a UMAP embedding.}

\item{umap_pca, n_neighbors, min_dist}{UMAP parameters (defaults 30 / 15 / 0.1);
\code{n_neighbors} is clamped to the sample count.}

\item{seed}{Random seed for UMAP.}
}
\value{
A \code{pop_structure} object (a list with \code{samples}, \code{pca} scores,
\code{pca_var}, \code{umap}, \code{meta}).
}
\description{
Mean-imputes missing genotypes, runs PCA (\code{\link[stats:prcomp]{stats::prcomp()}}) and, optionally, a
UMAP embedding (\pkg{uwot}) with PCA initialisation. Returns a \code{pop_structure}
object the \verb{plot_*()} functions read.
}
