% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ehh.R
\name{ehh_candidates}
\alias{ehh_candidates}
\title{The SNPs an EHH plot had to choose between}
\usage{
ehh_candidates(
  x,
  focal,
  group = NULL,
  genes = NULL,
  min_haplotypes = 10,
  reference = DEFAULT_REFERENCE
)
}
\arguments{
\item{x}{A \code{\link[=parasite_haplotypes]{parasite_haplotypes()}} object, or a \link{PopStructure}.}

\item{focal}{As in \code{\link[=plot_ehh]{plot_ehh()}}: a \code{chr:pos} id, a bare position, or a gene name in \code{genes}.}

\item{group}{Optional metadata column, for the per-group columns.}

\item{genes}{Gene table used to resolve \code{focal} (e.g. \link{PF3D7_GENES}).}

\item{min_haplotypes}{Groups smaller than this are left out, as in \code{\link[=plot_ehh]{plot_ehh()}}.}

\item{reference}{Reference id, used when \code{focal} names a whole chromosome.}
}
\value{
A tibble of \code{snp_id}, \code{chr}, \code{pos}, \code{maf}, \code{n_hap} and \code{chosen}, plus the per-group
columns when \code{group} is given. The chosen SNP is the first row; the rest follow by
descending \code{maf}.
}
\description{
\code{\link[=plot_ehh]{plot_ehh()}} measures decay from a single focal SNP, and a gene usually holds many. It takes
the one with the most balanced alleles and reports which -- this returns the whole shortlist
it chose from, chosen SNP first, so the decision is visible and can be overridden by passing
a \code{chr:pos} back to \code{focal}.
}
\details{
\code{maf} is the metric that decides it: minor-allele frequency across the haplotypes, which
\code{\link[=plot_ehh]{plot_ehh()}} maximises because EHH measured from a near-singleton is a flat line at 1 that
says nothing about a sweep.

With \code{group}, each group gets a \verb{maf_<group>} column and \code{n_groups_variable} counts the groups
the SNP actually varies in. That is the other way a panel comes back empty: a SNP can be well
balanced overall and monomorphic inside one group, which drops that group's curve with "the
focal SNP is not variable in it". Sorting on \code{n_groups_variable} finds a SNP that works
everywhere, when one exists.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
hap <- parasite_haplotypes(ps, maf = 0.05)
ehh_candidates(hap, "pfcrt", genes = PF_EXAMPLE_DRUG_GENES)
}
\seealso{
\code{\link[=plot_ehh]{plot_ehh()}}
}
