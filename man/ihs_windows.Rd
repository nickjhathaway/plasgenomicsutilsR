% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ihs.R
\name{ihs_windows}
\alias{ihs_windows}
\title{Windowed summary of an iHS scan}
\usage{
ihs_windows(
  scan,
  window = 50000,
  step = NULL,
  threshold = 2,
  min_snps = 10,
  metric = "ihs"
)
}
\arguments{
\item{scan}{A \code{\link[=run_ihs]{run_ihs()}} result, or any table with \code{chr}, \code{pos}, the \code{metric} column
and optionally \code{group}.}

\item{window}{Window width in base pairs.}

\item{step}{Distance between window starts; defaults to \code{window} (windows that tile
without overlapping). A smaller \code{step} slides the window and smooths the track, at the
cost of neighbouring windows sharing SNPs.}

\item{threshold}{The \code{abs(metric)} a SNP must exceed to be counted as extreme. The
conventional 2 for iHS.}

\item{min_snps}{Drop windows holding fewer scored SNPs than this. A window with three
SNPs can read 100\% and mean nothing; this is what keeps the sparse edges of the data
from becoming the tallest peaks.}

\item{metric}{Column to summarise (default \code{"ihs"}); its magnitude is used, so an
unpolarized scan needs no other handling.}
}
\value{
A tibble with \code{group} (when the scan has one), \code{chr}, \code{start}, \code{end}, \code{pos} (the
window midpoint), \code{n_snps}, \code{n_extreme}, \code{frac_extreme} and \code{max_abs}.
}
\description{
The fraction of SNPs in each window whose \code{abs(ihs)} exceeds \code{threshold} -- the summary
iHS is normally read through, rather than SNP by SNP.
}
\details{
Per-SNP iHS is a high-variance statistic: each score rests on one focal SNP's EHH decay,
so neighbouring SNPs in the same haplotype disagree freely and a genome-wide plot of
them looks like grass. A sweep does not raise one SNP, it raises a \emph{run} of them, and
counting the run is both what the original description of the statistic proposed and
what makes the result comparable to any other windowed track -- an IBD fraction, say.

It also sidesteps the per-SNP p-value, which \pkg{rehh} computes from a normal
approximation and does not correct for multiple testing: a nominal \code{p < 0.01} line is
crossed by 1\% of a neutral genome, so on a whole-genome scan it marks a tail, not a
finding.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
hap <- parasite_haplotypes(ps, maf = 0.05)
ihs_windows(run_ihs(hap, group = "country"), window = 1e5, min_snps = 3)
}
\references{
Voight, B. F., Kudaravalli, S., Wen, X. & Pritchard, J. K. (2006) A map of recent
positive selection in the human genome. \emph{PLoS Biology} 4, e72.
\doi{10.1371/journal.pbio.0040072}
}
\seealso{
\code{\link[=run_ihs]{run_ihs()}}, \code{\link[=plot_ihs]{plot_ihs()}}, \code{\link[=plot_ibd_tugofwar]{plot_ibd_tugofwar()}}
}
