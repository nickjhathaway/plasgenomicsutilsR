% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colour_blindness.R
\name{colour_blind_distance}
\alias{colour_blind_distance}
\alias{color_blind_distance}
\title{Smallest colour difference within (or between) palettes, under colour blindness}
\usage{
colour_blind_distance(
  x,
  y = NULL,
  vision = c("normal", "deutan", "protan", "tritan")
)

color_blind_distance(
  x,
  y = NULL,
  vision = c("normal", "deutan", "protan", "tritan")
)
}
\arguments{
\item{x}{A vector of colours.}

\item{y}{Optional second vector. When given, distances are measured \emph{between} \code{x} and
\code{y} (e.g. region colours against ancestry-component colours) rather than within \code{x}.}

\item{vision}{Which vision types to report, from \code{"normal"}, \code{"deutan"}, \code{"protan"},
\code{"tritan"}.}
}
\value{
A named numeric vector, one worst-case distance per vision type.
}
\description{
The worst-case pair: for one palette, the two colours a viewer would find hardest to
tell apart; for two, the closest a colour in one gets to a colour in the other. Reported
as CIEDE2000 distance -- roughly, below ~5 is a confusion risk, above ~10 is comfortable.
}
\examples{
colour_blind_distance(color_palette(6))
colour_blind_distance(RColorBrewer::brewer.pal(6, "Paired"))  # deutan pairs collapse
}
