% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diversity.R
\name{tajima_d_pvalue}
\alias{tajima_d_pvalue}
\title{p-value for a Tajima's D}
\usage{
tajima_d_pvalue(D, n, S, method = c("beta", "normal"))
}
\arguments{
\item{D}{Tajima's D.}

\item{n}{Number of sampled gene copies.}

\item{S}{Number of segregating sites.}

\item{method}{\code{"beta"} (Tajima's approximation over D's attainable range) or \code{"normal"}.}
}
\value{
A two-sided p-value, or \code{NA} when D is undefined.
}
\description{
Two-sided significance against the standard neutral model, either by Tajima's own beta
approximation (the default, and what \pkg{pegas} reports as \code{Pval.beta}) or by treating
D as standard normal (\code{Pval.normal}). Both agree with \pkg{pegas} to floating point.
}
\details{
\strong{Read it as conservative, and low-powered.} The variance term behind D assumes \emph{no
recombination}, which is the most variance the statistic can have; a heavily
recombining organism like \emph{P. falciparum} therefore under-rejects. Measured on a real
249-sample cohort, per-gene D reached \code{p < 0.05} for only 1-2.5\% of genes -- less than
chance would give -- while 72\% of genes had negative D. Per gene there are usually only
a handful of segregating sites, which leaves almost no power.

What the test asks is also not quite what a scan wants. It tests the standard neutral
model as a whole, so a population that has expanded gives negative D genome-wide and a
small p-value says "not a constant-size neutral population", not "this gene is under
selection". For shortlisting loci, rank on \code{tajima_d} itself, or use the
\code{tajima_percentile} that \code{\link[=pop_diversity]{pop_diversity()}} reports, and keep the p-value as context.
}
\examples{
tajima_d_pvalue(-2.1, n = 40, S = 25)
}
\references{
Tajima, F. (1989) Statistical method for testing the neutral mutation
hypothesis by DNA polymorphism. \emph{Genetics} 123, 585-595.
\doi{10.1093/genetics/123.3.585}
}
