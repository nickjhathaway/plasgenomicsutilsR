% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ibd_unrelated_set.R
\name{ibd_unrelated_set}
\alias{ibd_unrelated_set}
\title{The largest mutually unrelated set of samples}
\usage{
ibd_unrelated_set(
  pairs,
  weight = NULL,
  max_ibd = 0.01,
  samples = NULL,
  restarts = 200L,
  seed = 1L,
  add_meta_cols = NULL,
  meta = NULL
)
}
\arguments{
\item{pairs}{The per-pair table from \verb{plasgenomicsutils ibd_fraction_and_snp_density}
(\verb{*.pair_ibd_fraction.tsv.gz}): a path, a data frame, or an \link{IbdResults} carrying one.}

\item{weight}{Column holding the fraction. Defaults to \code{ibd_fraction_accessible}, the
callable-genome denominator; \code{ibd_fraction_full_genome} divides by the whole genome
instead, so it reads lower for the same pair.}

\item{max_ibd}{Allow no pair in the set to share more than this fraction (default \code{0.01}).}

\item{samples}{Optional subset of samples to keep.}

\item{restarts}{Randomised restarts to try (default \code{200L}); the first is deterministic.}

\item{seed}{Seed for tie-breaking, so a run reproduces. The caller's random state is
restored afterwards.}

\item{add_meta_cols}{Metadata columns to carry onto each row, by sample. A sample missing
from \code{meta} gets \code{NA}; factor columns keep their level order, so a region stays in map
order rather than turning alphabetical.}

\item{meta}{Sample metadata for \code{add_meta_cols}; taken from the \link{IbdResults} when \code{pairs}
is one, so it only needs giving alongside a plain table.}
}
\value{
A tibble with one row per analysed sample, selected first then by name:
\describe{
\item{\code{sample}}{the sample.}
\item{\code{selected}}{whether it is in the unrelated set.}
\item{\code{n_links}}{how many samples it shares more than \code{max_ibd} with.}
\item{\code{max_ibd}}{the most it shares with any of them, \code{NA} when none.}
\item{\code{blocked_by}}{for an unselected sample, a selected sample it conflicts with, so
the result can be audited; \code{NA} when selected.}
}
plus one column for each of \code{add_meta_cols}. Carries a \code{set_size} attribute and a
\code{max_ibd_within_set} attribute -- the most any two selected samples actually share, which
is at most \code{max_ibd} and says how unrelated the set really is.
}
\description{
The complement of \code{\link[=ibd_pair_clusters]{ibd_pair_clusters()}}: instead of who is related to whom, the largest
group found in which \strong{no} pair shares more than \code{max_ibd} of the genome. Use it to pick
founder haplotypes for simulation, to de-duplicate a cohort before statistics that assume
independent samples (allele frequencies, \eqn{F_{ST}}, PCA, admixture), or to report how
much independent signal a cohort really carries.
}
\details{
Keeping one sample per single-linkage cluster is \strong{not} the same thing, and is usually
much worse. The two coincide only when clusters are cliques; at a low cutoff the
relatedness graph is typically one sprawling component, so one-per-cluster returns a
handful of samples where this returns many.

Finding the true maximum is NP-hard, so this is a heuristic: greedy minimum-degree with
randomised restarts, then a pass that adds back any sample that turns out to fit. The
result is always \strong{valid} (no pair inside it exceeds \code{max_ibd}) and \strong{maximal} (no
sample could be added), but is not guaranteed to be the largest such set that exists.
More \code{restarts} searches harder. Samples sharing with nobody are always selected.

\code{weight} should hold a fraction computed from \strong{length-filtered} IBD segments. A raw
posterior site fraction carries a background floor -- routinely a median near 0.015 across
unrelated pairs -- so a cutoff near 1\% on one of those counts almost every pair as related
and collapses the set to nearly nothing.

Set size falls away steeply as \code{max_ibd} tightens, so scan a few cutoffs rather than
trusting one. The cutoff is an inclusive ceiling: a pair sharing exactly \code{max_ibd} may
stay, matching the strictly-greater comparison \code{\link[=plot_ibd_pair_network]{plot_ibd_pair_network()}} draws with.
}
\examples{
\dontrun{
u <- ibd_unrelated_set(ibd_all, max_ibd = 0.01)
sum(u$selected)
attr(u, "max_ibd_within_set")   # <= 0.01 by construction

# how the set shrinks as the cutoff tightens
sapply(c(0.005, 0.01, 0.02, 0.05),
       function(t) sum(ibd_unrelated_set(ibd_all, max_ibd = t)$selected))

# founder haplotypes for a simulation
writeLines(u$sample[u$selected], "founders.txt")

# does the unrelated set still span every site?
u <- ibd_all$ibd_unrelated_set(max_ibd = 0.01, add_meta_cols = "region")
table(subset(u, selected)$region)
}
}
\seealso{
\code{\link[=ibd_pair_clusters]{ibd_pair_clusters()}} for the related-to-whom view, \code{\link[=ibd_pair_links]{ibd_pair_links()}} for the
edges themselves, \code{\link[=plot_ibd_pair_network]{plot_ibd_pair_network()}} for the picture.
}
