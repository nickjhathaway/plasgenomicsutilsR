% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pop_structure.R
\name{merge_genotypes}
\alias{merge_genotypes}
\title{Merge loaded genotype sets into one}
\usage{
merge_genotypes(
  ...,
  samples = c("identical", "common"),
  on_overlap = c("error", "first", "last")
)
}
\arguments{
\item{...}{Two or more \code{\link[=load_genotypes]{load_genotypes()}} results.}

\item{samples}{\code{"identical"} (default) requires the same sample set in every input;
\code{"common"} keeps the intersection.}

\item{on_overlap}{What to do when the same \code{chr:pos} appears in more than one input:
\code{"error"} (default), or \code{"first"} / \code{"last"} to keep that input's calls.}
}
\value{
A list shaped like \code{\link[=load_genotypes]{load_genotypes()}}'s: \code{genotype}, \code{sample.id}, \code{snp.id},
\code{allele}, \code{pruned}, \code{positions}, \code{variants}. \code{pruned} is \code{TRUE} only when every input
was pruned -- a SNP added to a pruned set never faced pruning itself, which is usually
the reason for adding it.
}
\description{
Column-binds the genotype matrices from two or more \code{\link[=load_genotypes]{load_genotypes()}} results, keyed on
the \code{chr:pos} names the loader assigns.
}
\details{
Merging here rather than upstream is the point. Two callers write different INFO and
FORMAT fields, and reconciling them so \verb{bcftools merge} will accept both is real work
that changes nothing about the answer -- by the time a callset is a dosage matrix, all
that survives is the calls themselves, and those combine by position. So a variant that
had to be re-called separately, because the original callset arrived already filtered and
the caller's own output was gone, can be added without rebuilding the VCF it came from.

What is checked, because these are the ways a merge is silently wrong:
\itemize{
\item \strong{\code{allele}} must agree. One set counting alternate alleles and another counting
reference alleles are indistinguishable after the fact, and mixing them inverts the
dosages of whichever half disagrees.
\item \strong{Samples} must be the same set, in whatever order; the columns are realigned to the
first set's order. \code{samples = "common"} intersects instead, saying how many it dropped.
\item \strong{Positions} must not collide. The same \code{chr:pos} from two callers is two answers to
one question, and picking silently would hide the disagreement -- name which one wins
with \code{on_overlap}.
}

The result is sorted by chromosome and position, so it reads like a callset rather than
like the order the files happened to be given in. \code{snp.id} is renumbered: SNPRelate's ids
are per-file integers starting at 1, so they collide across files and mean nothing once
merged -- \code{chr:pos} is the identity that survives.
}
\examples{
\dontrun{
main <- load_genotypes("cohort.vcf.gz", gds = "cohort.gds", prune = TRUE)
extra <- load_genotypes("one_recalled_snp.vcf.gz", gds = "extra.gds", prune = FALSE)
both <- merge_genotypes(main, extra)
}
}
\seealso{
\code{\link[=load_genotypes]{load_genotypes()}}
}
