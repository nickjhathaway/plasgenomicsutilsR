% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locus_ibs_by_allele.R
\name{locus_ibs_by_allele}
\alias{locus_ibs_by_allele}
\title{Do two populations carry the same haplotype at a locus? An allele-split IBS test}
\usage{
locus_ibs_by_allele(
  x,
  locus,
  allele,
  carrier = NULL,
  reference = NULL,
  group_a,
  group_b,
  group = NULL,
  meta = NULL,
  n_snps = 53L,
  span_tol = 2,
  exclude_focal_chr = TRUE,
  min_sites = 10L,
  drop_ibd = NULL,
  map = NULL
)
}
\arguments{
\item{x}{Genotypes: a \link{parasite_haplotypes} object, a \code{load_genotypes()} list (its
\code{allele-index} encoding, which keeps missingness), or a bare integer matrix
(samples x SNPs) with \code{map}. Missing calls must be \code{NA}; an imputed panel is warned
about, since imputation would be read as agreement. For IBS and IBD to be comparable the
calls should be the ones the IBD run used (hmmibd-rs dominant-allele from \code{FORMAT/AD});
the allele-index panel is \code{GT}-derived and agrees with it wherever calls are unmixed.}

\item{locus}{The focal locus: a one-row data frame or list with \code{chr}, \code{start}, \code{end}
(0-based half-open, used for the \code{drop_ibd} overlap) and optional \code{centre} (the focal
window is centred here; defaults to the interval midpoint). A multi-row interval frame is
collapsed to its span.}

\item{allele}{The variant to split on: a metadata column name or a named \code{sample -> state}
vector (as \code{\link[=allele_states]{allele_states()}} returns).}

\item{carrier, reference}{State sets (matched with \code{\%in\%}), as in
\code{\link[=carrier_genome_wide_relatedness]{carrier_genome_wide_relatedness()}}.}

\item{group_a, group_b}{The two populations, each a set of \code{group} values. The contrast is
between them; a sample in neither is unused.}

\item{group}{Metadata column defining the populations (default \code{"region"}, or the object's
own group column when it carries one).}

\item{meta}{Sample metadata (\code{sample} plus \code{group} and the \code{allele} column). Taken from a
\code{parasite_haplotypes}' \verb{$meta} when not given.}

\item{n_snps}{Window size in SNPs (default \code{53}).}

\item{span_tol}{Keep null windows whose physical span is within this factor of the focal
window's span, either side (default \code{2}; \code{NULL} or \code{0} to not bound).}

\item{exclude_focal_chr}{Drop the focal chromosome from the null (default \code{TRUE}).}

\item{min_sites}{Comparable sites a pair needs in a window to contribute (default \code{10}).}

\item{drop_ibd}{Optionally also report the statistic with pairs already IBD across the
locus removed from both strata, which separates "a lineage moved across the border" from
"the carrier populations share a background". Pass an \link{IbdResults} (its blocks name the
IBD pairs), or \code{TRUE} to use \code{x} if it is one. Adds a second result row.}

\item{map}{When \code{x} is a bare genotype matrix, a data frame of \code{chr}/\code{pos} with one row per
SNP column giving each marker's position; ignored when \code{x} carries its own \verb{$map}.}
}
\value{
A tibble, one row per pair set (\code{"all pairs"}, and \code{"IBD pairs dropped"} when
\code{drop_ibd} is given), carrying the per-window null on the \code{null} attribute:
\describe{
\item{\code{locus}, \code{pairs}}{the locus label and which pair set the row is.}
\item{\code{n_a_car}, \code{n_b_car}, \code{n_a_ref}, \code{n_b_ref}}{samples in each population x stratum.}
\item{\code{cross_car}, \code{cross_ref}}{cross-population mean IBS in each stratum.}
\item{\code{a_car}, \code{b_car}}{within-population carrier cohesion, each population on its own.}
\item{\code{d_ind}}{\code{cross_car - cross_ref}, the statistic.}
\item{\code{null_n}, \code{null_mean}, \code{null_sd}}{the matched-window null.}
\item{\code{z}}{\code{(d_ind - null_mean) / null_sd}.}
\item{\code{p_two}}{two-sided empirical p against the null (a floor when small).}
\item{\code{focal_span_kb}, \code{span_tol}, \code{n_snps}}{the window the null was matched to.}
}
}
\description{
Measures whether the carriers of a variant in two populations are drawn from a common
haplotype pool, using pairwise identity (IBS) rather than called IBD, so it never depends
on the caller reaching the cross-population pairs the question turns on. The statistic is a
difference of differences -- cross-population carrier-vs-carrier identity minus
reference-vs-reference identity -- scored against a null of SNP-count-matched windows tiled
off the focal chromosome. It answers "one haplotype spreading across the boundary" versus
"independent origins", which the IBD block-extension family (the iHS-shaped, within-
population question; see \code{\link[=ibd_block_extension_by_allele]{ibd_block_extension_by_allele()}}) cannot settle on its own.
}
\section{What it does and does not say}{

A positive, significant result means the carrier chromosomes in the two populations are
drawn from a common pool rather than independently derived. It does \strong{not} count how many
backgrounds that pool holds; pair it with a tabulation of the other coding variants each
carrier also carries. A negative difference means the carriers are \emph{less} alike across the
boundary than the reference samples are -- independent backgrounds -- even where each
population's carriers cluster tightly on their own (\code{a_car}, \code{b_car}).
}

\section{Matched windows}{

Windows are matched on \strong{SNP count} (\code{n_snps}), not physical width, because IBS is a mean
over a window's SNPs and its variance is set by that count. A physical-span bound
(\code{span_tol}) is layered on top to stop a count-matched window that happens to span far more
bp from inflating the null variance. The focal chromosome is excluded from the null so the
locus cannot seed its own baseline, and a pair needs \code{min_sites} comparable sites in a
window to contribute. With a few hundred windows the smallest achievable two-sided p is a
resolution floor, so read a p at the floor as "no matched window reached the observed
value" rather than as an exact probability.
}

\seealso{
\code{\link[=ibd_block_extension_by_allele]{ibd_block_extension_by_allele()}} for the within-population iHS-shaped contrast,
\code{\link[=carrier_genome_wide_relatedness]{carrier_genome_wide_relatedness()}} for the genome-wide relatedness control,
\code{\link[=parasite_haplotypes]{parasite_haplotypes()}} and \code{load_genotypes()} for the genotype inputs.
}
