% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jost_d.R
\name{plot_diff_heatmap}
\alias{plot_diff_heatmap}
\alias{plot_jost_d_heatmap}
\title{Triangle heatmap of group x group differentiation}
\usage{
plot_diff_heatmap(
  pd,
  stat = c("mean", "median", "top_mean", "max"),
  top = 0.05,
  cluster = TRUE,
  dendrogram = TRUE,
  triangle = TRUE,
  annotate = NULL,
  meta = NULL,
  annotate_colours = NULL,
  group_colours = NULL,
  label = FALSE,
  digits = 2,
  colors = c("white", "#fde0dd", "#fa9fb5", "#c51b8a", "#7a0177"),
  trans = "identity",
  base_size = 11
)

plot_jost_d_heatmap(pd, stat = "mean", ...)
}
\arguments{
\item{pd}{A \code{\link[=pop_diff]{pop_diff()}} / \code{\link[=jost_d]{jost_d()}} result.}

\item{stat, top}{Summary passed to \code{\link[=pop_diff_matrix]{pop_diff_matrix()}} (\code{"mean"}, \code{"median"},
\code{"top_mean"}, \code{"max"}; \code{top} for the top-percentile mean).}

\item{cluster}{Order groups by hierarchical clustering of the matrix (default \code{TRUE}),
which is what puts similar groups side by side. With \code{cluster = FALSE} the axes follow
the grouping column's levels instead -- its factor order if it has one (e.g. from
\code{PopStructure$set_levels()}), otherwise a natural sort. Annotation strips and the
dendrogram tips take their colours in level order either way, so a group keeps the
same colour whichever ordering is drawn.}

\item{dendrogram}{Draw the clustering dendrogram across the top (needs \code{cluster}).}

\item{triangle}{Show only the lower triangle (with the legend in the empty corner).}

\item{annotate}{One or more annotations drawn as colour strips above the columns:
a metadata column name (looked up in \code{meta}), a vector of several column names, a
named \code{group -> value} vector, or a named list mixing these (the names label the
strips/legends). Each strip aligns to the clustered column order.}

\item{meta}{Metadata data frame for resolving \code{annotate} column names.}

\item{annotate_colours}{Named list \code{annotation -> (value -> colour)} giving custom
colours per annotation (unlisted annotations get an automatic palette).}

\item{group_colours}{Optional named \code{group -> colour} vector used to colour the
dendrogram leaf tips (so the tree carries the same colours as the UMAP / admixture);
defaults to an automatic palette. A \code{PopStructure} passes its shared map here.}

\item{label}{Print the value in each cell (default \code{FALSE}; the text can distract).}

\item{digits}{Cell-label digits.}

\item{colors}{Fill ramp (low -> high differentiation).}

\item{trans}{Fill transform, e.g. \code{"identity"} (default) or \code{"sqrt"} to lift a scale
dominated by near-zero values.}

\item{base_size}{Base font size.}

\item{...}{For \code{plot_jost_d_heatmap()}, arguments forwarded to \code{plot_diff_heatmap()}.}
}
\value{
A ggplot (or \pkg{patchwork}) object.
}
\description{
A lower-triangle heatmap of a group summary (see \code{\link[=pop_diff_matrix]{pop_diff_matrix()}}), styled like the
drug-gene triangles: the fill legend sits in the empty upper triangle. Optionally adds
a clustering dendrogram across the top and a metadata annotation strip (e.g. colour
each site by its country).
}
