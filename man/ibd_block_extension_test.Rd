% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ibd_block_extension.R
\name{ibd_block_extension_test}
\alias{ibd_block_extension_test}
\title{Test whether IBD blocks are longer at a locus than the sharing pairs' own background}
\usage{
ibd_block_extension_test(
  x,
  loci = NULL,
  group = NULL,
  within = 0,
  sharing = c("overlap", "complete"),
  pairs = c("within", "all", "between"),
  min_ref_blocks = 1L,
  min_pairs = 5L,
  adjust = c("per_locus", "per_group", "all", "none"),
  keep_pair_ratios = TRUE,
  meta = NULL
)
}
\arguments{
\item{x}{An \link{IbdResults} built with \verb{blocks =} and \verb{meta =}.}

\item{loci}{Loci to test: gene names from the object's track, \code{NULL} for every gene in
it, or an interval data frame (\code{name}, \code{chr}/\code{chrom}, \code{start}, \code{end}, optionally
\code{gene_id}). Rows of an interval frame that share a name, a chromosome and a \code{gene_id}
are merged into one interval spanning them, so an \code{aa_intervals()} table for a marker
set can be passed as-is once it carries a \code{name}, while a gene track passed this way
stays one locus per gene even where a name repeats across chromosomes.}

\item{group}{Metadata column defining the groups. Defaults to the object's declared group
column, then to the first non-\code{sample} column of \code{meta}.}

\item{within}{Pad each locus by this many bp on both sides when deciding whether a
segment counts (default \code{0}). It applies under either \code{sharing}, so with padding
\code{"complete"} asks the segment to cover the padded interval.}

\item{sharing}{What a pair's segment must do at the locus to be counted, the same choice
\code{\link[=plot_ibd_network]{plot_ibd_network()}} offers:
\describe{
\item{\code{"overlap"}}{(default) the segment touches the locus anywhere, which is
\code{\link[=gene_ibd_pairs]{gene_ibd_pairs()}}'s rule: \code{start < end_locus & end > start_locus}. The pair shares
\emph{some} of the locus.}
\item{\code{"complete"}}{the segment spans the whole locus. Far stricter, and it changes
what the statistic measures -- see the note below on why locus width then matters.}
}}

\item{pairs}{Which sample pairs to count. \code{"within"} (default) uses only pairs whose two
samples share a group and reports a \code{group} column. \code{"between"} uses only cross-group
pairs and \code{"all"} uses both; those two report \code{group_a} and \code{group_b} \strong{in place of}
\code{group}, one row per unordered pair of groups, which is the shape
\code{\link[=plot_pairwise_block_extension]{plot_pairwise_block_extension()}} draws. Nothing about the statistic changes -- a
cross-group pair is still measured against its own segments off the locus chromosome --
so the diagonal of an \code{"all"} result is exactly the \code{"within"} result.}

\item{min_ref_blocks}{Segments a pair needs off the locus chromosome before its baseline
is usable (default \code{1}). One is deliberate rather than lax: a pair with a single
segment genome-wide is a distant pair, and that segment is length-biased long because
short ones fell under the floor, so a thin baseline reads long and pushes the ratio
\emph{down}. Raising this is conservative in the wrong direction -- it drops whole groups
while rarely moving a conclusion.}

\item{min_pairs}{Pairs a group needs at a locus before it is tested (default \code{5}).
Thinner strata are dropped rather than reported untested.}

\item{adjust}{Scope of the Benjamini-Hochberg correction, which is a real choice and not
a detail. \code{"per_locus"} (default) makes one family of the groups tested at each locus,
which is right for a handful of hand-picked loci. \code{"per_group"} makes one family of the
loci tested in each group, which is what a genome-wide scan wants. \code{"all"} is a single
family over every row, \code{"none"} leaves \code{q_paired} as \code{NA}.}

\item{keep_pair_ratios}{Attach the per-pair log ratios as the \code{pair_ratios} attribute
(default \code{TRUE}). Set \code{FALSE} for a genome-wide scan: on one real cohort that attribute
was 288 MB of a 330 MB result, and nothing downstream of a scan reads it.}

\item{meta}{Sample metadata; taken from \code{x} when not given.}
}
\value{
A tibble, one row per locus x group, ordered by locus then descending
\code{paired_ratio}, carrying the per-pair ratios it was built from on the \code{pair_ratios}
attribute (\code{locus}, \code{group}, \code{sample1}, \code{sample2}, \code{locus_len}, \code{ref_n}, \code{ref_median},
\code{log2_ratio}):
\describe{
\item{\code{locus}, \code{name}, \code{gene_id}}{locus labels, as \code{\link[=gene_ibd_overlap]{gene_ibd_overlap()}} uses them.}
\item{\code{chr}, \code{start}, \code{end}, \code{span_bp}}{the interval tested, 0-based half-open.
Under \code{sharing = "complete"}, \code{span_bp} is a floor on \code{locus_median}, so compare
loci of like width.}
\item{\code{group}}{the group both samples of every counted pair belong to.}
\item{\code{n_pairs}}{pairs IBD across the locus with a usable baseline.}
\item{\code{gw_median}}{that group's genome-wide median segment length, over all its
within-group pairs.}
\item{\code{locus_median}}{median segment length across the locus.}
\item{\code{naive_ratio}}{\code{locus_median / gw_median}. Descriptive only.}
\item{\code{ref_median}}{median over the counted pairs of each pair's own baseline. Well
above \code{gw_median} means the sharing pairs are unusually related.}
\item{\code{paired_ratio}}{the statistic. \code{1} means a pair's segment at the locus is no
longer than its segments elsewhere.}
\item{\code{z_paired}}{\code{median(log2_ratio)} over the standard error of their mean. A rough
effect size for ranking loci, not the test.}
\item{\code{p_paired}, \code{p_two_sided}}{one-sided (\code{greater}) and two-sided signed-rank
p-values. Read the two-sided column before calling anything shorter: a one-sided p
near \code{1} means "not longer", never "significantly shorter". Ties use the normal
approximation.}
\item{\code{q_paired}}{\code{p_paired} adjusted within the family set by \code{adjust}.}
}
}
\description{
For each locus and each group of samples, compares the length of the IBD segment a pair
shares \strong{across the locus} with the length of that same pair's segments elsewhere in the
genome. Long segments at a locus are the footprint of a recent sweep, but raw length is
not comparable between groups: a group with less outcrossing carries longer segments
genome-wide, so the naive comparison can run backwards. Making each pair its own control
removes that, and makes the pair -- not the segment -- the unit of observation, which is
what the signed-rank test needs.
}
\details{
Two statistics come back, and only one of them is testable:
\itemize{
\item \code{naive_ratio} is the locus median segment length over the group's genome-wide median.
Numerator and denominator come from different sets of pairs, because pairs IBD at a
locus are selected for being related enough to share it. There is no error model behind
it, so it gets no p-value. It is reported to be compared against the next one.
\item \code{paired_ratio} is \verb{2^median(log2(locus segment / that same pair's median segment on every other chromosome))}. Background relatedness cancels pair by pair. \strong{This is the
one to quote.} \code{p_paired} tests it.
}

Where a locus's \code{ref_median} sits well above \code{gw_median}, the pairs sharing it are
unusually related and \code{naive_ratio} was counting that background rather than anything
local.

Each pair's baseline excludes the \strong{whole locus chromosome}, so a sweep's own footprint
cannot leak into the reference and flatten the ratio. Segments come from
\code{x$get_blocks()}, which has already dropped non-IBD rows and applied the short-segment
floor; taking a filtered numerator against an unfiltered denominator inflates these
ratios several-fold, so do not re-read the raw \code{hmm.txt} for one side of it.

Only pairs whose two samples fall in the same group are used, since a cross-group pair
has no single group to report under.

This measures segment \emph{length}. The count analogue, XiR,s (Henden et al. 2018,
\doi{10.1371/journal.pgen.1007279}), is computed by the companion Python pipeline and is
generally the better-powered statistic; treat this as its complement rather than its
replacement. For background on excess IBD as a selection signal see Albrechtsen et al.
(2010, \doi{10.1534/genetics.110.113977}); for the principled coalescent-time version of
what this ratio approximates, ASMC (Palamara et al. 2018,
\doi{10.1038/s41588-018-0177-x}).
}
\section{Reading it}{

Do not run the test over segments instead of pairs. One pair contributes several
correlated segments and the p-value comes out far too small. Splitting the pairs by
carriage of a core variant, which is the IBD analogue of iHS (see \code{\link[=run_ihs]{run_ihs()}}), turns a
statement about a group into one about an allele.

A \code{paired_ratio} of 1 is \strong{not} the null. A locus is a fixed target, and a segment
covers it with probability rising in the segment's own length, so the numerator is drawn
from a length-biased distribution while each pair's baseline is not. Every locus is
inflated by this, whether or not anything happened there. Read a locus against the
distribution from scanning every gene in the same group, not against 1.
}

\section{Why \code{sharing} changes what locus width means}{

Under \code{"overlap"} a segment counts if it touches the locus, so the selection weight rises
with segment length \emph{plus} locus width. Since a gene is small against a typical IBD
segment, every gene behaves like a point and takes the same length bias: on one real
cohort, locus width explained an R-squared below 0.002 of \code{log2(paired_ratio)}, and
binning the scan by width only added noise.

Under \code{"complete"} the segment must cover the locus, so it cannot be shorter than the
locus is wide. Locus width becomes a hard floor under \code{locus_median}, and the wider the
locus the more of the short-segment end of the distribution is cut away. The ratio then
rises with width for reasons that have nothing to do with selection, and loci of
different widths are no longer on one scale. So with \code{"complete"}, compare like widths --
bin the scan by \code{span_bp} -- or stay with \code{"overlap"} when ranking genes against each
other.
}

\examples{
# five related pairs, each sharing 80 kb across the locus and 20 kb elsewhere
pairs <- data.frame(sample1 = paste0("s", seq(1, 9, by = 2)),
                    sample2 = paste0("s", seq(2, 10, by = 2)))
blocks <- rbind(
  data.frame(pairs, chr = "7", start = 3.6e5, end = 4.4e5),   # over the locus
  data.frame(pairs, chr = "1", start = 1e5,   end = 1.2e5),   # baseline
  data.frame(pairs, chr = "2", start = 1e5,   end = 1.2e5))
blocks$different <- 0
blocks$Nsnp <- 40
meta <- data.frame(sample = paste0("s", 1:10), region = "north")
ibd <- ibd_results(blocks = blocks, meta = meta, group_col_in_meta = "region")
locus <- data.frame(name = "sweep", chr = "7", start = 4e5, end = 4.1e5)
ibd_block_extension_test(ibd, locus)[, c("locus", "group", "n_pairs", "paired_ratio")]
}
\seealso{
\code{\link[=gene_ibd_pairs]{gene_ibd_pairs()}} for the pairs themselves, \code{\link[=gene_ibd_overlap]{gene_ibd_overlap()}} for the
fraction of pairs sharing at a locus (the count side of the same question),
\link{plasgenomicsutilsR-coordinates} for the interval convention.
}
