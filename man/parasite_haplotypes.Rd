% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ihs.R
\name{parasite_haplotypes}
\alias{parasite_haplotypes}
\title{Build phased haplotypes for a haplotype-homozygosity scan}
\usage{
parasite_haplotypes(
  x,
  samples = NULL,
  fws = NULL,
  min_fws = IHS_MIN_FWS,
  het = c("sample", "missing"),
  maf = IHS_MIN_MAF,
  max_snp_missing = 0.1,
  max_sample_missing = 0.2,
  impute = TRUE,
  seed = 42,
  meta = NULL,
  genotype = NULL
)
}
\arguments{
\item{x}{A \link{PopStructure} or a genotype matrix (samples x SNPs, alt dosage 0/1/2,
\code{NA} missing, \code{chr:pos} column names).}

\item{samples}{Restrict to these samples before anything else.}

\item{fws}{Per-sample Fws: a named numeric vector, or a data frame with \code{sample} and
\code{fws} columns (e.g. read from \verb{plasgenomicsutils calculate_fws}). \code{NULL} skips the
monoclonal gate.}

\item{min_fws}{Fws floor for the gate (default 0.95).}

\item{het}{\code{"sample"} draws the allele at the population frequency; \code{"missing"} leaves
the call to imputation.}

\item{maf}{Minor-allele frequency floor (default 0.03).}

\item{max_snp_missing, max_sample_missing}{Missingness ceilings for SNPs and samples.}

\item{impute}{Fill remaining gaps by drawing at the SNP's allele frequency. \code{FALSE}
instead drops every SNP that still has a gap.}

\item{seed}{Random seed.}

\item{meta, genotype}{As in \code{\link[=pop_diversity]{pop_diversity()}}.}
}
\value{
A \code{parasite_haplotypes} object: \code{hap} (samples x SNPs, 0/1), \code{map}
(\code{chr}, \code{pos}, \code{snp_id}), \code{meta}, and a \code{filtering} record of what was dropped.
}
\description{
Turns a genotype matrix into the complete, unambiguous 0/1 haplotypes that
\code{\link[=run_ihs]{run_ihs()}} and its cross-population relatives need.
}
\details{
The steps, in order, each reported in the result:
\enumerate{
\item \strong{Monoclonal gate.} With \code{fws} supplied, samples below \code{min_fws} are dropped: a
polyclonal infection is a mixture of haplotypes, not one haplotype.
\item \strong{Mixed calls.} Whatever heterozygous calls remain are either resolved by drawing
the allele at the population frequency (\code{het = "sample"}, the usual choice for
\emph{Plasmodium}) or set to missing.
\item \strong{Filtering.} SNPs below \code{maf} or above \code{max_snp_missing}, then samples above
\code{max_sample_missing}.
\item \strong{Imputation.} Remaining gaps are filled by drawing at the SNP's allele frequency,
so the matrix is complete.
}

Both the allele draw and the imputation are random; \code{seed} makes a run reproducible.
Because the result depends on that draw, a signal worth reporting should survive
repeating the whole thing under a different seed.
}
\examples{
ps <- example_pop_structure(umap = FALSE)
parasite_haplotypes(ps, maf = 0.05)
}
\seealso{
\code{\link[=run_ihs]{run_ihs()}}, \code{\link[=run_rsb]{run_rsb()}}, \code{\link[=run_xpehh]{run_xpehh()}}
}
