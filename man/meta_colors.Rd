% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colors.R
\name{meta_colors}
\alias{meta_colors}
\title{Assign colours to the levels of metadata columns}
\usage{
meta_colors(meta, cols = NULL, overrides = NULL)
}
\arguments{
\item{meta}{A data frame of metadata.}

\item{cols}{Columns to build colours for (default: all but a \code{sample} column).}

\item{overrides}{Optional named list \code{column -> (level -> colour)} to override
individual assignments (e.g. \code{list(country = c(Ethiopia = "#FFDC3D"))}).}
}
\value{
A named list, one \code{level -> colour} named vector per column.
}
\description{
Builds a named list (one entry per column) of \verb{level -> hex colour} vectors, so a
single mapping can colour points in one plot and bars in another. Levels are taken
in factor order (or sorted); a colour-blind-friendly palette is chosen by level
count (interpolated past 15). \code{overrides} replaces specific colours.
}
\examples{
meta_colors(data.frame(sample = 1:3, region = c("A", "B", "A")))
}
