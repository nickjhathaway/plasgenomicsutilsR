% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pop_structure.R
\name{run_snmf}
\alias{run_snmf}
\title{Run sNMF (LEA) admixture on a genotype matrix}
\usage{
run_snmf(
  geno,
  K = 1:10,
  rep = 10,
  alpha = 10,
  seed = 42,
  cpu = 1,
  cache = TRUE,
  cache_dir = NULL,
  verbose = FALSE,
  log_file = NULL
)
}
\arguments{
\item{geno}{A genotype matrix (samples x SNPs, 0/1/2, \code{NA} allowed) or the list
from \code{\link[=load_genotypes]{load_genotypes()}}.}

\item{K}{Integer vector of ancestral-population counts to fit (default \code{1:10}).}

\item{rep}{Repetitions per \code{K}.}

\item{alpha}{Regularisation.}

\item{seed}{Random seed.}

\item{cpu}{CPU cores.}

\item{cache}{Reuse a previously computed project when the genotypes and all
parameters match (sNMF is slow); set \code{FALSE} to always recompute.}

\item{cache_dir}{Directory for the cached \code{.geno}/project (default a per-session
temp dir). Point it at a persistent path to reuse across sessions.}

\item{verbose}{Show LEA's (voluminous) console output. Default \code{FALSE} runs it
quietly; set \code{log_file} to capture it instead of discarding it.}

\item{log_file}{Optional file to write LEA's output to when \code{verbose = FALSE}.}
}
\value{
An \code{snmf_fit} object: a list with the LEA \code{project}, the fitted \code{K} range,
and \code{samples}.
}
\description{
Writes a \code{.geno} file (missing coded as 9) and runs \code{\link[LEA:snmf]{LEA::snmf()}} over a range of
\code{K}, returning the sNMF project. Cross-entropy is computed so \code{\link[=snmf_best_k]{snmf_best_k()}} can
pick \code{K}.
}
\examples{
\dontrun{
fit <- run_snmf(geno, K = 1:10, rep = 10, cache_dir = "snmf_cache")
snmf_best_k(fit)
}
}
