% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ibd_block_extension_by_allele.R
\name{ibd_block_extension_by_allele}
\alias{ibd_block_extension_by_allele}
\title{Split IBD block extension at a locus by carriage of a variant}
\usage{
ibd_block_extension_by_allele(
  x,
  loci,
  allele,
  carrier = NULL,
  reference = NULL,
  group = NULL,
  within = 0,
  sharing = c("overlap", "complete"),
  min_ref_blocks = 1L,
  min_pairs = 5L,
  adjust = c("per_locus", "per_group", "all", "none"),
  meta = NULL
)
}
\arguments{
\item{x}{An \link{IbdResults} built with \verb{blocks =} and \verb{meta =}.}

\item{loci}{Loci to test, as \code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}} takes them.}

\item{allele}{The variant to split on: the name of a metadata column, or a named vector
of \code{sample -> state}. Samples with \code{NA} are left out of every stratum.}

\item{carrier, reference}{Which states count as carrying and as reference. Default: the
column's first two levels, \code{reference} first, with a message saying which way round it
was read. Name them explicitly when the labels are not self-evident.}

\item{group, within, sharing, min_ref_blocks, meta}{Passed to \code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}}.}

\item{min_pairs}{Pairs a stratum needs at a locus before its length statistic is computed
(default \code{5}). The fraction statistic is reported whatever the count, since it has a
denominator either way.}

\item{adjust}{Benjamini-Hochberg scope for \code{p_length} and \code{p_fraction}, as in
\code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}}.}
}
\value{
A tibble, one row per locus x group, ordered by locus then by \code{p_fraction}:
\describe{
\item{\code{locus}, \code{name}, \code{gene_id}, \code{chr}, \code{start}, \code{end}, \code{span_bp}, \code{group}}{as in
\code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}}.}
\item{\code{n_carrier}, \code{n_reference}, \code{n_discordant}}{pairs IBD across the locus in each
stratum. Report all three; see the note on the discordant one.}
\item{\code{ratio_carrier}, \code{ratio_reference}}{\code{paired_ratio} within each stratum.}
\item{\code{ratio_contrast}}{\code{ratio_carrier / ratio_reference} on the per-pair scale, so
\code{1} means carriage makes no difference to segment length.}
\item{\code{p_length}}{two-sided rank-sum test of the two strata's per-pair log ratios.}
\item{\code{n_carrier_possible}, \code{n_reference_possible}}{pairs that \emph{could} share, from the
analyzed samples of each stratum.}
\item{\code{frac_carrier}, \code{frac_reference}}{sharing pairs over possible pairs.}
\item{\code{odds_ratio}, \code{p_fraction}}{Fisher's exact test of those two fractions. The
better-powered comparison.}
\item{\code{q_length}, \code{q_fraction}}{adjusted within the family set by \code{adjust}.}
}
The per-stratum full statistics are on the \code{strata} attribute, and the per-pair ratios,
labelled by stratum, on \code{pair_ratios}.
}
\description{
The allele-level form of \code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}}. Within each group, the pairs are
split by their state at a core variant into carrier/carrier, reference/reference and
discordant, the same per-pair extension statistic is computed in each stratum, and the two
matching strata are compared. Extension measured over all pairs says a locus is unusual in
a group; this says whether it is the \emph{haplotype} that is unusual, which is a different and
stronger claim.
}
\details{
Two statistics come back per locus and group, and they answer different questions:
\itemize{
\item \strong{length}, \code{ratio_carrier} against \code{ratio_reference}, tested by \code{p_length}. Among pairs
that share the locus at all, are the carriers' segments the longer ones?
\item \strong{fraction}, \code{frac_carrier} against \code{frac_reference}, tested by \code{p_fraction}. Of all
the pairs that \emph{could} share, how many do? This is usually the better powered of the
two, because it uses every pair rather than only the sharing ones, so read it first.
}
}
\section{The discordant stratum is a check, not a result}{

A pair sharing an interval by descent shares whatever allele sits in it, so pairs that are
IBD across the locus and discordant at the variant should be rare. A large
\code{n_discordant} points at genotyping error, at a recombination inside the interval, or at
the interval being wider than the haplotype -- look there before reading anything else.
}

\examples{
\dontrun{
ibd <- ibd_results(blocks = "hmm.txt", meta = meta, group_col_in_meta = "region")
ibd_block_extension_by_allele(ibd, loci = "pfpx1", allele = "PIN_Haplotype",
                              carrier = "Present", reference = "Absent")
}
}
\seealso{
\code{\link[=ibd_block_extension_test]{ibd_block_extension_test()}}, \code{\link[=ibd_block_extension_scan]{ibd_block_extension_scan()}} for the genome-wide
background, \code{\link[=run_ihs]{run_ihs()}} for the haplotype-length statistic this mirrors.
}
