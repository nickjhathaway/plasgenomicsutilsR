% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ibd_pair_clusters.R
\name{ibd_pair_clusters}
\alias{ibd_pair_clusters}
\title{Genome-wide IBD clusters, and which samples are connected}
\usage{
ibd_pair_clusters(
  pairs,
  weight = NULL,
  min_ibd = 0.01,
  samples = NULL,
  add_meta_cols = NULL,
  meta = NULL
)
}
\arguments{
\item{pairs}{The per-pair table from \verb{plasgenomicsutils ibd_fraction_and_snp_density}
(\verb{*.pair_ibd_fraction.tsv.gz}): a path, a data frame, or an \link{IbdResults} carrying one.}

\item{weight}{Column holding the fraction. Defaults to \code{ibd_fraction_accessible}, the
callable-genome denominator; \code{ibd_fraction_full_genome} divides by the whole genome
instead, so it reads lower for the same pair.}

\item{min_ibd}{Count a pair as linked only above this fraction (default \code{0.01}). Pass what
you plotted with, or the clusters will not match the components.}

\item{samples}{Optional subset of samples to keep.}

\item{add_meta_cols}{Metadata columns to carry onto each row, by sample. A sample missing
from \code{meta} gets \code{NA}; factor columns keep their level order, so a region stays in map
order rather than turning alphabetical.}

\item{meta}{Sample metadata for \code{add_meta_cols}; taken from the \link{IbdResults} when \code{pairs}
is one, so it only needs giving alongside a plain table.}
}
\value{
A tibble with one row per sample, connected samples first (largest cluster first,
then by name) and the unconnected after them:
\describe{
\item{\code{sample}}{the sample.}
\item{\code{connected}}{whether it shares more than \code{min_ibd} with any other sample.}
\item{\code{cluster_id}, \code{cluster_size}}{its single-linkage cluster and how many samples are
in it; \code{NA} when it is connected to nobody.}
\item{\code{n_links}}{how many samples it is linked to.}
\item{\code{max_ibd}}{the largest fraction it shares with any of them, \code{NA} when none.}
}
plus one column for each of \code{add_meta_cols}.
}
\description{
Who is related to whom across the genome as a table: one row per sample, saying whether it
shares more than \code{min_ibd} with anyone, and which single-linkage cluster it falls in. These
are the components \code{\link[=plot_ibd_pair_network]{plot_ibd_pair_network()}} draws -- the connected blobs and the grid of
unconnected samples beneath them -- so the same \code{weight} and \code{min_ibd} give the same answer
the picture shows.
}
\details{
Single linkage means a sample joins a cluster if it shares with \strong{any} member, so a chain
of pairs is one cluster even where its ends never share directly. Ids run largest cluster
first, so \code{cluster_id == 1} is the biggest group; a sample sharing with nobody gets \code{NA}.
\code{\link[=add_ibd_clusters]{add_ibd_clusters()}} and \code{\link[=gene_ibd_pairs]{gene_ibd_pairs()}} number their per-gene clusters the same way.
}
\examples{
\dontrun{
cl <- ibd_pair_clusters(ibd_all, min_ibd = 0.03)

# the connected samples, and the unconnected ones
cl$sample[cl$connected]
cl$sample[!cl$connected]

# the biggest cluster
subset(cl, cluster_id == 1)

# with where each sample came from, to see whether a cluster spans sites
cl <- ibd_all$ibd_pair_clusters(min_ibd = 0.03, add_meta_cols = c("region", "country"))
with(subset(cl, connected), table(cluster_id, region))

# colour the network by cluster, the way add_ibd_clusters() does per gene
ibd_all$set_meta(merge(ibd_all$get_meta(), cl[, c("sample", "cluster_id")], by = "sample"))
ibd_all$plot_ibd_pair_network(min_ibd = 0.03, color_group = "cluster_id")
}
}
\seealso{
\code{\link[=plot_ibd_pair_network]{plot_ibd_pair_network()}} for the picture, \code{\link[=ibd_pair_links]{ibd_pair_links()}} for the edges
themselves, \code{\link[=add_ibd_clusters]{add_ibd_clusters()}} and \code{\link[=gene_ibd_pairs]{gene_ibd_pairs()}} for the per-gene equivalents.
}
