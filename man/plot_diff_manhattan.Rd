% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jost_d.R
\name{plot_diff_manhattan}
\alias{plot_diff_manhattan}
\title{Genome-wide differentiation Manhattan plot}
\usage{
plot_diff_manhattan(
  pd,
  pair = NULL,
  combine = c("max", "mean"),
  reference = DEFAULT_REFERENCE,
  chroms = NULL,
  skip_chr = NULL,
  point_size = 0.6,
  point_alpha = 0.6,
  colours = NULL,
  colors = NULL
)
}
\arguments{
\item{pd}{A \code{\link[=pop_diff]{pop_diff()}} / \code{\link[=jost_d]{jost_d()}} result with \code{chr:pos} SNP ids.}

\item{pair}{Optional single group pair to plot: \code{c("A", "B")} or \code{"A vs B"}. Default
combines all pairs per SNP.}

\item{combine}{How to collapse pairs per SNP when \code{pair} is \code{NULL}: \code{"max"} (default)
or \code{"mean"}.}

\item{reference}{Reference id for the chromosome layout (default \code{DEFAULT_REFERENCE}).}

\item{chroms, skip_chr}{Optional chromosomes to keep / drop (the rest re-laid-out).}

\item{point_size, point_alpha}{Point aesthetics.}

\item{colours, colors}{Optional length-2 colour vector for the alternating chromosome bands.}
}
\value{
A ggplot object.
}
\description{
The per-SNP differentiation statistic along the genome, styled like the IBD Manhattan
plots. By default it collapses the pairwise comparisons to one value per SNP
(\code{combine = "max"}, the strongest differentiation at that SNP in any pair); pass
\code{pair} to plot a single group pair instead.
}
