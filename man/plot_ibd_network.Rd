% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ibd_network.R
\name{plot_ibd_network}
\alias{plot_ibd_network}
\title{IBD network at a gene or locus}
\usage{
plot_ibd_network(
  x,
  gene = NULL,
  locus = NULL,
  group = NULL,
  within = 0,
  include_isolated = FALSE,
  layout = "fr",
  spread = 1.5,
  node_size = 3,
  node_alpha = 0.9,
  edge_colour = "grey65",
  edge_alpha = 0.5,
  edge_width = 1,
  colours = NULL,
  title = NULL,
  subtitle = TRUE,
  seed = 42
)
}
\arguments{
\item{x}{An \link{IbdResults} with IBD \code{blocks}.}

\item{gene}{A single gene name from the object's track (its interval is used).}

\item{locus}{Alternatively, a locus: \code{"chr:pos"}, \code{"chr:start-end"}, or a one-row data
frame with \code{chr}, \code{start}, \code{end}. Give exactly one of \code{gene} / \code{locus}.}

\item{group}{Optional metadata column to colour nodes by (needs \code{meta}).}

\item{within}{Pad the interval by this many bp on both sides (default \code{0}).}

\item{include_isolated}{Keep samples with no IBD edge (default \code{FALSE}). \code{TRUE} also
shows every analyzed sample; the isolated samples are drawn in a grid below the
connected component (sorted by \code{group} when colouring by one). \code{FALSE} shows only the
connected nodes.}

\item{layout}{A \pkg{ggraph}/\pkg{igraph} layout name (default \code{"fr"}, Fruchterman-
Reingold). Other options include \code{"kk"} (Kamada-Kawai), \code{"stress"}, \code{"drl"},
\code{"circle"}, \code{"nicely"}, \code{"graphopt"}, and \code{"lgl"}. The connected component is laid out
on its own, with its aspect preserved.}

\item{spread}{How strongly to open up densely inter-connected groups (default \code{1.5}).
Edge attraction is weighted by \code{(1 - J)^spread}, where \code{J} is the Jaccard overlap of
the two samples' IBD neighbourhoods: edges inside a near-clique pull only weakly, so
the group expands enough to resolve individual samples, while edges linking otherwise
separate groups keep their full pull and cluster separation is preserved. \code{0} leaves
edges unweighted; raise it above the default to loosen the densest groups further, at
the cost of them taking up more of the canvas. Applies to weight-aware layouts
(\code{"fr"}, \code{"kk"}, \code{"drl"}, \code{"stress"}, ...); others are unaffected.}

\item{node_size, node_alpha}{Node point aesthetics.}

\item{edge_colour, edge_width}{Edge aesthetics (default width \code{1}).}

\item{edge_alpha}{Edge opacity (default \code{0.5}). \code{NULL} instead scales opacity down with
edge count (\code{~120 / n_edges}, clamped to [0.06, 0.6]).}

\item{colours}{Optional named \code{group -> colour} vector for the node fill.}

\item{title}{Plot title: \code{NULL} (default) uses \code{"IBD network: <label>"}, a string sets a
custom title, and \code{NA}/\code{FALSE} draws no title.}

\item{subtitle}{Show the sample / IBD-pair count line under the title (default \code{TRUE}).}

\item{seed}{Random seed for stochastic layouts (e.g. \code{"fr"}).}
}
\value{
A ggplot (\pkg{ggraph}) object.
}
\description{
A sample-level network for one gene or locus: each node is a sample and an edge joins
two samples whose pair shares an \strong{IBD block overlapping} the interval. Optionally
colours nodes by a metadata group, and optionally drops isolated (unconnected) nodes
for a cleaner picture -- or keeps them so the total sample count is visible.
}
\details{
Needs an \link{IbdResults} built with \verb{blocks =} (and \verb{meta =} for grouping / the full
sample set). See \code{\link[=gene_ibd_overlap]{gene_ibd_overlap()}} for the pairwise-fraction summary of the same data.
}
\examples{
\dontrun{
ibd <- ibd_results(blocks = "hmm.txt", meta = meta, genes = PF_EXAMPLE_DRUG_GENES)
plot_ibd_network(ibd, gene = "pfcrt", group = "region")
plot_ibd_network(ibd, locus = "Pf3D7_07_v3:403500", include_isolated = TRUE)
}
}
