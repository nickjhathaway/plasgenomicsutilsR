% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colour_blindness.R
\name{plot_colour_blind_check}
\alias{plot_colour_blind_check}
\alias{plot_color_blind_check}
\title{Show how a set of palettes reads under colour blindness}
\usage{
plot_colour_blind_check(
  palettes,
  subtitles = NULL,
  title = NULL,
  caption = TRUE,
  vision = c("normal", "deutan", "protan", "tritan"),
  show_distance = TRUE,
  base_size = 11,
  label_angle = 40
)

plot_color_blind_check(
  palettes,
  subtitles = NULL,
  title = NULL,
  caption = TRUE,
  vision = c("normal", "deutan", "protan", "tritan"),
  show_distance = TRUE,
  base_size = 11,
  label_angle = 40
)
}
\arguments{
\item{palettes}{A named list of colour vectors -- one entry per legend in the figure
being checked, e.g. \code{list("Region colours" = region_cols, "Components" = k_cols)}.
A bare colour vector is treated as a single unnamed palette.}

\item{subtitles}{Optional character vector, one per palette, describing where each is
used (e.g. \code{"UMAP points and the strip over each admixture panel"}).}

\item{title}{Overall figure title; \code{NULL} for none.}

\item{caption}{Explanatory caption under the figure: \code{TRUE} (default) for the standard
note on method and how to read the numbers, \code{FALSE} for none, or your own string.}

\item{vision}{Vision types to draw, from \code{"normal"}, \code{"deutan"}, \code{"protan"}, \code{"tritan"}.}

\item{show_distance}{Put the worst-case distance in each row label (default \code{TRUE}).}

\item{base_size}{Base font size.}

\item{label_angle}{Angle for the swatch labels (default \code{40}; use \code{0} for short names).}
}
\value{
A \link[patchwork:patchwork]{patchwork::patchwork} object; \code{ggplot2::ggsave()} or \code{\link[=save_plot]{save_plot()}} it.
}
\description{
Draws each palette once per vision type -- normal, deuteranope, protanope, tritanope --
with the worst-case CIEDE2000 distance for that row, so an accessibility claim about a
figure can be shown rather than asserted. Pass the colour maps a figure actually uses
(one per legend) and the panels come out in the order given.
}
\details{
Names carry through: a named colour vector labels its swatches, and the names of
\code{palettes} become the panel titles.
}
\examples{
plot_colour_blind_check(list("A 6-colour palette" = color_palette(6)))
}
