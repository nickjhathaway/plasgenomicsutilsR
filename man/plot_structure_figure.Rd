% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure_figure.R
\name{plot_structure_figure}
\alias{plot_structure_figure}
\title{Combined UMAP + admixture figure}
\usage{
plot_structure_figure(
  x,
  group = NULL,
  colour = group,
  K = NULL,
  rows = NULL,
  orientation = c("vertical", "horizontal"),
  sample_order = NULL,
  umap_colour = colour,
  region_colours = NULL,
  cluster_colours = NULL,
  region_label = NULL,
  cluster_label = "Ancestry\\ncomponent",
  base_size = 11,
  border = TRUE,
  border_colour = "black",
  border_linewidth = 0.15,
  legend = "right",
  legend_point_size = 3.5,
  point_size = 1.6,
  point_border = NULL,
  point_stroke = 0.3,
  tags = NULL,
  tag_size = NULL,
  point_alpha = 0.8,
  umap_ratio = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  color = NULL,
  border_color = NULL
)
}
\arguments{
\item{x}{A \link{PopStructure} with a UMAP (\code{run_umap()}) and an sNMF fit (\code{run_snmf()}).}

\item{group}{Metadata column to facet/colour the admixture by (default the first
non-\code{sample} metadata column).}

\item{colour, color}{Metadata column supplying the shared region colours (default \code{group}).}

\item{K}{Number of ancestral populations: an integer for a specific K, or \code{NULL} /
\code{"best_k"} to use the cross-entropy best K (\link{PopStructure}'s \code{best_k()}).}

\item{rows}{A list of character vectors giving which \code{group} levels sit on each
admixture row, e.g. \code{list(c("DRC", "Kenya"), c("Tanzania", "Uganda"))}. Default: all
levels on one row. Levels not listed are dropped.}

\item{orientation}{\code{"vertical"} puts the UMAP above the admixture; \code{"horizontal"} puts
it to the left.}

\item{sample_order}{Optional explicit sample order (see \code{\link[=admixture_order]{admixture_order()}}); by
default computed once (within group) and reused across the figure.}

\item{umap_colour}{Metadata column colouring the UMAP points (default \code{colour}).}

\item{region_colours, cluster_colours}{Optional named colour vectors overriding the
region strip / K-cluster fills.}

\item{region_label}{Legend title for the region colours (default \code{colour}).}

\item{cluster_label}{Legend title for the ancestry fills. The K components are what
sNMF calls clusters, but this figure sits them beside a UMAP, where the visible
groupings are also clusters, so a legend reading "cluster" invites reading the two as
the same thing. The default wraps over two lines so the longer wording costs no
legend width.}

\item{base_size}{Base font size shared by every panel.}

\item{border}{Outline each sample's admixture bar (default \code{TRUE}) so neighbours with
nearly identical ancestry stay distinct.}

\item{border_colour, border_color, border_linewidth}{Colour and width of the per-sample outline.}

\item{legend}{Where to collect the shared legends (\code{"right"}, \code{"left"}, \code{"bottom"},
\code{"top"}).}

\item{legend_point_size}{Size of the region dots in the UMAP legend (default \code{3.5}, so
the key reads clearly next to the small plotted points).}

\item{point_size, point_alpha}{Size and opacity of the UMAP scatter points.}

\item{point_border}{Outline colour for the UMAP points (e.g. \code{"black"}), or \code{NULL}
(default) for unoutlined points. Outlined points use shape 21, so the region
colours drive \code{fill} rather than \code{colour}.}

\item{point_stroke}{Width of that outline (default \code{0.3}); ignored when
\code{point_border} is \code{NULL}.}

\item{tags}{Panel tags for a figure legend, e.g. \code{c("a", "b")} (or just \code{"a"}, which
continues to \code{"b"}): the first goes on the UMAP, the second on the admixture block.
\code{NULL} (default) draws no tags. Tagging with \code{\link[patchwork:plot_annotation]{patchwork::plot_annotation()}}
directly does not work here -- \code{tag_levels} would letter every one of the dozen
admixture panels separately.}

\item{tag_size}{Tag point size (default \code{base_size * 1.4}, bold).}

\item{umap_ratio}{Relative size of the UMAP vs the admixture block (a single number;
default 1 means roughly equal).}

\item{file}{Optional path to save to (via \code{\link[=save_plot]{save_plot()}}) at the auto-computed size.}

\item{width, height}{Optional output size in inches (default auto from sample counts,
row count, and orientation); also attached to the result as attributes.}
}
\value{
A \pkg{patchwork} object (invisibly if \code{file} is given), with \code{width}/\code{height}
attributes carrying the suggested output size.
}
\description{
Draws a UMAP scatter and an sNMF admixture bar plot as \strong{one} figure that shares a
single theme (matching font sizes), a single region colour map (so UMAP points and the
admixture colour strips match), and collected legends. The admixture is faceted by a
metadata column with a colour strip -- and no text label -- over each region, samples
ordered once and reused, laid out over one or more rows.
}
\examples{
\dontrun{
ps <- example_pop_structure("africa")
ps$run_snmf(K = 1:9)
plot_structure_figure(ps, group = "site",
  rows = list(c("DRC", "Ethiopia", "Sudan"),
              c("Kenya_East", "Kenya_West", "Tanzania_East", "Tanzania_West")))
}
}
