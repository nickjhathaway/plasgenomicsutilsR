% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jost_d.R
\name{top_differentiating_snps}
\alias{top_differentiating_snps}
\title{The SNPs that most differentiate groups}
\usage{
top_differentiating_snps(jd, n, method = c("roundrobin", "max"))
}
\arguments{
\item{jd}{A \code{\link[=pop_diff]{pop_diff()}} / \code{\link[=jost_d]{jost_d()}} result.}

\item{n}{How many SNPs to return.}

\item{method}{\code{"roundrobin"} or \code{"max"}.}
}
\value{
A character vector of SNP ids (a subset of the result's SNPs).
}
\description{
Picks markers from a \code{\link[=pop_diff]{pop_diff()}} / \code{\link[=jost_d]{jost_d()}} result. \code{"roundrobin"} (default) walks
the pairwise comparisons taking each one's next-highest SNP in turn until \code{n} unique
SNPs are collected (so every pair contributes its top differentiators); \code{"max"} ranks
SNPs by their largest value across all pairs.
}
