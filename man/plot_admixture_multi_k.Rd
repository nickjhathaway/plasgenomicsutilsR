% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pop_structure.R
\name{plot_admixture_multi_k}
\alias{plot_admixture_multi_k}
\title{Admixture bar plots across every K, as pages}
\usage{
plot_admixture_multi_k(
  x,
  K = NULL,
  group = NULL,
  cross_entropy_first = TRUE,
  sample_order = NULL,
  sample_order_best_k = TRUE,
  best_k = NULL,
  stat = c("min", "mean"),
  meta = NULL,
  samples = NULL,
  ...
)
}
\arguments{
\item{x}{A \link{PopStructure} with a fitted sNMF, or an \code{\link[=run_snmf]{run_snmf()}} result (then give
\code{meta} and \code{samples}).}

\item{K}{Which K values to draw (default: every fitted K with a Q matrix, K >= 2 --
K = 1 is a single block and carries no information).}

\item{group}{Metadata column to facet by (e.g. \code{"region"}).}

\item{cross_entropy_first}{Lead with the cross-entropy elbow page (default \code{TRUE}).}

\item{sample_order}{Explicit sample order shared by every page. Overrides
\code{sample_order_best_k}.}

\item{sample_order_best_k}{Derive one shared sample order from the best K's Q and use
it on every page (default \code{TRUE}). \code{FALSE} lets each page cluster its own samples,
so bars move between pages.}

\item{best_k}{The K to treat as best; \code{NULL} (default) uses \code{\link[=snmf_best_k]{snmf_best_k()}}.}

\item{stat}{How \code{\link[=snmf_best_k]{snmf_best_k()}} and the elbow combine replicates: \code{"min"} (default)
or \code{"mean"}.}

\item{meta, samples}{Metadata and sample ids, needed only when \code{x} is a raw
\code{\link[=run_snmf]{run_snmf()}} result.}

\item{...}{Passed to \code{\link[=plot_admixture]{plot_admixture()}} (e.g. \code{group_bar}, \code{border}, \code{colours}).}
}
\value{
A named list of ggplots: \code{"cross_entropy"} (when asked for) then \code{"K=2"},
\code{"K=3"}, ... The best K's page is titled as such.
}
\description{
One \code{\link[=plot_admixture]{plot_admixture()}} per K, returned as a named list -- hand it straight to
\code{\link[=save_plot]{save_plot()}} for a multi-page PDF, one K per page. Optionally leads with the
cross-entropy elbow (\code{\link[=plot_snmf_cross_entropy]{plot_snmf_cross_entropy()}}) so the page that tells you which K to
believe comes first, with the best K marked in red.
}
\details{
Bars stay in the same place from page to page when a shared \code{sample_order} is used,
which is what makes the pages comparable: a sample sits at the same x on every K.
}
\examples{
\dontrun{
ps$run_snmf(K = 1:12)
pages <- ps$plot_admixture_multi_k(group = "region")
save_plot("admixture_all_k.pdf", pages)     # one K per page
}
}
\seealso{
\code{\link[=plot_snmf_cross_entropy]{plot_snmf_cross_entropy()}}, \code{\link[=plot_admixture]{plot_admixture()}}, \code{\link[=save_plot]{save_plot()}}
}
