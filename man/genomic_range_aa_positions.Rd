% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/range_aa_positions.R
\name{genomic_range_aa_positions}
\alias{genomic_range_aa_positions}
\title{The amino acids a genomic range covers}
\usage{
genomic_range_aa_positions(
  ranges,
  gff,
  keep = c("all", "hits"),
  partial = FALSE,
  fasta = NULL,
  genes = PF3D7_GENES,
  chrom = "chr",
  start = "start",
  end = "end"
)
}
\arguments{
\item{ranges}{An interval table (\code{chr} or \code{chrom}, \code{start}, \code{end}), such as
\code{\link[=bed_subtract]{bed_subtract()}} or \code{\link[=read_gff_features]{read_gff_features()}} return, or a gene table.}

\item{gff}{Path or URL to a GFF3, or the table \code{\link[=read_gff_cds]{read_gff_cds()}} returns -- parse once and
reuse it.}

\item{keep}{\code{"all"} (the default) keeps every input row, filling the result columns with
\code{NA} for an interval that covers no codon; \code{"hits"} drops those rows.}

\item{partial}{Count a codon the interval covers only partly (default \code{FALSE}).}

\item{fasta}{Optional sequence, as for \code{\link[=snp_aa_positions]{snp_aa_positions()}}: a path or URL to the genome
(gzipped is fine), or a named vector of sequences. When given, \code{aa_seq} holds the
reference residues of the covered codons, \code{aa_start} to \code{aa_end}, in transcript order;
a stop is \code{*} and an unreadable codon \code{X}. A GFF that ends in a \verb{##FASTA} section
supplies its own sequence.}

\item{genes}{A gene table with \code{name} and \code{gene_id}, used to fill \code{gene_name}. Defaults to
\link{PF3D7_GENES}; \code{NULL} leaves \code{gene_name} \code{NA}.}

\item{chrom, start, end}{Column names in \code{ranges} (defaults \code{"chr"}, \code{"start"}, \code{"end"};
\code{"chrom"} is accepted as a chromosome alias).}
}
\value{
A tibble with one row per interval and covered transcript: every column of
\code{ranges}, then \code{transcript_id}, \code{gene_id}, \code{gene_name}, \code{aa_start}, \code{aa_end}, \code{n_aa},
\code{partial_start}, \code{partial_end}, \code{strand}, \code{coding} (whether any codon was covered), and
\code{aa_seq} when sequence is available. Rows follow the input order; an interval covering
several transcripts has several rows.
}
\description{
For each interval, every transcript whose CDS it overlaps and the range of codons of
that transcript the interval covers -- the residues a target window, an amplicon, or a
piece of gene left after \code{\link[=bed_subtract]{bed_subtract()}} will actually read. Covered means the codon's
bases lie inside the interval, so a window that starts 100 bp upstream of a gene reports
from codon 1, and one that starts inside the gene reports from the first codon it holds
whole; nothing is inferred from the interval's first and last positions.
}
\details{
A codon is covered when all three of its bases are inside the interval. \code{partial = TRUE}
also counts a codon the interval holds only one or two bases of -- the codon it starts or
ends in -- and \code{partial_start} / \code{partial_end} say when that happened. Because an interval
is contiguous and a transcript's coding bases are in order along it, the covered codons of
one transcript are always a contiguous run, \code{aa_start} to \code{aa_end}.

Codon numbers are \strong{1-based}, counting the initiator methionine as 1, as everywhere in
the package (\link{plasgenomicsutilsR-coordinates}). A CDS that includes its stop codon reports
it as the last codon (425 for \emph{pfcrt}, whose protein is 424 residues), as
\code{\link[=snp_aa_positions]{snp_aa_positions()}} does; in \code{aa_seq} it is \code{*}. Genomic coordinates in \code{ranges} are 0-based
half-open, like every interval table.
}
\examples{
gff <- system.file("extdata", "pf3d7_drug_gene_cds.gff", package = "plasgenomicsutilsR")
cds <- read_gff_cds(gff)

# pfcrt with its tandem repeats removed: which residues does each piece still read?
crt <- PF3D7_GENES[PF3D7_GENES$name == "pfcrt", ]
pieces <- bed_subtract(crt, tandem_repeats_to_avoid(pf3d7_tandem_repeats()), pad = 10)
genomic_range_aa_positions(pieces, cds)[, c("piece", "start", "end", "aa_start", "aa_end",
                                            "n_aa")]

# the K76T codon itself, and a window holding only its middle base
k76 <- data.frame(chr = "Pf3D7_07_v3", start = c(403623, 403624), end = c(403626, 403625))
genomic_range_aa_positions(k76, cds)[, c("start", "end", "aa_start", "aa_end", "coding")]
genomic_range_aa_positions(k76, cds, partial = TRUE)[, c("start", "end", "aa_start",
                                                         "partial_start", "partial_end")]

# with sequence, the residues themselves
fa <- system.file("extdata", "pf3d7_drug_gene_regions.fasta.gz", package = "plasgenomicsutilsR")
genomic_range_aa_positions(k76[1, ], cds, fasta = fa)$aa_seq
}
\seealso{
\code{\link[=snp_aa_positions]{snp_aa_positions()}} for single positions, \code{\link[=aa_intervals]{aa_intervals()}} for the other
direction, \code{\link[=bed_subtract]{bed_subtract()}} and \code{\link[=read_gff_features]{read_gff_features()}} for where the intervals come from.
}
