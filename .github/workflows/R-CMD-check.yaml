# Run R CMD check (which runs the testthat suite) across operating systems and R
# versions. Standard r-lib/actions matrix workflow: macOS + Windows on R release, and
# Ubuntu on R devel / release / oldrel-1. setup-r-dependencies is biocViews-aware, so the
# Bioconductor Suggests (SNPRelate, gdsfmt, LEA) are installed and their code paths run.
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

name: R-CMD-check

permissions: read-all

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}
    name: ${{ matrix.config.os }} (${{ matrix.config.r }})
    # A full matrix leg runs in 5-10 min; the ceiling is for a cold dependency cache.
    # Without it a job that hangs in setup holds a runner for the 6-hour default, and the
    # run neither passes nor fails. R devel has no binary packages on Posit Package
    # Manager, so a cold cache there means every dependency (igraph, ggplot2, the
    # Bioconductor ones) is compiled from source -- more than 25 min. The cache is only
    # saved when the install step succeeds, so a ceiling the cold build cannot meet would
    # leave that leg failing on every run; hence its own, longer limits.
    timeout-minutes: ${{ matrix.config.job-timeout || 40 }}
    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: macos-latest,   r: 'release'}
          - {os: windows-latest, r: 'release'}
          - {os: ubuntu-latest,  r: 'devel', http-user-agent: 'release',
             deps-timeout: 60, job-timeout: 75}
          - {os: ubuntu-latest,  r: 'release'}
          - {os: ubuntu-latest,  r: 'oldrel-1'}
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      # Hosted runners have 4 cores. Let each package's own compile use them; the
      # package-level parallelism is `Ncpus` on setup-r below.
      MAKEFLAGS: -j4
    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      # Installing R takes well under a minute; a cap here turns the occasional hung
      # download into a failure the retry workflow can act on, rather than a stalled job.
      - uses: r-lib/actions/setup-r@v2
        timeout-minutes: 10
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true
          # setup-r writes `options(Ncpus = 1)` into the Rprofile by default, and pak
          # takes that as its number of install workers -- so with a cold cache it built
          # the 146 dependencies one at a time (32 min on R devel, where nothing is
          # binary). 4 workers matches the runner's cores.
          Ncpus: 4

      - uses: r-lib/actions/setup-r-dependencies@v2
        timeout-minutes: ${{ matrix.config.deps-timeout || 25 }}
        with:
          extra-packages: any::rcmdcheck
          needs: check

      - uses: r-lib/actions/check-r-package@v2
        with:
          upload-snapshots: true
          build_args: 'c("--no-manual","--compact-vignettes=gs+qpdf")'

  # `_pkgdown.yml` is in .Rbuildignore, so R CMD check never sees it: a vignette or topic
  # missing from the index only surfaces when a tag fires the site deploy, long after the
  # commit that caused it. Validating the config needs neither the package nor its
  # dependencies, so this is a separate job rather than a step in the matrix above.
  pkgdown-config:
    runs-on: ubuntu-latest
    name: pkgdown config
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      # check_pkgdown() builds the articles index, and that renders each vignette's title
      # and description through pandoc -- so it is needed even though nothing here builds
      # the site or the vignettes themselves.
      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        timeout-minutes: 10
        with:
          use-public-rspm: true

      - name: Install pkgdown
        run: install.packages("pkgdown")
        shell: Rscript {0}

      - name: Check _pkgdown.yml against the package
        run: pkgdown::check_pkgdown()
        shell: Rscript {0}
